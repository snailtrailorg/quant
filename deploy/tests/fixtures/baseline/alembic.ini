# 沙箱 alembic 配置（真 alembic + 本地 PG quant 库 sbx_deploy schema）
# DB 连串不经此文件：env.py 从环境读 QUANT_ALEMBIC_URL（quant-alembic-wrapper source shared/.env 注入）
[alembic]
script_location = migrations
# sqlalchemy.url 留空——env.py 环境变量优先（生产同形：连接串在 .env，属 quant 600）
