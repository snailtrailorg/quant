# 00 - 总体设计

## 1. 背景与目标

私有化部署的个人多市场量化交易平台，部署在 Linux 云服务器（Alibaba Cloud Linux 3 / OpenAnolis / al8 / RHEL8 系），7×24h 常驻运行。覆盖三类标的，全部可实盘（受实盘三级开关 + 分市场分项控制）：

| 市场 | 模式 | 交易通道 | 开关分项 |
|---|---|---|---|
| A股个股 | 日线选股 + 分钟级研判 + 实盘交易 | 中泰 XTP（vnpy_xtp） | `astock` |
| 可转债 / 场内ETF | 全自动 T+0 程序化交易 | 中泰 XTP（vnpy_xtp） | `convertible` / `etf` |
| 加密永续合约 | CTA 趋势（双均线）+ 网格/对冲（未来） | 币安/OKX（vnpy 加密网关，外部 gate） | `binance_perp` / `okx_perp` |

核心差异化：**多市场混合 + 配置驱动策略 + 平台化通用接口**——别人通过配置 + 实现接口子类即可接入自己的数据源/通道/AI/工具，不改平台代码。

## 2. 设计原则

1. **私有化优先**：数据、密钥、策略全部自留，无第三方托管。运行期 AI 只用国内模型（DeepSeek/火山方舟/GLM），不接 Anthropic/OpenAI 运行期。Claude Code 仅作开发助手。
2. **成熟开源优先，零自研底层**：交易/网关用 vnpy + vnpy_xtp，回测自建 BacktestEngine（纯 Python，不依赖 vnpy CtaBacktestingEngine）。
3. **统一模型结构、分离执行语义**：Strategy 基类 + Factor 注册制 + SignalAggregator + DSL，各市场执行规则下沉到 ExecutionAdapter。
4. **配置驱动而非硬编码**：因子组合/权重/参数走 Web 配置 + DSL 表达式，每策略改代码是错误做法。
5. **策略注册制 + 双模式统一执行**：`@register_strategy("type")` + `from_config` 按 type 分发。DSL 编译成 Python + 代码模式直接 Python，都成 Strategy 子类实例走 on_bar→ActionSignal→风控→执行。执行一套（区别于专家"双引擎两套执行"），可审计/AI 生成统一。
6. **回测与实盘同构零迁移**：数据 schema 与未来 XTP 实时行情对齐，on_bar 逻辑回测/实盘一致。
7. **平台化通用接口**：6 大接口抽象（DataSource / Broker / MessageChannel / Task / RiskRule / LLMProvider），基类 + DB 配置表 + 工厂 `get_<x>(provider)`，别人实现子类即接入。
8. **多模型 fallback**：LLM 网关按 `priority` 全局排序，primary + fallback 容灾（2026-08-07 移除 tier 分级）。
9. **实盘三级开关**（AND）：`.env ENABLE_LIVE_TRADING` 总闸 + Web `live_trading_config` 5 分项 + 策略 `enabled`+`backtest_verified`。任一关即拒单。
10. **轻量化**：venv + systemd，无 Docker；PostgreSQL 18 + pgvector + Valkey 统一承载，弃 TimescaleDB/重型向量库。

## 3. 总体架构（分层）

```
┌─────────────────────────────────────────────────────────────────┐
│  前端展示层  Vue3 + Element Plus + echarts/klinecharts            │
│  策略管理/回测可视化/实盘看板/风控/日志/数据/飞书/AI助手/系统设置   │
└───────────────┬─────────────────────────────────────────────────┘
                │ FastAPI REST + WebSocket（JWT 认证 + RBAC 四角色）
┌───────────────┴─────────────────────────────────────────────────┐
│  Web API 层（FastAPI ~110 端点）                                  │
│  认证/策略CRUD/回测组(并行/串行/SSE)/持仓盈亏/风控/实盘开关/       │
│  LLM 工具闭环/平台化CRUD(数据源/通道/规则/模型/预算/任务)          │
├─────────────────────────────────────────────────────────────────┤
│  调度与网关层                                                     │
│  ├─ Celery + Valkey: 选股/同步/报告/风控巡检/健康/回测分发       │
│  ├─ LLM 网关(自建): priority 全局主备容灾 + 工具白名单 + 熔断     │
│  ├─ 告警(自建): MessageChannel 接口 + 分级路由 + PG 持久化        │
│  └─ 后台任务管理(PT1): tasks/task_logs 统一 + 卡死检测 + 告警     │
├─────────────────────────────────────────────────────────────────┤
│  平台化通用接口层（6 大 ABC 接口 + 工厂）                          │
│  ├─ DataSource: Tushare/AkShare(补) → DB 化凭证                   │
│  ├─ Broker: XTP/Binance/OKX → DB 化凭证                            │
│  ├─ MessageChannel: WechatWork/Discord/Server酱 → DB 化            │
│  ├─ Task: 统一后台任务管理                                         │
│  ├─ RiskRule: max_position/max_single_order/daily_loss_limit       │
│  └─ LLMProvider: DeepSeek(主)+火山方舟+GLM(备) → DB 化             │
├─────────────────────────────────────────────────────────────────┤
│  策略框架层（自建，注册制 + 配置驱动）                              │
│  ├─ Strategy 基类 + @register_strategy + from_config(type) 分发   │
│  ├─ Factor 注册制 + DSL 安全 eval + 品类兼容校验                  │
│  ├─ 自定义因子（factor_def 表，用户 Web 写 Python 代码）           │
│  ├─ SignalAggregator + ActionSignal(volume_type/price_type)        │
│  ├─ 参数定义系统（parameter_defs 声明参数 schema，任务动态表单）     │
│  ├─ ExecutionAdapter: XTPAdapter / CryptoPerpAdapter / Backtest   │
│  └─ BacktestEngine: 纯 Python，on_bar_callback 实时推送           │
├─────────────────────────────────────────────────────────────────┤
│  实盘化层（策略与标的分离，一标的一进程）                            │
│  ├─ strategy_config 为策略配方（无标的绑定）                        │
│  ├─ live_task 为实盘任务（绑定策略+标的+任务参数）                  │
│  ├─ 创建时 strategy_snapshot 快照，后续改策略不影响已跑任务          │
│  └─ strategy_runner --task-id <live_task_id>:                       │
│     → 读 live_task + strategy_snapshot                              │
│     → broker_config DB 凭证 → MainEngine + XtpGateway              │
│     → connect → subscribe → tick → BarGenerator → on_bar           │
│     → place_order → 写 account_snapshot                             │
│     → 心跳(热加载) + 断线补缺口 + 因子重算触发                     │
├─────────────────────────────────────────────────────────────────┤
│  风控中心（自建）                                                  │
│  ├─ check_order 前置校验（三级开关 + 回撤/亏损 + 分市场规则）      │
│  ├─ 风控覆写 adjusted（超仓位截断 volume）                        │
│  └─ emergency_halt / resume（Valkey 无状态熔断）                   │
├─────────────────────────────────────────────────────────────────┤
│  数据中台（自建）                                                  │
│  ├─ Tushare(主)/AkShare(补) 拉取 + validate_bars 质量校验         │
│  ├─ PostgreSQL（业务 + bar_1D/1min/5min + pgvector）              │
│  ├─ Valkey（实时/熔断状态/进度/告警历史）                          │
│  └─ alembic schema 版本管理                                        │
└─────────────────────────────────────────────────────────────────┘
```

## 4. 模块清单

| # | 模块 | 职责 | 自建/第三方 | 文档 |
|---|---|---|---|---|
| 01 | LLM 网关 | 国内模型统一入口，priority 容灾 + 工具白名单(RBAC) + 熔断 + 用量日志 | **自建** | 01-llm-gateway.md |
| 02 | 策略框架 | Strategy 基类 + @register_strategy + Factor/DSL + BacktestEngine | **自建** | 02-strategy-framework.md |
| 03 | A股分析引擎 | 日线选股 + 分钟研判（MinuteAnalysisEngine） | **自建** | 03-astock-analysis.md |
| 04 | 可转债/ETF | 双低轮动（ConvertibleDoubleLowStrategy，@register_strategy） | 自建配置层 | 04-convertible-etf-engine.md |
| 05 | 加密合约 | CTA 趋势（CryptoCTATrendStrategy 双均线，@register_strategy） | 自建配置层 | 05-crypto-perp-engine.md |
| 06 | 数据中台 | Tushare/AkShare 拉取 + 清洗 + validate_bars + bar 表 | **自建** | 06-data-platform.md |
| 07 | 风控中心 | 三级开关 + 分市场风控(4/5 规则) + adjusted 覆写 + 熔断 | **自建** | 07-risk-control.md |
| 08 | Web 管理后台 | ~110 端点 + RBAC 四角色 + 邀请制用户管理 + 头像/个人中心 + N语言 | 自建 | 08-web-admin.md |
| 09 | 调度层 | Celery 定时任务 + beat + 进度 Valkey + cron 表达式 + 交易日历过滤 | 任务自建 | 09-scheduler.md |
| 10 | 告警/通知 | notify 统一入口 → notifications 表(站内铃铛) + risk+critical 外推 MessageChannel | **自建** | 10-alert-notify.md |
| 11 | 飞书/Lark | ws 长连接 + per-机器人角色 + 多机器人 + AI 工具 + 确认卡片 | **自建** | 11-feishu-lark.md |
| 12 | 实盘任务 | live_task 表（策略+标的+参数值+快照），一标的一进程，systemd 管理 | **自建** | 02-strategy-framework.md |
| SR | strategy_runner | 每策略独立子进程实盘入口（broker DB 凭证 + tick→on_bar） | **自建** | strategy-live-architecture.md |
| TM | task_manager | 后台任务统一管理 + 卡死检测 + 强制删除 + 告警 | **自建** | 接口契约 §4 |
| EM | 邮件发件箱 | email_outbox 持久化 + 指数退避重发 + SMTP DB 配置 + 发送日志 | **自建** | 08-web-admin.md §邮件 |
| NC | 通知中心 | notifications 表 + category×role 可见矩阵 + 铃铛轮询 + ack | **自建** | 10-alert-notify.md §通知中心 |
| — | vnpy 核心 + vnpy_xtp | 交易内核/网关（vnpy 4.x + vnpy_xtp，Linux 原生） | 第三方 | 不单列 |
| — | PostgreSQL 18 + pgvector + Valkey | 存储/缓存/向量/队列 | 第三方 | 不单列 |
| — | FastAPI / Celery / Vue3 / Element Plus | 框架 | 第三方 | 不单列 |

## 5. 模块间数据流

**行情流入**
```
Tushare/AkShare ──┐
XTP 实时 tick ────┤──> 数据中台(清洗/validate_bars) ──> PG(历史 bar)
                  │                                      │
                  └── strategy_runner: tick→BarGenerator→on_bar
```

**策略执行流（实盘）**
```
strategy_config（配方）+ live_task（任务，一标的一进程）
  → strategy_runner --task-id <live_task_id>
  → 读 strategy_snapshot + live_task.symbol + live_task.params
  → XtpGateway tick → BarGenerator → strategy.on_bar(bar, history)
    → 因子计算 → SignalAggregator → Signal
    → check_order(三级开关 + 风控规则 + adjusted 覆写)
    → XTPAdapter.send_order(Order)
    → _log_signal_order(写 signal_log + order_log)  ← 三账数据来源
    → record_broker_usage(写 broker_usage)
```

**策略执行流（回测）**
```
BacktestEngine.run(config, bars, on_bar_callback)
  → monkey-patch place_order(跳风控, BacktestAdapter 按收盘价成交)
  → 每 bar 推 progress/bar/trade/equity 到 Valkey → SSE 前端实时
  → validate_no_future_data + precheck_backtest_data(预检)
```

**AI 流（工具闭环）**
```
Web/飞书 → gateway.chat(messages, tools, role, caller)
  → LLM 返回 content + tool_calls
  → 循环执行 tool_calls(query_position/risk_state/astock_analysis...)
  → 结果回填 messages → 再 chat → 最终 content
  → 飞书操作类(emergency_halt/strategy_stop) → 确认卡片 → execute_confirmed_tool
```

**三账对账流**
```
策略 place_order → signal_log(信号) + order_log(委托)  ← backtest_runs.task_id 关联 tasks.id（PT1 卡死检测复用）
ExecutionAdapter 事件 → trade_log(成交)
reconcile_three_books → 比对(信号无委托/委托不成交/滑点异常) → 告警
```

**风控/通知流**
```
事件源(邮件失败/任务失败/风控/数据异常) → alert_notify.notify(level, category, title, body)
  → PG notifications(持久化) + 顶栏铃铛(60s轮询 active) → 点击按 category 跳转 → 全部确认
  → 仅 risk+critical 推外部通道(Discord/WechatWork)；订阅型 report() 保留外推
一键熔断 → emergency_halt(Valkey) → 所有策略 check_order 拒单
```

**邮件流**
```
接口触发(邀请/重置/开通) → queue_email(email_outbox 落PG) → try_row(立即试发)
  → 成功: status=sent
  → 失败: 指数退避重发(1→2→4→8→16→30min, 6次耗尽标failed → 通知中心铃铛)
Celery beat email_outbox_sweep 每分钟扫描待发
SMTP 配置走 system_config(smtp_*五项, 密码Fernet加密, 弃.env)
```

## 6. 跨模块核心接口

### 6.1 LLM 网关
```python
gateway.chat(messages, *, tools=None, role="viewer", timeout=30, retries=1, caller=None) -> LLMResponse
async gateway.chat_stream(messages, *, tools=None, role="viewer", caller=None) -> AsyncGenerator[str]
# 按 priority 全局排序 primary + fallback；熔断 closed/open/half_open（半开探测 + threading.Lock 并发安全）；
# 工具角色白名单∩（调用方只能缩小不能越权）；输入 50 万字符拒 + 超 max_input_tokens 截断
```

### 6.2 策略框架
```python
@register_strategy("type_name")
class MyStrategy(Strategy):
    def on_bar(self, bar: dict, history: list[dict]) -> Signal: ...

Strategy.from_config(config, adapter)  # 按 config.type 分发到注册子类

class Factor:
    def compute(self, ctx: BarContext) -> float  # BarContext.sma(n) 有缓存

class BacktestEngine:
    def run(self, config, bars, *, on_bar_callback=None) -> BacktestResult
    # 内含 validate_no_future_data + precheck_backtest_data
```

### 6.3 数据中台
```python
get_conn() -> Connection          # 连接池
save_bars(freq, rows) -> int     # validate_bars 入库前校验
get_bars(symbol, freq, start, end) -> DataFrame
is_trading_day(d) -> bool
platform.get_bar(symbol, freq, start, end) -> DataFrame  # DataPlatform 单例
```

### 6.4 风控中心
```python
RiskControl.get().check_order(order, account) -> RiskDecision
  # 三级开关(.env + Web 分项 + 策略) + 回撤/亏损 + 分市场(_check_etf_conv/_check_crypto)
  # 返回 approved/rejected + adjusted(覆写 volume)
RiskControl.get().emergency_halt(reason) / .resume()
RiskControl.get().is_halted()  # 直读 Valkey 不缓存
```

### 6.5 平台化通用接口（6 大）
```python
get_data_source(provider) -> DataSource | None    # TushareDataSource
get_broker(provider) -> Broker | None             # XTPBroker/BinanceBroker/OKXBroker
get_channel(provider) -> MessageChannel | None     # WechatWork/Discord/ServerChan
create_task(task_id, name, type, trigger, user, params)  # 统一后台任务
get_rule(type, params) -> RiskRule | None         # MaxPosition/MaxSingleOrder/DailyLossLimit
gateway = LLMGateway()  # 单例
# 每个：基类 + DB 配置表 + _REGISTRY + get_<x>(provider)
```

### 6.6 Web API 端点概览
```
认证: /api/auth/* (JWT+jti黑名单 + 邀请制 + 邮箱/用户名登录 + bcrypt + 找回密码)
用户: /api/user/profile /api/user/avatar /api/user/deactivate /api/invites/*
策略: /api/strategy/* (CRUD + start/stop/verify + systemd)
回测: /api/backtest/* (创建/列表/详情/汇总/SSE 实时流)
持仓: /api/position /api/pnl /api/orders /api/dashboard
风控: /api/risk/* /api/live-trading/* (三级开关)
LLM: /api/chat (工具闭环) /api/llm-models/* /api/llm-budget/* /api/llm-usage/summary
平台化: /api/data-sources/* /api/brokers/* /api/channels/* /api/risk-rules/*
任务: /api/tasks/* (统一后台任务 + 卡死检测)
飞书: /api/feishu/* (多机器人 + 扫码)
通知: /api/notifications (role过滤类别) /api/notifications/ack-all
邮件: /api/email-outbox /api/email/test /api/smtp-config
条款: /api/terms (公开, N语言items数组)
系统: /api/system-config/* (含smtp_*/base_url)
WS: /ws/chat (流式聊天+认证) /ws/market (行情占位)
```

## 7. 部署拓扑

**单机部署**（当前）：一台 Alibaba Cloud Linux 3 ECS，全部模块 systemd 守护。

| 服务 | 功能 | 端口 |
|---|---|---|
| `quant-web-api@quant` | FastAPI uvicorn | 8000（nginx 反代 443） |
| `quant-celery-worker@quant` | Celery 异步任务 | - |
| `quant-celery-beat@quant` | Celery 定时调度 | - |
| `quant-feishu-bot@<id>` | 飞书 ws 长连接（每机器人一实例） | - |
| `quant-strategy@<id>` | 策略实盘子进程（每策略一实例） | - |

- 线上地址：https://quant.snailtrail.cc
- polkit 规则：quant 用户可 `systemctl start/stop/restart quant-strategy@*`（Web 控制）
- 数据库定时备份：`backup-db.sh`（pg_dump + gzip + 保留 7 天）
- 资源限制：`MemoryMax`/`CPUQuota` cgroup 限策略进程
- **部署隔离铁律**（与 safebox 共存）：clear-redis 只 `FLUSHDB -n` 不 `FLUSHALL`；restart-web 用 `reload` 不 `restart`；clear-pg 严格 `DROP DATABASE quant` 不碰 safebox 库

## 8. 关键约束

1. **实盘三级开关**（AND，铁律）：`.env ENABLE_LIVE_TRADING` + Web `live_trading_config` 5 分项（convertible/etf/astock/binance_perp/okx_perp）+ 策略 `enabled`+`backtest_verified`。任一关 → `check_order` 拒单。
2. **运行期 AI 只用国内模型**：DeepSeek（主）/ 火山方舟（当前）/ GLM（备），不接 Claude/OpenAI 运行期。LLM 回复按输入语言自然回复（2026-08-07 移除 lang 注入）。
3. **Python 3.10 本地 / 3.11 服务器**：不用 3.14（vnpy 硬 pin PySide6）。
4. **回测自建 BacktestEngine**：纯 Python，不依赖 vnpy CtaBacktestingEngine。回测与实盘 on_bar 一致。
5. **平台化通用接口**：6 大接口按通用方向设计，实现可简化。别人配置 + 实现子类即接入。
6. **策略注册制**：新策略继承 Strategy + @register_strategy，from_config 按 type 分发。配置驱动非硬编码。
7. **可转债/ETF 实盘走中泰 XTP + vnpy_xtp**（Linux 原生），需中泰开户。
8. **配置驱动非硬编码**：策略因子组合/权重/参数走 Web 配置 + DSL，每个策略改代码是错误做法。
9. **schema 版本管理**：alembic 迁移文件为唯一真相源，不手动 ALTER。运行时 DDL 清零（2026-08-13），`verify_schema()` 启动校验。
10. **类型级同步异步化**：耗时同步操作走 Celery 异步 + Valkey 进度轮询。per-symbol 单只同步保持同步（快）。
11. **N 语言架构**：注册表驱动（en 缺省），加语言=只加条目零逻辑改动。条款全语言堆叠；邮件跟操作者界面语言；页面跟浏览器。
12. **错误码化**：`ApiError(status, CODE, 中文兜底)` → `{detail, code}`；前端 `apiErr(e)` 本地化映射+回落。
13. **SMTP 配置 DB 化**：`system_config` 表 smtp_* 五项（密码加密），弃 .env。条款单一源 terms.py。
14. **账户保护**：管理页不能动自己（末位 admin 隐式不可达）；自助注销路径末位 admin 显式设防。删除=软删+脱敏。

## 8.5 访问控制与角色（RBAC 四角色）

平台**单系统多用户**，四个角色共享同一套数据，差异仅在**能做什么**。非多租户，数据不加 `user_id` 隔离，只加 `audit_log.actor` 记操作人。

| 角色 | Web API | 飞书工具 | 说明 |
|---|---|---|---|
| **Viewer**（查看） | 只读 GET | 读类工具 | 默认角色 |
| **Analyst**（研究） | + 策略/回测/数据同步 | 读类工具 | 研究隔离防误操作 |
| **Trader**（交易） | + 策略启停/熔断/参数改 | 读类 + halt + 策略启停 | 交易操作 |
| **Admin**（管理） | 全部 + 账户/密钥/风控规则/系统配置 | 全部（操作类带确认，resume 仅 Admin） | 系统管理 |

**安全约定**：
- 用户密码 bcrypt（hashpw/checkpw，旧 sha256 自动重置）。
- API 密钥系统级 AES 加密存储（`crypto_utils.encrypt/decrypt`），仅 Admin 管理。
- 用户开通方式：admin 邀请制（填 email 发邀请邮件 → 被邀请者自助开通，默认 Viewer）。非开放注册，非多租户。
- 熔断：Trader+Admin 可 `emergency_halt`；`resume` 仅 Admin。
- 审计：所有 mutation 必带 actor 写 `audit_log`。
- LLM 工具按角色过滤（`_filter_tools`：角色白名单 ∩ 传入 tools）。

## 9. 技术栈

| 类别 | 选型 |
|---|---|
| 语言 | Python 3.10（本地 venv）/ 3.11（服务器 venv）|
| 交易内核 | vnpy 4.x + vnpy_xtp（中泰 XTP，Linux 原生）+ vnpy 加密网关（外部 gate）|
| 回测 | **自建 BacktestEngine**（纯 Python，不依赖 vnpy CtaBacktestingEngine）|
| 后端 | FastAPI + uvicorn（单进程，全局状态安全）|
| 前端 | Vue3 + Element Plus + echarts + klinecharts |
| 数据库 | PostgreSQL 18 + pgvector + alembic schema 版本管理 |
| 缓存/队列 | Valkey（Redis 协议兼容）|
| 调度 | Celery + Valkey（21 个定时任务）|
| 数据源 | Tushare Pro（主，积分 200）/ AkShare（补，免费）/ XTP 实时 tick |
| AI 模型 | 火山方舟 ark-code-latest（当前主）+ DeepSeek（fallback）+ GLM（备），自建 LLM 网关 |
| 部署 | venv + systemd（无 Docker），Alibaba Cloud Linux 3 |

## 10. 文档导航

- [01 LLM 网关](01-llm-gateway.md)
- [02 策略/模型统一框架](02-strategy-framework.md)
- [03 A股分析引擎](03-astock-analysis.md)
- [04 可转债/ETF 引擎](04-convertible-etf-engine.md)
- [05 加密永续合约引擎](05-crypto-perp-engine.md)
- [06 数据中台](06-data-platform.md)
- [07 风控中心](07-risk-control.md)
- [08 Web 管理后台](08-web-admin.md)
- [09 调度层](09-scheduler.md)
- [10 告警/通知](10-alert-notify.md)
- [11 飞书/Lark 对接层](11-feishu-lark.md)
- [接口契约字典](接口契约.md)（跨模块签名 + 数据结构，任务自包含基础）
- [模块契约/](模块契约/)（13 份逐模块 public API + 依赖 + 被调 + 读写表）
- [批量实施设计](../批量实施设计.md)（A-E 模块拆分）
- [竞品需求对照](../竞品需求对照分析.md)（60 项逐条分析）

**自包含任务体系**：接口契约字典 + 13 份模块契约 + 任务模板（8 字段：目标/依赖/产出/限定范围/接口契约/验收/mock/参考）三层配套，做任务时只读「任务文件 + 接口契约 + 本模块契约」即可动手，零代码阅读。硬约束：限定范围 ≤3 文件 + 参考 ≤2 份。

---

## 修订记录
- 2026-08-16 更新：模块清单加邮件发件箱(EM)/通知中心(NC)/用户管理细化；数据流加邮件流/通知流；关键约束加 N语言/错误码化/SMTP DB化/账户保护；端点概览补全
- 2026-08-11 全面重写 + 参考文档合并（P0-P4 遗漏清单 54 项完成后同步；合并 14 份参考文档已采纳内容：双模式统一执行/自包含任务体系/飞书 per-机器人角色/邀请制+bcrypt/alembic 版本管理/类型级异步化/部署隔离铁律/熔断并发 Lock/cron+交易日历/回测组 tasks 关联）
- 2026-08-10 部分更新（Amazon2023→Alibaba Linux 3 / A股只读→可实盘 / tier 移除 / AStockReadonlyAdapter 标注废止）
- 2026-07-22 初版（12 份架构文档定稿）
