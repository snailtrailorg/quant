# D26 · 市场接入架构（MarketSpec 声明 + 扁平 provider 插件）

> 状态：**v5 终审定稿**（2026-09-25：三轮双盲+验证审+终审——金融 PASS(带 P2 注记)/架构余 1 P1（a′ 续租参数数学，已修）+版本标签，本轮一并清零）
> 定位：接入面演进锚点；实施触发点=下一次接入面动工；**实施批方案细化须再过双盲审**（§五清单为本稿核心交付）。
> 核心一句话：**一个账号 = 一行接口配置 = 一个 hub + 一套 TD 会话；hub 绑死账号行，MD/TD 同凭证。**

## 零、设计取舍：为什么是账号级 hub 而非共享单例 hub

**裁定（2026-09-25）：全市场唯一拓扑 = 账号级实例化**——A 股与加密同形态（XTP 双账号=双行=双 hub）。曾考虑的替代方案=A 股共享单例 hub（全市场一份聚合，通道切换池）+M5 单活双实例互备，被否。五条理由：

1. **分支税**：A 股/加密分支 10+ 处；**最强活证据=v3 盲审挖出的现存加密潜伏 bug**——worker `_hub_alive`（hub_worker.py:116）读裸键 `quant:hb:md-hub`，加密 hub 写 per-account 键——同机 A 股 hub 恰写裸键 → 加密 worker 读到的是 **A 股 hub 的心跳=恒 True（fail-open 掩蔽加密 hub 死亡，比 fail-closed 更危险）**（加密实盘未开闸所以未爆）。迁移批顺带修复，并列为加密实盘开闸前必修项。
2. **故障域反转**（措辞修正自 v3）：故障域=账号域——实例局部故障（进程崩溃/OOM）在重拉窗内仅该账号受影响；共模故障（代码 bug/Valkey 挂）两种拓扑等同；单点的时间界由 systemd 重拉压至 1-2 分钟。承载裁定的是⑤，本条不单独承载。
3. **一致性反对弱化**：微差 bar 在 A03 §8.4 双时态语义下非新罪（流生成版本允许微差）；但**对账前提=锚落地**（见 §3.4 依赖注记）。
4. **M5 退役红利**：互备体系（switch.py 265 行实测/active_instance/intent/guarded Lua/AB 双实例）全部退役（退役边界精确定义见 §4.1）。
5. **底座同质化（根本）**：「聚合 hub」特殊形态组件消失——hub 从共享基础设施退化为账号附属进程，单一形态=注册即行 CRUD、代码路径唯一、故障模式单一。

**哲学（用户立法）**：逻辑越简洁代码质量越高，真实故障点越少；资源不足加配置即可。

## 一、背景与演进

D25 后「市场」一级是散落分支。v1 两级插件树 → v2 双盲审收敛 MarketSpec+扁平 provider → v3 用户终裁统一拓扑 → v4 终裁后双盲审全修 → 五处自定方案扩展论证 → 验证审 → **v5 修复+终审**（a′ 参数数学闭合）。

## 二、术语规范（用户原词 → 本方案术语 → 业界通用）

| 用户原词 | 本方案术语 | 业界通用 |
|---|---|---|
| 模块/插件 | 适配器/插件 | Connector/Gateway |
| 国内市场模块 | MarketSpec（市场声明） | Market（市场域） |
| 券商（下一级） | 供应商插件 | Brokerage/Data Connector（数据源=交易所及其信息公司） |
| hub | hub（行情馈送中枢） | Feed Handler |
| 实盘/交易能力 | MD 能力/TD 能力 | Market Data/Order Execution |
| 节奏域 | 交易时段 | Market Calendar（Spec=骨架，market_hours 表=运营真源） |
| 品类管道 | 数据通道 | Feed Channel（bar/depth_snapshot/trade_tick/order_tick） |
| 每账号一个hub | 账号级实例化（唯一拓扑） | Per-account instantiation |

## 三、架构模型

### 3.1 市场域判据（沿用）

市场域=行情事实权威按标的唯一的品种域；域内撮合差异由 A05 三维+A03 scope 承载。

### 3.2 结构：一份声明 + 一层插件 + 一种拓扑

```
MarketSpec（quant_common/markets.py 扩展——纯数据）
│  trading_day_anchor: 自然日 | UTC0点 | 夜盘开盘沿    ← 法定字段（期货夜盘 C4 防雷）
│  sessions_skeleton: 日盘/夜盘/24×7（骨架；market_hours 表=运营真源）
│  channels: [bar(聚合), depth_snapshot/trade_tick/order_tick(透传，L2 启用)]
│            + per-channel 旋钮：stream_maxlen / 订阅上限（§3.4 内存与配额上界）
│  flush_policy: 三窗 finalize | stale 周期
│
└─ provider 插件（扁平；角色可选 MD/TD/pull——现状无一家双角色齐备，隐含必备=空实现陷阱）
```

### 3.3 账号=hub 宿主（核心立法 + 盲审补强）

> 一行 trading 域 external_interface = 一个账号 = 一个 hub + 一套 TD 构建器；hub 用该行凭证开 MD，worker 用同行开 TD。

**hub 进程模型统一（外部表现一致性立法，2026-09-25 用户确认）**：
- **常驻**：hub 随账号行常驻（SA4 期望态=行存在⇔hub 在场），无「交易时段启停」——A 股收盘后 hub 仍在（窗外挂起态：logout 保持 CREATED、不产 bar）；加密照常 24/7。
- **连接生命周期=hub 内部状态机，禁用进程重启实现时段管理**：A 股连接窗（9:05 建连/15:10 挂起——网关 `poll_supervise` renew/suspend 原语，XtpMdGateway 现状已如此）与加密 24/7+断线重连，都是网关内部状态机；进程重启会丢在途聚合桶+断流+gen 跳变，用它做时段管理=纯损失。
- **进程重启的合法场景仅三类**：异常（崩溃/Watchdog 挂死/守卫善后 exit 3——前次异常退出的键残留自愈）/接口行变更（exit 9——凭证/provider 是建连参数，变更须重建连接）/部署。
- 由此外部表现全市场同构：常驻 + 同退出码矩阵 + 同心跳 + 同键模型——「底座同质化」的进程级表述。

- 流键统一 `hub:bars:{account_id}:{symbol}`；同源校验全市场统一（无跳过分支）。
- **订阅真相源保持全局**（`_desired_symbols`=running∪白名单∪临时订阅，无 account 维）——各 hub 全量复制订阅。代价=A股 L1 订阅额度×N（per-hub 连接额度反宽裕，已接受）+ **L2 授权费 per 账号×N + 加密 per-IP 限速共享预算（同机 N hub 共享币安 weight——「加配置」对 IP 限速不成立，须上界意识）**。
- `hub:latest_tick` 契约（盲审 P2）：写者=各账号 hub 各写 `hub:latest_tick:{account_id}:{symbol}`（TTL 65s）；读者（stock_detail）=**SCAN `hub:latest_tick:*:{symbol}` 取在场者中 payload ts 最大者**（TTL 封绝对在场（≤65s 写过）、ts 封相对陈旧——多账号任取可显示最长 65s 陈旧价，故必须比 ts；键量百级无性能问题）——禁「读任意裸键」（会变相复活选行逻辑）。**fallback 注记**：若实施中 SCAN 游标语义遇坑，备选=聚合键 `hub:latest_tick:agg:{symbol}`（各 hub 双写、读者取 payload ts 新者——「墙钟最后赢」在 hub 时钟差下可能取旧值，须写侧比 ts 或明示接受 65s 上界；物化视图性质丢失无损）。
- **md_row 跨通道解耦不做**，且注记：非纯加法——流键/同源校验/水位键身份全锚 account_id，后补 md_row 须重定义流身份=MD 源行 id（重新分叉），故若未来要做，**现在就把流身份措辞定义为 MD 源行 id** 可买断分叉（实施批措辞按此）。
- **XTP client_id 上限守卫**（盲审 P2-3）：同账号 worker 派生 `(tid−1)%98+2`——tid 相差 98 撞号=「任务在跑但 TD 永远连不上」；建任务时校验同账号 client_id 占用。
- **多账号一致性用户警示**（盲审 P2-4）：双账户对照实验/逐步迁移仓位场景——行为**不复制**（偶发信号分叉+成交侧排队/部分成交差异远大于行情微差）；对照口径建议意图级非逐 bar 级；迁移场景预期持仓漂移以 reconcile 对账。

### 3.4 核心不变式（+落地依赖与缺根补强）

| 不变式 | 全市场统一态 |
|---|---|
| 同源数据不复制 | **best-effort**：多账号 hub=多份公共流各自聚合；**对账锚=PG 盘后回补** |
| hub 不碰交易 | TD 不流经 hub |

**对账锚落地依赖（金融 P1-1，诚实注记）**：A 股分钟侧 hub 落库已退役（2026-09-23）、stk_mins 未购买——**bar_1min 现为空，锚暂不存在**；加密盘后回补仅是 A03 §8.4 蓝图。锚落地前 best-effort=无对账的知情接受（A股锚=stk_mins 采购 gate；加密锚=回补批）；diff 告警（A03 per-field 容差）列为实施批配套。

**缺根≠微差（金融 P1-2 + v5 验证审三补，实施批配套）**：hub 重启窗内分钟不产 bar；重启后首桶 volume 只含剩余增量=**静默半根**——回补只修历史不救当日决策。四件配套：
① worker **ts 域缺口检测**：`ts_key − max_ts > 60s` **仅在 sessions 连续段内判定**（段首沿豁免）——裸比较会在 A 股午休（~90min）/隔夜/节假日每日误触发（水位键持久化跨重启）=告警疲劳+rewarm 风暴；
② **检出行为=sticky 冻结**（复用 frozen.stream 语义，重启解冻）——ts 缺口=源侧丢根，rewarm 补不了（A股 bar_1min 空），比 seq gap 更严重，不得按 gen_jump 档（仅 rewarm）继续交易带洞因子窗；**解冻语义注记**：重启后 rewarm 推进水位过缺口即不再触发=解冻即操作者显式接受带洞窗（强制人工介入的控制目标已达成）；
③ **静默半根配套**：聚合器桶 `span<60s` → 标 untrusted=1（一行），worker 既有 untrusted 冻结链自动接管；
④ 回补归因：order_log 加 bar 指纹列（ts+OHLCV+gen 随下单落）。

**Valkey 内存上界公式**（架构 P2-5）：STREAM_MAXLEN=5000/标的/账号 × ~250B——100 标的×2 账号 ≈ 250MB，线性、立法内；MarketSpec channels 留 per-market maxlen 旋钮。

### 3.5 真源分层（v2 立法沿用）

FIELD_SCHEMA 真源留 `interfaces/base.py`（引用不复制）；MarketSpec 纯数据落 quant_common（收编三处重复归属）；TD builder 下沉可引用层。

## 四、M5 退役与生命周期（v4 精确定义）

### 4.1 退役边界与守卫（方案 a′——v5 参数数学闭合终审修）

```
退役：intent / active_instance 仲裁 / guarded Lua / AB 双实例部署
保留（方案 a′——续租原语不拆，只拆仲裁）：
     boot 守卫 = SET NX EX 30（新进程启动抢自己的键）
     失败 → exit 3 + RestartSec=30 自愈（最坏 ≤2×RestartSec≈60s——真实竞态是旧 lease TTL
     比旧进程活得久，非双活进程，systemd 同名串行保证后者）
     续租 = 5s EXPIRE 30（对齐现状 _lease_renew——main.py:416 Lua CAS 续 30、周期 5s=TTL/6，
     终审修：v5 曾写 60s 续租>30s TTL=数学不闭合，键间隙过期→EXPIRE no-op→围栏 30s 后全开）
     带外第二进程（非 systemd 途径误启）NX 恒失败=同账号双活被防——长活 fencing 成立
     优雅退出 DEL 键；键生命周期=终身持有+5s 续（拆的只是仲裁复杂度，续租原语原样保留）
gen：守卫通过后 INCR（守卫失败不自增——防空代次让 worker 无谓重暖机）；
     worker 跳变检测语义原样完整
WatchdogSec/sd_notify：EngineLoop 级进程挂死检测，与 M5 无关，保留
SA4 lease-held 前置检查（_sa4_hub_guards）在 a′ 下趋于恒空——无害语义漂移，注记随实施清理
```

**退出码矩阵（v5 终版）**：留 `0`（优雅退出）/`1`（故障重拉）/`9`（接口行 provider 变更自重启，批 64——与 M5 无关）/`78`（EX_CONFIG）/`3`（boot 守卫失败自愈，不进 Prevent——2026-09-22 staging 实证防部署波永久死）/`4`（**Valkey 不可达重试耗尽**，parts.py:236——勿误读为守卫重试码，进 Prevent 可再议）；`6`/`5` 随 M5 退役（唯一生产者分别是 intent/active_instance 仲裁与续租丢路径，均已死）。**Prevent 终态=`0 78`**（现 `0 6 78` 随 6 退役改写）。unit 注释与 `run_hub_postverify.py` 旧注释同步。

### 4.2 生命周期（v4 拆分换行两义——金融 P1-3）

| 事件 | 动作 | 量级 |
|---|---|---|
| 账号接入/增行 | SA4 按行拉起 hub（统一） | 轻 |
| 账号停用/删行 | 先维护键后停实例 | 轻 |
| **凭证/参数变更（同账号）** | hub 自重启重读行——**待实施**：`_poll_iface_switch` 现只比 provider，凭证轮换不触发 exit 9（现状缺口）；扩比对 credentials 摘要或 updated_at | 轻（~30s 窗，TD 在 worker 手连续性 OK） |
| **跨账号迁移** | **重量级金融操作**——硬前置引用 A03 §10.3 检查单（在途清零/持仓资金快照/五键重估；旧账号持仓不随行、基线 per-account） | 重 |
| **通道故障** | **任务冻结待恢复**（BUY 拒/SELL 放行）——**执行主体=自动**（buy_ok 心跳门+bar 新鲜门，hub_worker.py:200-201 现状机制：bar 停流自动拒 BUY、SELL 放行；恢复自动解；sticky 冻结需重启解冻）——**换行是计划性迁移，不是故障响应**（XTP 故障换 EMQ=换券商=天级） | SOP |
| 进程崩溃 | systemd 重拉+gen 跳变重暖机 | 自动 |
| 换行情通道 | = 换行（§上两行） | —— |

**冷切换窗丢根**：**实施批先建白名单文件条目**（现象/容差/验证义务三件套；批 62 未实施时条目随文件存在、引擎随批 62 生效——**冷切换丢根验收不依赖批 62**）——v3 所称「M7 白名单已预登记」为悬空引用（金融 P1-4 修；批 62 现有白名单是口径归一白名单，另一种东西）。

### 4.3 观测面 N 化与配置界面（v4 独立小节——架构 P1-4/P1-5）

**观测面（从 1→N，五处清单；「crypto 现状推广」不成立——加密 per-account 心跳今天不在任何观测面，是从零建）**：
1. collector.py hub 快照读：单 hash → N 实例结构
2. monitor.py R4 hub_hb_lost / R6 tick_stalled：单 streak 状态机 → per-account streak+告警 component 去重键；R4 的缺席发现=**unit 清单 × hb 键交叉**（仅 SCAN 会失去「unit active 但 hb 键消失」（Valkey 可达而写路径坏）的唯一探测器）
3. /readyz `hub_hb` 检查：语义裁定=**在场集健康 + 空集地板**（实际在场的 hub 心跳全部新鲜**且在场集非空**才 ready——空集=vacuous 真空洞，D26 迁移发布（unit 改名）恰是「全没起来」最可能场景，空集必须 not ready；期望集比对归 SA4 周期对账——职责分离，防「一个凭证未配好的坏行 Failed 拖死发布判定」）。在场集语义**不得外溢到 quant-hbcheck**（见 #4）。
4. deploy `deploy_hub_hb_redis_key` + quant-hbcheck postverify：**钉死=DB 驱动期望集**（逐 trading 域 enabled 行验 hb 键 TTL+字段+**hash 内 ts 判龄**——非仅 TTL>0，防旧心跳 90s 残留假过；**非空断言**——豁免口径：期望集为空的合法态（全新 bootstrap 零行/维护期全 trading 行禁用）下探针跳过 hub 检查并显式输出「期望集空」而非静默绿）——发布探针是同步卡点，**必须扛期望集**（SCAN 在场发现天然测不到缺席：波次重启后 hub 起不来→无键→SCAN 看不见→发布绿着带死 hub 完成；且迁移批 unit 换名使 systemd 采集旧名单失效——DB 行是期望集唯一正确来源）。SCAN 至多做附加校验，不做门。
5. 前端 Health 页 hub 单对象渲染 → N 实例
观察日三查口径：hub 零重启/心跳/241 根完整性 → 「hub 集合」判定。

**position/拖拽收束（消二义——架构 P1-5）**：
- position 列**保留**（routing 软排序消费+create 全局 max+1 依赖）；reorder 端点**保留**（全局全量序列，语义不动）
- 前端仅 trading 域行**撤拖拽手柄**（通道切换语义消失）；pull 型数据源行保留拖拽
- `get_interface_row()` **删缺省选行分支**，改 row_id 必填 raise（无主路径会静默绑首行账号；与「hub 绑死账号行」fail-fast 哲学一致）

## 五、迁移面全清单（v4 核心交付——流键契约变更的全部消费点，架构 P1-3）

| # | 消费点 | 位置 | 动作 |
|---|---|---|---|
| 1 | worker 流键/同源校验 | strategy_runner/hub_worker.py `bar_stream_key`/`_account_mismatch` | A 股键化（统一带 account_id） |
| 2 | **worker `_hub_alive`** | strategy_runner/hub_worker.py:116 读裸键 | 改 per-account 键——**顺带修现存加密潜伏 bug**（§零理由①），列为迁移验收项+加密开闸必修 |
| 3 | worker max_ts 水位键 | strategy_runner/hub_worker.py:172 | A 股键化（部署窗外切换=旧水位失联无害，写明） |
| 4 | databus.subscribe | databus.py:194 裸键 | 键化（A03 M5 第二个流消费者） |
| 5 | /readyz hub_hb | web_api/routes/system.py:114 | §4.3 语义裁定落地（不改=readyz 直接红） |
| 6 | `get_interface_row` 缺省选行分支删除 | broker.py:207 缺省分支 + md_hub/main.py:242-246 A股实例走 None | **删缺省分支改 row_id 必填 raise + 退役旧 --id 启动路径**（main.py:449-461）；DB 必填已由迁移 0100/0104 完成，无需回填 |
| 7 | 旧 unit 退役 | SA4 `_desired_units` **无条件 append builtin @quant**（scheduler/tasks.py:1348「常开」）+`vid.isdigit()` 跳过非数字名 | **显式停会被 SA4 ≤300s 内主动拉回（非放任）**——须与 #13 期望源改造同批：停旧 unit+**删 builtin 期望条目**+disable/mask |
| 8 | 旧 Valkey 键清理 | `hub:bars:{symbol}`/水位键无 TTL | 显式清理（永久残留） |
| 9 | deploy hub 波次 | release.yml fallback+inventory 默认值 | 改 DB 驱动全实例 |
| 10 | 观测面五处 | §4.3 | N 化 |
| 11 | `_desired_symbols` 订阅过滤 | md_hub/main.py:269 全局真相 | **保持全局**（§3.3 已接受代价）——迁移项=验证各 hub 订阅就位 |
| 12 | 交易日锚/C4/时段骨架/flush/收编三处归属 | v2 立法 | 实施批展开 |
| 13 | **SA4 期望源改造** | `_desired_units` builtin @quant 常开条目 + 期望表 `market='crypto'` 过滤 | **builtin 条目退役；external_interface trading 域 enabled 行（全市场，去 crypto 过滤）生成 per-account 期望**——不改造则 SA4 每 300s 与迁移对抗（旧 @quant 复活+新 hub 并存=同账号双写流）；与 #7/#9 同批 |

**批面口径（架构 P2-4 修）：全市场一批**——v3「加密不用动」只对 hub 侧成立；消费面统一（worker/观测面）本身动加密路径，一批切完。**批内顺序约束**：#6（删缺省分支+退役旧路径）与波次（阶段 7）前完成；#5/#7/#9/#13 与 hub 键切换同波次；#5/#7/#9/#13 与 hub 键切换同波次——管道阶段隐式保证，拆分实施须遵守。**staging 覆盖面**：staging 无加密账号行（外部 gate），crypto 路径（#2 修复/观测面 N 化）彩排不可达——crypto 侧验证义务=单测+加密开闸前必修清单承担（#2 已列必修项）。

**迁移时机**：现网 1 A 股账号+1 任务，成本最低；staging 彩排+部署窗三段+整版本回滚兜底。

## 六、扩展场景验证

| 场景 | 动作 | 波及面 |
|---|---|---|
| XTP 双账号 | 两行→两 hub | 零代码；用户警示见 §3.3（行为不复制） |
| 加密加账号 | 行 CRUD | 一 hub |
| 启用 L2 | channels 启用+per-provider 矩阵 | **L2 授权费 ×N 账号**（§3.3 边界） |
| 接期货 | MarketSpec+CTP provider | 新账号即新 hub |
| 换通道 | §4.2 两行（轻/重分开） | 见表 |

## 七、范围外

A03 完整五层总线；双源并行对账（归 M7——L1/L2 仅弱对账：量强对、价白名单）；D25 能力集（正交）；期货通道远期清单届时补。

## 八、版本与盲审处置记录

**v2 双盲审**（金融 P1×5+P2×6/架构 P0×1+P1×4+P2×4）：判据正名/交易日锚/L2 通道精确化/真源分层/MarketSpec 纯数据/MarketHours 主从——全吸收（git 史 v2 §八）。
**v3 用户终裁**：拓扑统一+hub 绑行+M5 退役+position 退役方向+底座同质化。
**终审（金融 PASS 带 P2×4/架构非 PASS 余 1 P1）→ v5 定稿**：两审确认上轮修复 100% 忠实+代码锚点抽查全准+金融风险面闭环；架构师抓出 **a′ 续租参数数学不闭合**（60s 续>TTL 30=键间隙过期 fencing 失效——v5 新引入，种子意见本身带错照单未验；终审修=5s 续对齐现状 main.py:416）+版本标签残留（v4→v5/§4.1 标题 a→a′/§一演进句）；金融 P2 顺手清（sticky 解冻=显式接受注记/非空断言 bootstrap 豁免口径）。
**v4+论证修正 验证双盲（金融 P1×4+P2×7/架构 P1×2+P2×5）→ v5**：上轮修复全部核实落地（代码落点抽查 100% 准）、五处自定方案 3 处成立 2 处带缺口。修复：①SA4 期望源改造入清单 #13+#7 反拉事实更正（架构 P1-1/金融 P2-10）；②quant-hbcheck 钉死 DB 驱动期望集+非空断言+ts 判龄，SCAN 不做门（架构 P1-2/金融 P1-4）；③readyz 空集地板（金融 P1-3）；④ts 缺口检测时段感知+段首豁免+检出=sticky 冻结+半根桶 span<60s 标 untrusted（金融 P1-1/P1-2/P2-5）；⑤守卫方案 a→a′（保留 60s 低频续租+优雅退出 DEL——拆 renew 丢长活 fencing；Prevent 终态 0 78；exit 4 生产者注记）；⑥加密 bug 症状更正（恒 True fail-open 掩蔽死亡，非恒 False）；⑦latest_tick 取在场者中 ts 最大+fallback ts 护卫；⑧冻结 SOP 执行主体=自动门；⑨批 62 预登记时序（白名单文件先行，验收不依赖批 62）；⑩R4 交叉发现+SA4 lease-held 恒空注记+批内顺序约束+staging 覆盖面归属。
**v4 双盲审**（金融 P1×4+P2×5/架构 P1×5+P2×6）全修：
- 金融：P1-1 对账锚落地依赖注记（§3.4）；P1-2 缺根检测+bar 指纹（§3.4）；P1-3 换行两义拆分+故障冻结 SOP（§4.2）；P1-4 白名单改实施批预登记（§4.2）。P2：md_row 非纯加法/流身份措辞（§3.3）、订阅与费用边界（§3.3）、client_id 守卫（§3.3）、用户警示（§3.3/§六）、bar 指纹（§3.4）。
- 架构：P1-1 守卫方案 a+gen INCR 时机+退出码矩阵（§4.1）；P1-2 凭证变更标待实施（§4.2）；P1-3 迁移面 12 项全清单（§五）；P1-4 观测面五处独立小节（§4.3）；P1-5 position 收束+缺省选行删（§4.3）。P2：latest_tick SCAN 契约（§3.3）、订阅×N 显式接受（§3.3）、理由②措辞修正（§零）、全市场一批口径（§五）、内存上界公式（§3.4）、文档小疵。
