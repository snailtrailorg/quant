# A2 数据质量校验（#30）

> **自包含任务**：仅读本文 + 「接口契约 §Bar/§三② + 模块契约 data_platform §save_bars」即可动手，**不读 db.py 代码**。
> 模块：A 数据 / 子任务 A2 / 来源：竞品 F-DATA-020 / 依赖：无（db.save_bars 已就绪）

---

## 目标
`save_bars` 入库前校验：剔 `open/high/low/close=0` 的行 + 标 ts 断点 warning（不剔断点）。

## 依赖（就绪）
- `db.save_bars` ✅（db.py:55，模块契约 data_platform §一）
- rows 11 字段结构 ✅（接口契约 §Bar + 模块契约 data_platform §六 不变量）

## 产出
- `db.py` 加 `validate_bars(rows)` 函数
- `db.py` `save_bars` 开头加 `rows = validate_bars(rows)`
- `tests/test_validate_bars.py`（3 例）

## 限定范围
- 只改 `server/src/data_platform/db.py`（加 `validate_bars` 函数 + `save_bars` 一行调用）
- 新建 `server/tests/test_validate_bars.py`
- **不碰** `schema.py` / `engine.py` / `main.py` / 其他文件

## 接口契约
- **新加**：`validate_bars(rows: list[tuple]) -> list[tuple]`
  - 剔除 `open/high/low/close=0` 的行（坏数据）
  - ts 断点（日线相邻 >7 天）记 `logging.warning`，**不剔**
  - 返回清洗后 rows
- **现有调用**：`save_bars(freq: str, rows: list[tuple]) -> int`（db.py:55，开头加 `rows = validate_bars(rows)`）
- **rows 结构**（11 字段元组）：`(symbol, freq, ts, open, high, low, close, volume, amount, adj_factor, source)`（接口契约 §Bar）

## 验收标准
- `pytest tests/test_validate_bars.py` 过（3 例：剔0 / 断点标记不剔 / 正常全保留）
- `python -c "from src.data_platform.db import save_bars, validate_bars; print('OK')"`
- 全套不回归：`pytest tests/ -q`（63+3=66 过）

## mock 方式
纯函数测 `validate_bars`，不连 DB：
```python
from datetime import datetime, timedelta
ts1 = datetime(2026,8,1)
ts2 = datetime(2026,8,9)  # 距 ts1 8 天（>7 断点）

# 1. 剔 ohlc=0
rows_zero = [("600000.SHSE","1D",ts1,0,0,0,0,100,1000,None,"tushare")]
assert validate_bars(rows_zero) == []

# 2. 正常保留
rows_ok = [("600000.SHSE","1D",ts1,10,10,10,10,100,1000,None,"tushare")]
assert validate_bars(rows_ok) == rows_ok

# 3. 断点不剔（记 warning）
rows_gap = [rows_ok[0], ("600000.SHSE","1D",ts2,11,11,11,11,200,2000,None,"tushare")]
assert len(validate_bars(rows_gap)) == 2  # 不剔，只 warning
```

## 参考文档（≤ 2 份）
1. 接口契约 §Bar（rows 结构）+ §三② validate_bars（签名）-- `docs/architecture/接口契约.md`
2. 模块契约 data_platform §一 save_bars + §六 不变量 -- `docs/architecture/模块契约/data_platform.md`

---

## 验证
做任务时零代码阅读（仅读本文 + 2 份契约文档）。这是自包含任务体系的第一个样本。
