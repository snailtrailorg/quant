# 18 · 审核记录：批三尾量四步法复盘（口径/判类/编辑/实测）

> v1.0 · 2026-09-01 · 对象：2587a0c/984a52b/0c93d9e，prod 202609012310-984a52b。方法：三口径独立复数 + 源码实据 + 方法论评审。

## 1. 核验结论（全部实锤）

| 声称 | 复验 | 判定 |
|---|---|---|
| 终态三口径 225/40/62 | 我以同口径（views+components，`grep -cE` 修正转义）独立复数：**225/40/62 完全吻合** | ✅ |
| 基线校准 232 vs 17 号 227 差 5 = components/RunConfig.vue | 属实——**17 号口径定义缺陷**（未限定目录），实施方抓得对，本号 §3 修订 | ✅（我方认领） |
| DataManage 操作列 290→400 | DataManage.vue:53 `width="400"`，无 290 残留 | ✅ |
| 行高改前改后 [49,81,49…]→[49,49…] | 探针逻辑读源码复现（.cell 内层测量），实施方浏览器断言自测输出可信 | ✅（待用户 Windows 真机体感终验背书） |
| repro-1280.mjs 入库 + 凭证环境变量化 | 0c93d9e；明文密码泄漏面顺手修复 | ✅ |

## 2. 方法论评审：四环皆立，两处反哺我方规范

**一·口径对齐 ✅**——"先统一数数的方法再谈数字"+"基线校准→追差→语义一致才可比"是正确工程方法；grep 子串坑（width= 吞 min-width）披露诚实。

**二·判类 ✅ 且升级我方规范**——14 号 §2.8 原以**列名**判类（"名称/描述/详情类"），实施方提出**值形态判据**（人读自然语言→弹性化；等宽可预期→保留固定），并以 Permissions.vue id 列（label="名称"但值是 nav 代码→保留）证明列名判据会误改。**判据已回写 14 号 §2.8**。无宽列只补 tooltip 不加 min-width（避免无意义 churn）、220 留存列逐条过判类（"不是改不动，是不该改"）——克制正确。

**三·编辑机制 ✅**——锚点唯一性断言（不唯一即炸，绝不静默错改）是标准做法；min-width=原 width 的语义（下限不缩水、只宽不窄）正确理解规范；提交卫生（跨批先回退再分批）延续。

**四·实测闭环 ✅ 全篇最值钱**——
1. 1280×800 视口代理原理正确（布局由 CSS 像素决定，与 DPI 无关）+ 诚实局限声明（字体度量仅近似）
2. **td 高度探针假阳性→.cell 探针**：EP 等高行内 td 恒等于行高无区分度——假阳性探针比没有探针更危险；改量内层 .cell 后精准锁定唯一元凶。这是真 DOM 测量专业知识
3. **真 Bug 判类正确**：四按钮 380px 塞 290px 折行，没有"顺手弹性化"（操作列固定宽是规范），按 DESIGN.md §5 按钮数预留加宽 400——判类纪律在压力下守住了

**口诀收录**："数字口径先对齐、判类标准成文、编辑带断言、验收用量测而不是观感——四环里任何一环靠感觉，就会回到形式合规实质失效。"

## 3. 回写与修订（本号执行）

1. **14 号 §2.8 判类标准升级**：列名判据→值形态判据（含三边界案例：label 与值不符看值 / 配置键值与枚举保留 / 无宽列只补 tooltip）
2. **17 号计数口径修订**：范围 `src/views/*.vue` **+ src/components/*.vue**（17 号 v1.0 漏目录导致 227≠232 基线差）
3. repro-1280.mjs 的 KPI 同排断言 + .cell 行高探针列为**标准验收门**（收官日清单 #4 素材已就位）

## 4. 一个治理尾巴（下轮一把处理）

`17-复验记录-五项清尾.md` untracked + `INDEX.md` modified 未提交——reference 快照链断裂（01-16 在 git，17 号游离）。**建议随下个 commit 显式提交**，保持 `docs/reference/web-design/` 快照完整性；实施方本轮特意隔离未卷入（隔离动作本身对），但快照入账是欠的。

## 5. 状态

**14 号跨平台显示修复方案至此全部完结**（P0/P1/P2 + 批三尾量 + 实测闭环）。剩余仅：用户 Windows@150% 真机体感终验、reference 快照提交、收官日清单常规项。
