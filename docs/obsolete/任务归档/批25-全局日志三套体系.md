# 批25 · 全局日志两套体系（Apache 式，v3）

> 2026-09-15 立项；v2 双盲审（A 1P0+3P1+5P2 / B 3P0+5P1+2P2）全吸收；**v3 用户再简化：notify_log 撤**——邮件/IM/SMS 发送本质是系统运行事件，
> 全写 system_log（成功=INFO/失败=ERROR/待重发=WARN，module=email|im|sms，运行日志筛选即得——迭代十六分组筛选直接复用）。**两套日志定稿**：
> ①系统运行日志（全模块+三通道发送事件）②用户操作审计日志。
> 用户裁定：落库=logger 统一 handler 异步批量；保留=运行 30 天/审计永久；存量不迁；**运行日志=管理员面**（GET /api/log 权限 read→user_mgmt 收紧——现状 read 全员与裁定不符）；收件人明文（管理员可见无碍）。
> **UI 简化收益**：运行/审计页签删除功能退役；发送日志无需独立表/页签/块。
> **关机冲刷（"完美"条件）**：分进程钩子把缓冲批量刷库。

## 一、目标

| 套 | 表 | 职责 | 写入 |
|---|---|---|---|
| ①运行日志 | `system_log`（新） | 全模块运行事件 + 三通道发送事件（module=email/im/sms） | logger 统一 handler+显式事件点 |
| ②操作审计 | `audit_log`（现有不动） | 用户操作留痕 | 现有 audit_log() |

- `email_outbox` **完全不动**（纯队列，v3 撤加列）；`task_logs` 保留（live-task 时间线+任务详情），运行日志页切 system_log。

## 二、后端设计

### 2.1 表结构（alembic 0077：system_log 单新表）
```sql
CREATE TABLE system_log (
  id BIGSERIAL PRIMARY KEY,
  ts TIMESTAMPTZ NOT NULL DEFAULT now(),
  level VARCHAR(8) NOT NULL,          -- DEBUG/INFO/WARN/ERROR
  source VARCHAR(40) NOT NULL,        -- 进程/单元：web-api/worker/beat/live:1/hub
  module VARCHAR(60) NOT NULL DEFAULT '', -- email/scheduler/risk/...（自由）
  message TEXT NOT NULL
);
CREATE INDEX idx_system_log_ts ON system_log (ts DESC);
CREATE INDEX idx_system_log_level ON system_log (level);

```

### 2.2 SystemLogHandler（`src/data_platform/log_sink.py` 新）——按进程分型（盲审 A-P0/B-P0-2：celery prefork 三连击）
- `logging.Handler` 子类：内存队列（deque maxlen=2000）+ 后台线程 5s/100 条批量 `executemany`；flush 用 `threading.Lock`+drain-while（后台线程与关机钩子并发安全）。
- **装配按进程分型**：
  - **web-api**：import 期装 root handler；冲刷=FastAPI `lifespan shutdown`+`atexit`（**不裸抢 SIGTERM**——uvicorn 自装优雅退出，盲审 B-P0-2①）。
  - **celery worker/beat/risk**：`worker_process_init` 信号内**每子进程重启 flush 线程**（线程不跨 fork；celery `hijack_root_logger=True` 摘 root handler → 装配用 celery `setup_logging` 信号挂回，A-P0①）+`worker_shutdown` 子进程自冲刷（billiard `os._exit` 绕过 atexit，A-P0③；`--max-tasks-per-child=100` 高频再生下每子进程自洽）。
  - **live-task（`src/strategy_runner/main.py`）/md-hub（`src/md_hub/main.py`）**：入口自持 SIGTERM（链式调原 handler 后 sys.exit 让 atexit 跑）；source=`live:{task_id}`（--task-id 推导）。注意两入口 `logging.basicConfig` 在装配后变 no-op（root 已有 handler）需自验（A-P2-9）。
  - **im-pool（`src/im_bot/pool.py`）+feishu ws_client 子进程**（A-P1-3 补）：pool 装配+Popen spawn 的 ws_client 子进程环境变量继承 source，若不落库显式声明排除。
- **source 取值**：env `QUANT_LOG_SOURCE`（.env+systemd 单元 `Environment=` 双写；缺省按进程名兜底 `unknown`）——盲审 B-P1-8 钉死。
- 连接：批量写复用 `get_conn()` 每批短连接（全仓一致性优先，5s 频次可接受）。
- level ≥INFO 落库；access log 不落（uvicorn.access propagate=false 实证，A-P2-8 ✓ 零噪音）。
- DB 故障降级：批量写失败丢批次+stderr 告警。

### 2.3 写入面改造
- `main.py`：装配 handler（web-api source）。
- worker/beat/hub：各入口装配（source 对应单元名）。
- **邮件链事件写 system_log（module=email）**：入队/发送成功/失败/重试耗尽四事件（`alert_notify/dispatch.py` + outbox sweep）。
- **三通道发送事件写 system_log（v3 用户裁定）——级别映射：成功=INFO/失败与重试耗尽=ERROR/入队待重发=WARN**：
  - **email**：入队=dispatch enqueue 处（WARN，"已入队待发"）；成功/失败/重试耗尽=`email_service/_try_row_sync` 终态两 UPDATE 分支（INFO/ERROR；四路径必经——盲审 A-P1-2 单点结论沿用）。message=`→ {to} ｜ {subject} ｜ {状态/错误}（重试 n 次）`。
  - **im/sms**：`alert_tasks.py` 三任务终态返回处（im=bot 维度整行记，per-user 结局不展开——known-limit；sms 失败=ERROR）。
  - stuck `sending`（worker 死亡窗）无终态事件——outbox 行仍在可查（known-limit 沿批7 语义）。
- 现有 logger 调用零改动（handler 统一捕获）。

### 2.4 GC（beat 任务）
`cleanup_logs` 日频（beat_schedule 注册，app.py——notifications_cleanup 同构并轨）：system_log>30d 批删/audit_log 只插不删零 GC。批次形态（PG 无 DELETE LIMIT）：`DELETE FROM system_log WHERE id IN (SELECT id FROM system_log WHERE ts<now()-interval '30 days' LIMIT 5000)` 循环至 0 行、每批独立短事务（18 号规范 §批次）。

### 2.5 端点
- `GET /api/log`：**无参分支切 system_log**（200 条）+**权限 read→user_mgmt 收紧**（v3 用户裁定：运行日志=管理员面；收件人明文无碍）；**task_id 参数分支保留读 task_logs**（live-task 时间线唯一消费 LiveTask.vue:303——盲审 B-P0-1，不可整体切；该分支权限同收紧）。
  - **可感知取舍**：数据同步任务 step 过程日志从运行日志页消失（保留在任务详情）——编码时 log_task 双写 system_log 一行补齐。
- `GET /api/audit` 不动。
- **退役（盲审去幽灵：运行日志删除从未存在）**：`POST /api/audit/delete`（risk.py:254）+Audit.vue 删除钮+api.js deleteAudit+测试（test_batch23_delete_endpoints.py 拆 notifications 部分保留）。**通知收件箱删除（/api/notifications/delete）保留**（收件箱=用户自己的消息，语义不同——待用户确认）。
- 词条 deleteSelected/deleteAll 为 NotificationTable 共用**保留勿删**。

## 三、前端设计

- **三页签**：运行日志（system_log，天然含 email/im/sms 事件行——hotfix4 映射撤除换真数据）/审计日志/通知消息（收件箱现状，发送日志块 v3 撤）。
- 运行日志页/审计页：删除钮退役（筛选+导出保留；迭代十六成果直接复用）。
- 动作序维持：筛选→导出→（列设置）。

## 三.5 已裁定
- 发送日志摆位：v3 撤（发送事件进运行日志，无需独立块）。
- 收件箱删除保留（用户消息语义）。

## 四、限定范围

- 只改：alembic 0077/`log_sink.py` 新/`dispatch.py`+outbox sweep 写入点/`main.py`+worker 入口装配/GC beat/两个 GET 端点改+两个 DELETE 端点退役/前端三页签（Logs/Audit/NotificationTable/Observe）。
- 不碰：audit_log 表与写入链/task_logs 表与 live-task 时间线读/notifications 收件箱链（除删除保留确认）/IM pool。
- 存量：不迁移；email_outbox 老数据留在队列表（发送日志从启用日起累计）。

## 五、验收标准

1. alembic upgrade 两表建齐；pytest 全绿（含新：handler 批量/flush 三钩子/GC/notify_log 写入/端点）。
2. 手测：触发告警邮件→system_log 有 module=email 事件行（risk worker 子进程产生——worker_process_init 装配路径实证；INFO/ERROR/WARN 三态各验）；关机冲刷双轨：本地 `kill -TERM <pid>`、staging `systemctl stop quant-web-api@quant.service`（dev 无 systemd，B-P1-7）。
3. 前端：运行日志含邮件事件行（真数据非映射）；审计/运行日志无删除钮；普通用户访问 /api/log 得 403。
4. build 绿+令牌门绿；staging 彩排→prod。

## 六、mock 方式

handler 单测：patch executemany 计数；flush 钩子：monkeypatch atexit/signal 直调 flush 断言队列空。三通道事件：patch _try_row_sync 终态分支断言 system_log 行级别。

## 七、参考文档（≤2）

1. `docs/design/D18-数据库操作规范.md`（批量写/长事务防护）
2. `docs/obsolete/任务归档/批24-字号标准与全站收编.md` 迭代十六（问题来龙去脉）

## 八、工作量（盲审 B-P2-10 补）
五段：迁移 0077（单表）/log_sink+装配（分型×5 入口）/写入面（dispatch+email_service+alert_tasks 三通道事件）/GC（单路）/端点（切+权限收紧+audit 退役三件）/前端（撤映射+删删除钮）——v3 较 v2 减一表一端点一 GC 路，后端一日可完。
