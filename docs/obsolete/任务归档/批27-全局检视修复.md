# 批27 全局检视修复（2026-09-15 立项；v2=方案双盲审 A/B 全吸收+27-4 路线重裁）

> 背景：全局代码检视（双检视员 A 低级层+B 隐性层交叉，撤销 1 条虚报）→ 31 子项。
> 方案双盲审（A P0×1+P1×5+P2×12 / B P0×1+P1×5+P2×7）**全吸收进本 v2**。
> 用户三裁定：全部 30+1 条修；**27-4 = 官方同款 ws patch+卡 2.0**（原 ws card handler 被 SDK 源码三重实证否决：1.7.1~1.7.3 ws client `elif CARD: return` 丢弃卡片帧；官方 openclaw-lark 的 monkey-patch 实践翻案本路线）；群聊限只读。
> 撤销不修：A-P2-2 限流键共享桶（B 实证推翻：uvicorn proxy_headers 默认 True+nginx 同机 XFF）。

## G1 P1×7

### 27-1 邮件 sending 黑洞（v2：回收计次防无限循环）
- **位置**：`email_service/__init__.py:119-158` + `sweep()` :166-178
- **修法（v2 四件）**：①claim UPDATE 加 `next_attempt_at=now()+interval '10 min'`（认领超时锚）；②sweep 开头回收 `UPDATE email_outbox SET status='pending', attempts=attempts+1 WHERE status='sending' AND next_attempt_at<=now() RETURNING id`——**attempts+1**（盲审 A：防"已发送成功但写库前死"→每 10 分钟无限重发；attempts>=MAX 的行直接标 `failed` 走 _final_failure_notify 可见终态）；③回收 UPDATE 后**显式 conn.commit()**（盲审 B：sweep 现结构 SELECT-only 无 commit，with 退出=回滚）；④成功/失败回写 UPDATE（:140/:146/:151）WHERE 加 `AND status='sending'`（防回收-重领重叠双发）
- **验收**：新测——sending 过期行回收且 attempts+1；attempts 达上限回收即 failed；回写在非 sending 态不落

### 27-2 Redis 超时（v2：清单补全+验收正则改宽+halt_reason 并 try）
- **位置（v2 补全）**：`web_api/routes/backtest.py:17-20`、`web_api/redis_pool.py:12-15` 与 `:18`（feishu_redis_pool 第二池）、`risk_control/risk.py:67-70`、`scheduler/tasks.py:460,491,572,765,831,933,1400`、`data_platform/market_snapshot.py:30`、`stock_detail.py:31`、`alert_notify/dispatch.py:83`、`web_api/routes/system.py:85,92,223`、`trading.py:34`、`pool_minute.py:105`、`tencent_minute.py:165`、`feishu_bot/tasks.py:26`、`feishu_bot/router.py:79`、`quant_common/eventbus.py:131`、`sync_lock.py:49`、`health_monitor/collector.py:29`
- **修法**：全部 `socket_timeout=2, socket_connect_timeout=2`（对齐批18 notify 先例）。sse_bridge 已有 6s 不动；xsleeper block≤500ms 已处理 Timeout 不动
- **顺手（盲审 A）**：risk.py `halt_reason()`（:249-250 在 try 外）并进 :243 的 try——超时也有 risk_log 落迹
- **验收**：`grep -rnE 'redis\.(Redis|ConnectionPool)\.(from_url)|redis\.from_url' server/src --include='*.py'` 逐处核 timeout 参数（豁免：sse_bridge 自建连接/测试/scripts）
- **不做**：backtest gen() 的 r.get 迁 to_thread（池超时已解冻死要害）

### 27-3 对账差异单永不落库（v2：逐单 commit+rollback）
- **位置**：`scheduler/tasks.py:294-309`（v2 注：编码时按语义定位——G1 先于 G4 施工防行号漂移）
- **修法（v2）**：**逐单 commit**（循环内 execute 后即 `conn.commit()`），内层 except 加 `conn.rollback()`——循环外单 commit 在任一 INSERT 失败后=aborted 事务上 commit 实为 rollback 全批丢弃（盲审 A：含成功行）；单条失败只丢自己且终止 aborted 态保住后续比对 SELECT
- **验收**：新测——mock conn 断言每 INSERT 后有 commit；单条失败后续 INSERT 存活

### 27-4 飞书操作确认卡（v2 重写：官方同款 ws patch+卡 2.0）
- **实证链**：lark SDK 1.7.1 本地源码+1.7.3 wheel 解包+双盲审三重确认 `ws/client.py _handle_data_frame` `elif message_type == CARD: return` 丢弃卡片帧；`EventDispatcherHandler.register_p2_card_action_trigger`（dispatcher_handler.py:224）注册的 processor 只服务 HTTP；**官方 openclaw-lark（ByteDance）`src/core/lark-client.ts:381-396` 对 node SDK 打同款 patch**（wrap handleEventData 把帧头 type:"card" 改写 "event" 交原 handler 路由）——monkey-patch 是官方工程实践
- **修法三件**：
  1. **ws patch**（学官方）：`ws_client.py` 建 Client 后 wrap `_handle_data_frame`——CARD 帧改写 message_type 为 EVENT 再交原方法（dispatcher 的 `_do_without_validation` 会路由到 `p2.card.action.trigger` 的 callback processor）；**版本探针**：patch 前 assert lark_oapi 版本在已验证集合（防 SDK 升级静默失效——探针失败降级文本模式+日志告警）
  2. **确认卡迁 CardKit 2.0**：`build_confirm_card` 改 2.0 构建（SDK `api/cardkit/v1`，创建卡片实体+消息引用卡 id）；按钮 action.value 带 {action, tool, args, nonce}——1.0 interactive 卡是否触发 2.0 事件不确定，**直接用 2.0 消不确定性**
  3. **handler**：`register_p2_card_action_trigger` 消费（扁平 `operator.open_id`/`action.value`）→ 复用 router.py 三道闸中适用的两道（event_id 去重 `feishu:card:{event_id}` 跨通道同值域/60s 时效）+ resolve_im_identity 身份解析（**签名闸不适用 ws**——鉴权=连接级 app_secret 凭证握手，注释写明）→ execute_confirmed_tool 链（actor=username，见 27-13）
- **HTTP 面去留（裁定）**：router.py `/lark/card/callback` **保留**——降级备用通道（若未来切 HTTP 回调即用），去重键同域兼容双通道防双执行；nginx 不加路由（探针外封不变）
- **验收**：新测——patch 后 CARD 帧（mock）路由到 callback processor；event_id 重复拒/过期拒/身份未解析拒；版本探针失败降级路径。**真机验证一轮**（用户配合：飞书发急停→确认卡→点确认→执行+回复）——2.0 卡构建+回调全链唯一可信验证
- **风险声明**：SDK 升级时 patch 可能碎（官方同款风险）——版本探针+降级文本兜底+升级 checklist 注明

#### v3.1 定稿（2026-09-16 双盲审 A/B 全吸收——A 正确性 P0×2+P1×5+P2×8 / B 可执行性 P0×2+P1×6+P2×7，两员 venv 实测踩坑）

> v3 增补段两处结构性错误已更正（见下 ⑤⑦）；v2 残留两处矛盾作废（⑧⑨）。

**修法定稿（编码照此执行）**：
1. **ws patch（条件改写）**：`client._handle_data_frame = wrapper`（实例属性遮蔽，先取 orig 再赋值防递归；跨重连存活——`_reconnect` 复用同一实例）。wrapper 逻辑：读帧 headers 的 type/sum——仅 type==CARD 时尝试 peek：sum>1 先 `client._combine(msg_id, sum, seq, payload)` 拿合成 payload（返回 None=未集齐→透传；末帧重入幂等，单测钉住防 SDK 漂移）；peek=try/except 全包 json.loads 取 `header.event_type`（解析失败/无键=不改写不冒泡，维持 SDK 原行为）。**命中判定=`f"p2.{event_type}" in (callback_processor_map ∪ processorMap)`**（注册表键带 schema 前缀，裸键恒不命中——双盲审同报 P0；单测必须断言合成 CARD 帧真被改写，防"一致地错"）。命中→**遍历 headers 对已有 key=="type" 头原地赋 value="event"**（`_get_by_key` 取首个，append 追加无效=静默死代码——B-P0-2；容器项赋值 TypeError 只能遍历改——A 实测）→交 orig。EVENT 帧不经 wrapper 判断直接透传 orig。
2. **版本探针**：`importlib.metadata.version("lark-oapi")`（`lark_oapi.__version__` 不存在——实测）；已验证集 **{1.7.1, 1.7.3}**（两版本 ws client 源码已核行为一致；requirements `>=1.3` 下限不钉、服务器可能装 1.7.3）+`hasattr(Client, "_handle_data_frame")` 双保险。失败=不 patch+`_CARD_PATCH_OK=False`。
3. **降级机制**：ws_client 模块级 `_CARD_PATCH_OK` flag（True 缺省，探针后置 False）；`process_message_async` 的 confirm_card 闭包按 flag 决定真实发卡或**传 None**（handlers.py 现成降级路径承接，见 ⑩）。**仅 ws 进程生效**；webhook 入口（router.py）不在此机制内（其无公网入口本就不在生产路径，注记不扩散）。每次降级发送时 logger.warning（非仅启动一次）。
4. **handler 注册**：main() builder 链加 `.register_p2_card_action_trigger(on_card)`（handler 闭包捕获局部 fid，不读模块级 _FID）。`on_card(data: P2CardActionTrigger) -> None`（返回 None=ACK 200 空体；执行结果由 execute_confirmed_tool 内 send_text 回复——与 HTTP 面一致，不做 toast）。**线程模型（A-P1-2，对齐补审E-3 同根因）**：handler 内仅字段提取（event_id/action.value/operator.open_id），**全部闸门+执行移入 threading.Thread**——闸门同步 IO（redis 2s+PG 查询）跑 asyncio loop 会冻结连接（LLM chat 同款教训）。
5. **闸门清单（ws 面六闸，顺序钉死：解析→时效→平台级→身份→权限→dedup→spawn；dedup 放最后=500 重推可重走闸门全幂等）**：
   - ①60s 时效：`card_action_fresh(action.value)`（ts 缺/过期拒）
   - ②**平台级 bot 闸（裁定：对齐 HTTP 面沿用批11C A-P1-1——卡片确认面仅平台级 bot）**：main() 启动时查一次本 bid 的 `owner_user_id IS NULL`（bot 配置变更需重启进程，查一次语义成立）；非平台级→拒（文案同 router 降级"请到 Web 执行"）
   - ③身份：`resolve_im_identity(open_id, int(fid))`（int 显式化）；None→拒
   - ④权限键：`_need = "resume" if tool=="risk_resume" else ("halt" if tool=="emergency_halt" else "trade")`；不在 perms→拒
   - ⑤event_id 去重：redis `feishu:card:{event_id}` nx ex=300（**跨通道同域**；form 照抄 router.py:79 含 socket_timeout=2；fail-open 对齐 HTTP 面+warning）
   - ⑥**卡级 exec 去重（新，A-P1-5）**：`feishu:card:exec:{ts}:{tool}` nx ex=300——同卡 60s 窗内连点 N 次（每次新 event_id，dedup⑤ 挡不住）只执行一次；ts=建卡时刻同卡恒定。**router.py HTTP 面同点同修**（同病同修）
   - cancel 点击：仅 ACK 不操作（对齐 HTTP 面 router.py:88 只理 confirm）
6. **确认卡 2.0（结构更正——A-P0-2）**：**按钮直挂 body.elements**（openclaw `sanitizeNativeFeishuCard` 输出形态，生产实证；顶层无 action 键——v3 增补段结构图作废）：
```python
{"schema": "2.0",
 "config": {"update_multi": True},
 "header": {"title": {"tag": "plain_text", "content": f"⚠️ 操作确认: {tool}"}, "template": "red"},
 "body": {"elements": [
    {"tag": "markdown", "content": f"**操作**: {tool}\n**参数**: {json}\n**原因**: {reason or 'LLM 触发'}"},
    {"tag": "button", "text": {"tag": "plain_text", "content": "✅ 确认执行"}, "type": "primary",
     "behaviors": [{"type": "callback", "value": {"action": "confirm", "tool": tool, "args": args, "ts": int(time.time())}}]},
    {"tag": "button", "text": {"tag": "plain_text", "content": "❌ 取消"}, "type": "danger",
     "behaviors": [{"type": "callback", "value": {"action": "cancel", "tool": tool, "ts": int(time.time())}}]},
 ]}}
```
7. **发送（v2 cardkit 实体路线作废——B-P1-1）**：inline 发送，复用 `FeishuClient.send_card` 零改动（msg_type=interactive+content=卡 JSON——node SDK sendCard/openclaw sendCardFeishu 生产同款）。
8. **value 字段钉死（v2 nonce 作废——B-P1-1）**：`{action, tool, args, ts}`；`card_action_fresh` 读 ts 不变；args 回传 dict，spawn 按 HTTP 面同款 `json.dumps(args) if isinstance(args, dict)`。
9. **username 两通道补传（27-13 尾巴）**：ws spawn 与 router.py:135 调用均传 `identity["username"]`。
10. **降级文案（文案师已产出，待办#9 一并落地）**：handlers.py 群聊拒答两条（tool_calls 层/confirm_card 降级层）替换为文案师 A 候选；ws 降级 confirm_card=None 的承接文案核对 handlers.py:116-118 现有措辞，替换为文案师 A 候选 `确认卡片暂时发不出来，这次操作没有执行。请到网页端完成这项操作。`
11. **测试（A/B 实测三坑全收）**：Frame required 字段补齐（SeqID/LogID/service，漏则 SerializeToString EncodeError）；`_write_message` 必须 stub（`_conn=None` 时 EVENT 尾部 ACK 抛 ConnectionClosedException 且在原方法 try 外）；异步用 `asyncio.new_event_loop().run_until_complete(...)`（项目无 pytest-asyncio，test_eventbus.py 先例）；`P2CardActionTrigger({...})` 从 dict 构造实测可行；redis mock 先例 `patch("redis.Redis.from_url", return_value=fake)`。**test_sd_security.py:102-105 适配 2.0 路径**（唯一引用 build_confirm_card 的存量测试）。新钉 ~10：改写命中/不命中/多包幂等/探针失败不 patch/六闸各一/username 传递/2.0 结构断言。
12. **部署 checklist 增补**：开放平台卡片回调（card.action.trigger）需为长连接模式（EVENT 帧推送前提——真机若无任何帧到达先查此配置）；SDK 升级须重验证 patch（升级 checklist 注明）。
13. **不做**：event.token 对照 verification_token 纵深防御（A-P2-8）——ws 连接级 app_secret 鉴权已成立，引入新配置依赖不值。

**实施偏差注记（编码后代码双盲审 A/B 修订，2026-09-16）**：
- ⑥ exec 键主成分改 `open_message_id`（缺失回退 `ts:open_id`）——代码盲审共识：纯 `{ts}:{tool}` 同秒同 tool 两卡互斥（"停掉 s1 和 s2"第二张被静默吞），拦截分支两通道均回文本（文案师第 6 条）
- ③ 降级 flag 落点=feishu_client.CARD_CHANNEL_OK（非计划的 ws_client._CARD_PATCH_OK——闭包与 setter 同模块更净）；降级动作=闭包内直接 send_text（非计划的 confirm_card 传 None——钉钉/企微无卡能力，"暂时发不出来"文案本就不该套用）；handlers.py:118 的 MVP 降级文案保留不动（钉钉/企微语义"平台无卡能力"≠飞书"通道故障"）
- send_card 补响应校验返 bool（代码盲审 B-P1：2.0 卡首发被拒零观测=真机验证期最高可见故障模式恰好无感）
- 挂账下批：自助 bot 发卡前拦截+专用文案（代码盲审 A-P2/文案师建议——闸门已 fail-closed，体验项）；postverify 加 prod lark-oapi 版本 ∈ 集合检查（并入部署后 27-4 真机专项）
- 测试增强：真 SDK Client+真 dispatcher 端到端钉（防同版本热改注册表属性名）；exec 键区分负例（两卡两键）

#### v3 增补（2026-09-16 施工前证据链更新——证据留档，修法以 v3.1 定稿为准；原文结构图/nonce/cardkit 实体路线已作废）

**新证据（node SDK 1.74.0+openclaw 2026.9.4 源码级，npm 镜像拉包核实）**：
1. node SDK `WSClient.handleEventData` 同样 `if (type !== MessageType.event) return`——**node SDK 也丢弃非 EVENT 帧**（源码级实证）
2. openclaw 2026.9.4（@openclaw/feishu 插件）**已无任何 monkey-patch**，ws 模式下 card.action.trigger 挂 EventDispatcher 正常分发（生产实践）——上一棒核实的旧版 patch（lark-client.ts CARD→EVENT 帧头改写）是历史实现
3. ∴ 飞书 ws 长连接下 card.action.trigger **大概率以 EVENT 帧推送**（v2 事件信封 `{schema:"2.0",header:{event_type},event:{operator,action,context}}`，node SDK RawCardActionEvent 类型官方注释佐证 payload 结构）——v2"卡片回调走 CARD 帧"是未实证假定
4. CardKit 2.0 结构实证（node SDK `buildStreamingCard`/openclaw `sanitizeNativeFeishuCardButton`）：`{schema:"2.0",config,header,body:{elements:[{tag:"markdown",...}]},action:{actions:[{tag:"button",text,type,behaviors:[{type:"callback",value:{...}}]}]}}`；inline 发送=msg_type=interactive+content=卡 JSON（node SDK sendCard 同款）

**v3 修法（patch 从无条件改写改为条件改写——双路兼容，比 v2 更保守）**：
1. **ws patch 条件化**：wrap `_handle_data_frame`——仅当帧 type=CARD **且** payload JSON 的 `header.event_type` 命中 dispatcher 注册表（callback_processor_map∪processorMap）时，改写帧头 type=event 交原方法；否则维持原样（EVENT 帧不经 patch 直达原生路径；CARD 帧无事件信封=保持 SDK 原行为）。EVENT 帧（主流路径）与 CARD 帧（历史路径）**双双覆盖**，不赌飞书走哪条
2. **版本探针语义细化**：探针失败≠全局降级——只丢"CARD 帧兼容路径"；EVENT 帧路径（公开 builder API 注册）与 2.0 卡发送不依赖 patch。**降级动作**（保守，对齐 v2 裁定语义）：探针失败→confirm_card 钩子降级 send_text（文案走文案师）+logger.warning；探针通过→正常发卡
3. **handler 注册**：`register_p2_card_action_trigger`（P2CardActionTrigger→Response，返回 None=SDK 回 200 空体）；两道闸 event_id 去重（`feishu:card:{event_id}` 同域跨通道）+60s 时效（action.value.ts）；身份=`resolve_im_identity(operator.open_id, bid)`（ws 进程本身 per-bot，批13 收口语义）；签名闸不适用 ws（鉴权=连接级 app_secret 握手，注释写明）；执行走 execute_confirmed_tool **传 username**（27-13 尾巴：router.py:135 也漏传——G2 提交只改了函数签名没改调用点，本项两通道一并补）
4. **回执模式**：handler 返回 None，执行结果由 execute_confirmed_tool 内 send_text 回复（与 HTTP 面一致，不做 toast）

### 27-5 pool 退避（v2：spawned_at 赋值点钉死）
- **修法（v2）**：`_Child.spawned_at` 记 monotonic；**在 `ensure()` 每次 Popen 处赋值**（盲审 A：凭证变更重起路径 `_stop()` 后重 spawn 的秒崩进程，若只在 __init__ 记会被旧 spawned_at 误判健康）；`note_healthy` 判 `now-spawned_at >= POLL_S` 才重置
- **验收**：新测——spawn 后 <POLL_S 崩溃退避翻倍；≥POLL_S 后崩溃重置；**凭证重起后秒崩不重置**

### 27-6 PermMatrix 失败态（v2：跨组污染+验收可执行化）
- **修法（v2）**：`loaded` 标志——load 成功置 true；**watch group 切换时重置**（盲审 A：失败态 apiSel/navSel 保留上一组值=空矩阵保存还能把上一组权限写进当前组）；失败态矩阵区遮罩+save() 前置拒绝+isDirty 恒 false
- **验收（v2 可执行化）**：代码走查三断言（loaded 守卫在 save/isDirty/watch 三处）+本地 DevTools Network Offline 人工验一次（截图贴进展）

### 27-7 群聊限只读（v2：perms 钳制为根修）
- **修法（v2 两层）**：①**根修**：handle_incoming group 时传 gateway 的 **perms 钳为 {"read"}**（盲审 A：gateway._filter_tools 按 perms 重建工具集，LLM 根本看不到操作工具，避免反复尝试烧轮次）；②兜底：tool_calls 循环 operational 分支 group 拒答（文案"群聊仅支持查询，操作请私聊"，文案师过）+confirm_card 仅 p2p 发（group 降级文本同拒答）
- **验收**：新测——group+perms 钳制（gateway 收到 read-only）；group+操作 tool_call 拒答；p2p 全功能

## G2 快修 13（v2 修订标注）

| # | 位置 | 修法（v2 修订） |
|---|---|---|
| 27-8 | auth_routes.py:86 / ws_client.py:41,59 / feishu_client.py:296 / main.py:113,119 | print→logger；ws_client/feishu_client `===` 调试行整删（与相邻 logger 重复）。**与 27-4 同文件（ws_client）——27-4 先做，新代码直接 logger** |
| 27-9 | backtest.py:341-397 | 裸 json.loads 换 _safe_json |
| 27-10 | system.py:444 / risk.py:47 / sync.py:194+engine.py:1406 / risk.py:136 | `max(1, min(x, cap))`；**page 也钳 ≥1**（A）；float try→ApiError(400,"BAD_PARAM") |
| 27-11 | chat.py:108 | `payload.get("db_role") or payload.get("role", "viewer")`（A：对齐 auth.py:67 缺省写法） |
| 27-12 | im_bots.py:171 | json.loads try+fail-open（对齐 :103-110） |
| 27-13 | feishu_client.py:357 | 审计 actor 改 username；**顺手**：emergency_halt reason 的 `f"飞书:{open_id}"`（:325）同换（A） |
| 27-14 | auth_routes.py:450 | audit target 改用户名 |
| 27-15 | locales 重复键 2 对 | 删前保后（settings='设置'/commission='佣金'，消费方语义核对过零行为变化）；**构建期防：`web/scripts/check-locales.mjs`（acorn 扫 ObjectExpression 重复 Property——vite 依赖树已带 acorn，勿用正则：settings 键跨命名空间 6 处必误报）挂 `package.json` prebuild 链**（check-tokens.sh 先例） |
| 27-16 | DataManage.vue:389-390 | 透传 `cronForm.value.enabled`（B：实际变量名） |
| 27-17 | DataManage.vue:282-285 | onToggle 失败回滚+toast |
| 27-18 | MainLayout.vue:169 | **点修变面修**（A）：对照后端 nav 键全集核所有路由前缀映射（observe/integrations 同查），补齐缺失映射 |
| 27-19 | Logs.vue:57-60 | 非 403 也 toast |
| 27-20 | UserManagement.vue:270-287 | 创建组成功同步 origDesc |
| 27-21 | **sync.py:337-338**（B：位置勘误，非 system.py） | 吞错加 logger.warning |

## G3 中等 5（v2 修订）

### 27-22 update_permissions 无 audit_log
- 三维写后各记 audit_log（role/dimension/键集摘要）——对齐 update_user_override 先例（auth_routes.py:279）

### 27-23 changeProvider 后端事务化（v2 改选）
- **选后端事务化**（A/B 同判：前端倒置引入"新 provider+旧数据混合+重试被守卫卡死"新缺陷）：新端点或复用现有端点事务内 改 provider+删旧数据 原子提交；重建仍独立触发（幂等可重试）
- **验收**：新测——事务中途失败 provider 不变；成功后旧数据零残留

### 27-24 role 双源统一
- DataManage.vue:189 删除钮显隐改 meOnce（getMe）——对齐 MainLayout

### 27-25 ⌘K 索引 i18n
- searchIndex 改 computed/ref + locale watch 重建（v2：模块级 const 数组非响应式，是结构改动）；路径 `web/src/layouts/MainLayout.vue`（B 勘误）；验收加"切语言后 ⌘K 即现新语言"

### 27-26 大表聚合缓存（v2：按 freq 分键）
- risk.py:272 data-integrity（**缓存按 freq 参数分键**，A：1D/1min/5min 三形态）+ backtest.py:147 minute-status（同查参数分键）60s 进程内缓存（_METRICS_CACHE 先例 system.py:149）

## G4 挂账清偿 5（v2 修订）

### 27-27 死代码清理（v2：连根拔+孤儿清理）
- 删 `check_user`（**连 feishu_bot/__init__.py:5+bot.py:10 两处 re-export+test_feishu_integration.py:295-307 三测试**，B）；删三表探测（tasks.py:257-268，**孤儿 conn.commit()（:269）一并清+ :312-313 引用注释同步**，A）；删 astock_minute_analysis 死任务（B 复核：无 beat 无 send_task 无 API 引用，可删）；data_increment_daily **连函数删**（B：只摘 beat 留僵尸函数）——**先于 27-31 施工**（B：免给死任务写锚点）
### 27-28 配额三副本抽取（v2：签名支持平台级）
- im_bots.py 抽 `_quota_guard(uid)` 单源；**第三处是平台级路径（owner IS NULL）——签名要能表达平台计数分支**（A）；三面调用；**与 27-29 合并施工**（B 同文件顺序）
### 27-29 魔法配置收口（v2：alembic seed）
- **迁移 0078** seed `user_bot_quota='5'`/`platform_bot_quota='10'`（0076 INSERT...ON CONFLICT DO NOTHING 先例——表行存在前端 RunConfig 才渲染编辑项，代码级 default 前端改不了）；代码读表回落 5/10；其余常量保模块头集中（不强行全配置化）
### 27-30 标识符白名单（v2：正则含大写）
- 正则 `^[A-Za-z_][A-Za-z0-9_]{0,62}$`（A：存量 pg_table 含 `bar_1D` 大写——原正则会静默破坏合法清数据）；不符跳过 DELETE 但继续 :274 重置 UPDATE（B：对齐现有 try/except 结构）+warning；部署前 `SELECT DISTINCT pg_table FROM sync_config` 全量预校验
### 27-31 beat crontab 化（v2：映射表+协调+行为收紧声明）
- **先 27-27 摘死任务**再映射；映射表（A，锚点避部署窗禁段 8:55/11:35-12:40/15.05）：astock-select-daily→16:0x（避 16:30 daily-report）/drift-check→15:3x-16:0x/notifications-cleanup→03:1x/logs-cleanup→03:2x（避 04:00 备份/04:07 校准）/convertible-terms-sync→03:4x/static-list-sync 604800→周日 04:37/broker-health-check 21600→hour='*/6' 错峰分钟；**900→'*/15'、3600→整点错峰、300→'*/5' 可无损表达；60s/30s 保 interval（crontab 表达不了）**；sync-scheduler 300s 有 cron 窗口功能依赖**勿动**
- **行为收紧声明**（A）：现浮点锚=beat 启动时刻本就随机——crontab 化是收紧非保持；staging 彩排观察首轮锚点不撞车；部署当天首跑时刻最长偏移 24h（选低峰锚）

## 八步法映射（v2）
方案 v2（双盲审 26 项修订全吸收+27-4 重裁）→ 编码（G1→G2→G3→G4；**显式依赖序：27-4 先于 27-8（同文件）；27-27 先于 27-31；27-28+27-29 合并施工**；27-4 内部：patch→卡 2.0→handler→测试→真机）→ 代码双盲审 → 单测（+新钉 ~18）→ 提交 → staging 彩排（27-31 首轮锚点观察）→ prod（部署窗内）→ **部署后专项：27-3 对账差异单真 DB 落库 / 27-4 飞书卡片真机确认（用户配合一轮）/ 27-30 存量 pg_table 预校验**。
