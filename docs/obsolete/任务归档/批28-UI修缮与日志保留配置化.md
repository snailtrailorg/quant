# 批28 UI 修缮+日志保留配置化（2026-09-16 立项；v2.1=方案盲审 A/B 全吸收——A P0×1+P1×6+P2×5 / B P0×2+P1×6+P2×8，两员交叉确认）

> 背景：用户 prod 验收后提出 7 项修缮（UI 六项+日志 GC 配置化一项）。
> 裁定：邀请"作废"=现有"撤销"改名换图标+**恒显禁用态**（用户裁定 2026-09-16 二次修订：撤销显示出来不可操作做成不能点击，删除保留两个不一样）；通知消息页签此前已决策干掉（批23 通知确认机制退役余留 UI）。

## G1 UI 六项

### 28-1 邀请记录：撤销按钮恒显+禁用态（用户三裁：①就是撤销 ②恒显不可操作做成不能点击 ③措辞保留"撤销"比作废合适）
- `UserManagement.vue:110`：IconBtn `RefreshLeft`→`Close` 图标（type warning 保留），**显示条件改恒显——非 pending 状态 `:disabled`**；title `account.inviteRevoke` 与措辞族**全部不动**（撤销/确认撤销/已撤销——用户裁定保留原词）
- 后端 `/api/invites/{tid}/revoke` 端点/行为/审计 action `invite_revoke`/状态值 `revoked` 均不动
- `RefreshLeft` import（:213）换 `Close`（IconBtn 的 disabled 支持核对——el-button 原生透传）

### 28-2 用户管理页签文案
- 页签：用户列表→**用户**、用户群组→**用户组**（`um.tabUsers`/`um.tabGroups` zh:160——仅本页消费，改值安全）
- 页签下标题"用户管理"→**所有用户**：改 **`account.userMgmt`** 值（zh:835/en:2023；仅 UserManagement.vue:12 消费）。**双键陷阱（盲审 B P1-4）：`nav.userMgmt`（zh:170）与 account.userMgmt 同名同值——禁按值 replace_all（必中两行=导航菜单变名），按命名空间精确改 account 侧，nav 不动**

### 28-3 通知消息页签退役
- `Observe.vue`：tabs 数组删 notifications 项+NotificationTable import/模板引用删（v-else 改 v-else-if 结构核对）
- `components/NotificationTable.vue` 文件删（仅 Observe 引用——已核）；**`api.js deleteNotifications` 删（仅此组件用）；`getNotifications` 不删（Dashboard.vue:113,194 仍在用——盲审 A P2-4）**
- 词条：`tabs.notifications` zh/en 删；`log.notifyCategory`/`log.titleCol` 孤儿词条顺手清
- 后端 `/api/notifications/delete` 成 UI 孤儿端点**保留**（require_perm 保护，无害——明写防下棒误判死码再动后端）；notifications_cleanup 任务与通知数据链**不动**（告警 dispatch 仍在写表——UI 呈现退役≠数据链退役）

### 28-4 Profile 页签视图边框+底部关闭钮
- tab 内容容器（TabsShell 内容外层）加边框框起（局部样式：border+padding+圆角，令牌化）
- 边框下方右侧放"关闭"按钮：**`@click="onOuterClose(() => { dlg = false })"`**（盲审 A P1-1 契约——done 显式落 dlg，直接绑 onOuterClose 会把 MouseEvent 当 done 抛 TypeError；与扫码挽留闸同链）；仅 Profile（TabsShell 组件全局不动）
- `common.close` 词条已存在（zh :22/en :1215）复用

### 28-5 修改密码按钮靠右+弹窗元素未达右缘清查（v2.1 落 A/B/C 分类表——盲审 B 实测 51 处/弹窗内 27）
- 根因①：pwd info-table `max-width: 480px`+按钮行在 480 内。修：按钮行移出 info-table 独立全宽**左提示右按钮**（pwd-rule 随行走 space-between）；**右缘基准=28-4 新边框容器内右缘**（盲审 B P2-5 裁定——28-4 先做改参照系）
- 根因②定宽清查 **A/B/C 分类表（盲审 A/B 共识，逐文件点名——方案清单=承诺）**：
  - **A 类改自适应**（单列两栏形态）：Profile.vue:38/137/163/166、UserManagement.vue:67/169/174、Factors.vue:97、ParameterForm.vue（3 处）
  - **B 类保留定宽**（横排紧凑排版约束）：KlineDialog.vue:9/15（两 select 并排工具条）、Strategy.vue:87-97/139-152（因子行+参数定义行 7 控件横排）、Backtest.vue:113（feePreset 行）
  - **C 类不动**（弹窗外/刻意布局）：RunConfig 表格列内编辑控件、Trading.vue:92-99（页内 inline form）、SymbolManage/Pool/DataSources 工具条与表格单元格、Login 系 el-card width:400px（居中登录卡）
- **控件分支（盲审 B P1-1）**：el-input 去定宽即可填满；**el-select/el-input-number 需显式 width:100%**（EP 基类 .el-input-number 固定 150px/eselect 收缩到内容宽——"去定宽"不等于自适应）；divider 判定=取所在内容容器全宽
- 逐文件核对非正则批替；build+用户视觉验收（两级验收铁律）

### 28-6 系统指标顺序
- `SystemMetricsCards.vue:49-53` defs 数组 [mem, disk, swap]→**[mem, swap, disk]**（内存/交换分区/磁盘——行号勘误盲审 B P2-3；阈值/告警按 kind 独立查表无顺序耦合）

## G2 日志保留配置化

### 28-7 保留期限配置项（含审计永久保留缺省；v2 盲审 A P0/P1 全收）
- 现状核：cleanup_logs（批25）已实现**硬编码 30 天**批删+beat 挂载（批27-31 crontab 03:2x 已上产）——实现有效，仅缺配置化；audit_log 无清理（事实永久）
- **两键同语义（盲审 A P0-1）：0=不清理（skip）**——log 与 audit 一致，杜绝"log=0=保留 0 天=全表清空"的误读路径；三处设防：① update_system_config 对两键服务端校验整数≥0（非法 400）；② RunConfig el-input-number `:min="0"`；③ 任务侧 days<=0 → skip+logger.warning（纵深防直改库）
- 迁移 **0079**：seed **四列含 value_type='int'**（`0076_resource_cfg.py` 先例——**勿照 0078 漏 value_type 列**，盲审 A P1-4）；顺手 `CREATE INDEX CONCURRENTLY idx_audit_log_ts`（盲审 A P2-1——audit_log 现仅 PK 无 ts 索引，retention>0 时日频批删全表扫；18 号规范 CONCURRENTLY 防锁写）
- `cleanup_logs` 改读配置（**抽 `_read_int_cfg(key, default)` 小函数**——try/except 回落范式 im_bots.py:203-211 先例，盲审 B P1-2：**既有钉 test_batch25_system_log.py:96-105 的 side_effect 迭代器会被配置读取消耗一格→必红，须改服务配置行**）；**SQL 写法钉死（盲审 A P1-5）：Python 侧算截止时刻 `datetime.now(UTC)-timedelta(days=n)` 后 `WHERE ts < %s` 参数化——禁 f-string interval（注入面）**；**audit 清理并入本任务**（零 beat 改动，返回保 `"deleted"` 键+增 `"audit_deleted"`——向后兼容既有断言风格）；audit_retention_days>0 才清，0=永久缺省；audit ts 索引照 **0077 普通 create_index 式**（盲审 B P1-5 勘误 A 的 CONCURRENTLY 说法——0077 先例即事务内式）
- 前端 RunConfig 渲染两编辑项（**表行存在才渲染**——27-29 先例）；**description 走 DB seed 单语中文**（含"0=不清理"语义——RunConfig 机制不走 locale 词条，盲审 A P2-3）：'运行日志保留天数（0=不清理）'/'审计日志保留天数（0=永久保留）'

## 施工顺序与验收
G1（28-1→28-6，纯前端+词条）→ G2（迁移+后端+RunConfig）→ pytest 全绿+build 绿+locales 门 → 代码双盲审 → 视觉项用户验收（两级验收）→ 与批27 同车 staging→prod。

**测试**：28-7 新钉 ~5（cleanup 读配置生效/回落 30/**log=0 不清（盲审 A P0-1 负例）**/audit=0 不清/audit>0 清）；UI 项无逻辑测试（视觉用户验收）。

**不做**：作废扩展到已使用状态（裁定=撤销语义恒显禁用）；通知数据链/清理任务退役；TabsShell 全局边框（仅 Profile）；28-5 密集网格定宽（白名单排除）。
