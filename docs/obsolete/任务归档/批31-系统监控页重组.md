# 批31 · 系统监控页重组（三页签）

> 2026-09-16 立项，用户裁定（口头确认）："把内容重新组织，并没有功能上的改变"；**默认落运行状态**。

## 一、变更

`web/src/views/Observe.vue` 单文件重组 + 词条（组件全部复用零改动）：

```
现状：三组卡片直铺 + "系统日志"卡内双页签（logs/audit）
改为：顶层三页签（TabsShell）
  1. 运行状态（default）= 资源消耗（原"系统指标"仅改名）+ 系统服务 + 实时连接
  2. 运行日志（Logs 原样，含底部邮件发件箱——批22 hotfix3 裁定随迁）
  3. 审计日志（Audit 原样）
```

- 外层"系统日志" el-card 壳拆除（页签提升后无承载必要）；页签间距沿用 TabsShell 缺省
- 词条：`sysmon.metrics` → "资源消耗"（zh/en）；新顶层页签名"运行状态"（tabs 命名空间或 sysmon.tabStatus）；旧 `sysmon.logsTitle`（"系统日志"卡头）随壳退役
- 零功能变更：无接口/逻辑/数据改动；批28-6 的指标顺序（内存→交换分区→磁盘）等既有裁定全部保持

## 二、限定范围

只改：`web/src/views/Observe.vue`、`web/src/locales/index.js`（zh/en 两处）。不碰五个子组件/后端/路由。

## 三、验收

- `npm run build` 绿（含 check-tokens/check-locales 双守门）
- 本地 5173 视觉验收（用户本人——前端交付铁律：视觉判断归用户）：三页签就位/默认运行状态/三组卡片完整/日志与审计可用/发件箱仍在运行日志底部
- 文案改名过文案师

## 四、流程说明

微批（单文件重组）走**独立快审**先例（ca32ba5 同款）+ 用户本地视觉验收；不走双轮双盲。
