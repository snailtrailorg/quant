# 批37 · UI 调整三件（2026-09-17 用户指令；微批）

1. **短信凭证迁集成中心**：AlertSettings 的短信凭证弹窗（阿里云 AccessKey/签名/模板×2）抽 `SmsCard.vue` 组件，集成中心新页签「短信凭证」（SmtpCard 同款）；端点 GET/PUT `/api/alerts/sms-config` 零动；告警页 header 的凭证入口图标钮退役（告警页保留 sms_configured 横条提示——配置入口在集成页）。
2. **菜单改名+置底**：nav.permResources「资源注册表」→「系统权限」；nav.settings「设置」→「系统设置」；ops 组内顺序交换=系统权限在前、系统设置最下（MainLayout 模板 + 注册表 NAV_ITEMS_BASE order 同步）。
3. **系统权限页列宽**：后两页签状态列 90px 放不下只读 tag 长句——改短词 tag「只读」+title 保留完整句（readonlyShort 词条）；表格列默认宽微调。

流程：编码→独立快审→build 双守门→本地验收→上产。
