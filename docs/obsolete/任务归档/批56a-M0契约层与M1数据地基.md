# 批 56a：M0 契约层 + M1 数据地基（表与填充）

> 29 号《实施模块与契约》v3.1 冻结后的第一个实施批。范围=29 号 §三（M0 全部）+§四（M1 减 ts 迁移——56b 另批）。方案=29 号（已六审冻结），本文件只做任务自包含化。

## 目标
把 28 号蓝图的跨模块类型落成可执行代码+测试钉+CI 断言（M0）；建 SecurityMaster/节奏域三表+回填+读接口（M1 后端）；交付标的属性查询页（M1 面）。完成后：26 个 DataKind 有类型钉守门、查 600000.SHSE 返回乘数/时段/品类。

## 依赖（就绪）
- 29 号 v3.1 冻结 ✅（方案=其 §三/§四，逐条照做不偏离）
- 28 号 v3.3 ✅（术语/组合表/DDL 来源）
- 迁移链 head=0091 ✅（本批 0092 接续）
- 现有测试基线 1193 绿 ✅

## 产出
**M0（纯类型零风险）**
- 新 `server/src/quant_common/contract.py`：DataKind(26)/Temporality/KIND_TEMPORALITY/is_legal/错误三分类/AsOf/DataRequest(含 sub_kind+mode)/Subscription/ContractEvent/Watermark/SnapshotRow(TypedDict)/CapabilityDecl/Quality/Scope(含 ASTOCK_ALL/ASTOCK_SHSE_SZSE 常量)/ALLOWED_BAR_COLUMNS
- `adapters/base.py` register_adapter 加校验钩子（类有 `capability_decls` 属性则逐项 is_legal——现状无声明=空转，29 号立法）
- 新 `server/tests/test_contract_gate.py` + `server/tests/fixtures/provider_gate_whitelist.txt`（首轮基线）
- 分层映射附录（contract.py 模块 docstring 内表）

**M1 后端**
- 迁移 0092：security_master/security_state/market_hours/band_rules 四表+种子（astock_main 五阶段时段/crypto_247+band 规则四行）+三表合并回填（static_symbols+etf_basic_info+cb_basic_info→按品类标 category）+九张数据表加 dataset_version 列（bar_1D/1min/5min/15min/30min/60min/1h/4h/index+daily_basic）
- 新 `server/src/data_platform/security_master.py`：SMClient（get/effective_attr/covers）+MarketHours（sessions/day_anchor/is_auction）
- 填充链接线：static_list/etf_list/cb_basic 三同步 upsert security_master（ON CONFLICT DO UPDATE）；namechange_sync 追加写 security_state(st)；cb_basic 追加写 security_state(conv_price)
- schema_expectations chain_scratch 重跑

**M1 面**
- 前端标的属性查询页（集成中心新页签，只读：vt_symbol 查询→主档+时变时间线+时段表展示）；词条文案师；perm=read

## 限定范围
- 只改：quant_common（新 contract.py）/adapters/base.py（仅注册钩子）/data_platform（新 security_master.py）/engine.py（仅三个 list/basic handler 追加 upsert 一行调用+namechange 追加）/migrations 0092/web（新页+路由+词条）
- 不碰：ts/as_shanghai（56b）、routing（57）、_HANDLERS 结构（58）、任何现有同步行为（追加写不改写）
- 明确不做：suspend/dividend/margin_tier 新同步（29 号占位）、ts UTC 迁移（56b 生死前置另批）

## 接口契约
新加（29 号 §三/§四逐字照抄，此处列关键签名）：
```python
# quant_common/contract.py
DataKind = Literal["bar_daily", ..., "fx_rate"]        # 26 词全集照 28 §十一
def is_legal(kind: DataKind, t: Temporality) -> bool
class Scope(TypedFrozen):  markets; exchanges; categories  # None=通配
ASTOCK_ALL = Scope(frozenset({"astock"}), None, None)
@dataclass(frozen=True) class DataRequest:  # 9 字段照 29 §三（kind/sub_kind/symbols/temporality/range_/freq/as_of/deadline_ms/consumer_tag/mode）
@dataclass(frozen=True) class CapabilityDecl: kind; temporality; scope; sub_kinds; pit
SnapshotRow = TypedDict(...)                            # 14 字段照 28 §3.1
# data_platform/security_master.py
class SMClient: get(vt_symbol)->SecurityAttr|None; effective_attr(vt_symbol,kind,at)->dict; covers(scope,symbols)->bool
class MarketHours: sessions(session_id,at)->list[Phase]; day_anchor(session_id)->datetime; is_auction(vt_symbol,at)->bool
```
现有调用位置：register_adapter（adapters/base.py:88）；engine 三 handler（astock_list/etf_list/cb_basic 的 save 函数后追加）；namechange handler（engine.py 全量重建分支）。

## 验收标准
1. `pytest server/tests/test_contract_gate.py server/tests/test_security_master.py -q` 全绿（组合表 26×3 全格断言/Scope covers 三例/is_legal 正反例/SnapshotRow 键集/回填后查 600000=stock+乘数 100+astock_main、查转债=T+0、查 ETF=etf）
2. `pytest server/tests/ -q` 1193+新增全绿零回归
3. `cd server && alembic upgrade head && alembic downgrade 0091 && alembic upgrade head`（0092 双向重放绿）
4. dev 真库：`psql -c "SELECT category, multiplier, trade_phase FROM security_master WHERE vt_symbol LIKE '600000%' OR category='convertible' LIMIT 3"` 返回正确品类
5. `npm run build` 绿+check-locales 绿（新词条 zh/en 对称）
6. CI 三断言可运行：provider 白名单基线文件入 repo+to_contract 列集常量钉（断言 2 执法在 M3 接 to_contract，本批钉常量）
7. 前端页面 dev 实测：输入 600000.SHSE 展示三块信息

## mock 方式
- SMClient/MarketHours 单测：mock get_conn 返回构造行（_FakeConn 惯例，test_external_interface.py 同款）
- 回填正确性：真库断言（不 mock——行为级验收）
- 填充链 upsert：patch get_conn 断言 SQL 含 ON CONFLICT
- 前端：build+守门+dev 人工实测（UI 两级验收铁律）

## 参考文档（≤2）
1. `docs/architecture/A04-数据供给总线实施模块与契约.md` §三/§四（**主规格——逐条照做**）+§一第四铁律（风格对齐八项）
2. `docs/architecture/A03-数据供给总线设计.md` §十一（26 词+组合表）/§3.1（SnapshotRow 字段）/§4.1-4.2（DDL+五阶段时段表）
