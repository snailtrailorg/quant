# 批 59：M4 消费门面（DataBus+消费方迁移）

> 29 号 §七=主规格。只切拉型（subscribe 句柄占位 M5）。

## 目标
DataBus 四方法实现（local_pg 候选+fetch-on-miss+版本冻结+水位线返回）；消费方逐个迁移（回测→因子→选股→分析→风控快照）。

## 依赖（就绪）
- 批 58 ✅（fetch(kind) 在位）

## 产出
data_platform/databus.py（get/get_bars/get_snapshot 骨架+db.get_bars 包装为 local_pg 候选+快照 Valkey TTL+SET NX 写者选举）+版本冻结（frozen_or_current+回放队列）+五消费方迁移补丁。

## 限定范围
不动交易（M6）；不重写 db.get_bars（包装）；subscribe 返回占位句柄。

## 接口契约
get(kind,req)->ContractFrame；store.save(frame,version=frozen_or_current(kind))。

## 验收标准
①迁移前后同一回测 run 结果完全一致（对账脚本）②快照走 Valkey 缓存（键含 account）③冻结窗口读旧快照实测（切换演练）④on-miss 链内完成不重入门面（代码断言+测试）。

## mock 方式
回测对账用真库两跑 diff；快照缓存 mock redis。

## 参考文档
29 号 §七；28 号 §七/§九。
