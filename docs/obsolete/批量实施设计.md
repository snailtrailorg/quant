# 批量实施设计（分层 + 子模块 + 接口签名 + 边界）

> 方法论：分层设计 -> 子模块拆分 -> 设接口**签名** -> 独立开发测试 -> 组装集成。
>
> **下一棒入口 = 模块 A，从 A1 开始**（无"阶段1"中间层，已砍）。

## 已完成（63 单测过）
- #2 因子 API / #7 三账对账 API / #8 data_continuity 修复 / #13 bcrypt（已完成）
- PI1 alert_notify->MessageChannel ✅ / PI2 risk_control->DB ✅
- 待办表 PI1/PI2 已标 ✅
- A1 分钟线管线 ✅（2026-08-09，migration 0022 + per-symbol 泛化 _PER_SYMBOL_META + type 级 handler _sync_astock_minute + 12 单测）

## 跨模块接口签名（3 个关键，组装必依赖）

### ① BacktestEngine.on_bar_callback
```python
def on_bar_callback(bar: dict, ctx: dict) -> None
# bar: {ts: datetime, open/high/low/close: float, volume: float}
# ctx: {position: list[Position], equity: float, trades: list[Trade], log: str, progress: {current:int, total:int, pct:float}}
# 调用时机：BacktestEngine.run 每完成一 bar 的 on_bar 逻辑后调
# 回调内：publish {ts, ohlc, position, equity, trades, log} 到 Valkey `backtest:{task_id}`
```
- 现状：`backtest.py:95 BacktestEngine.run(config, bars, ...)` 当前无 callback 参数

### ② save_bars 前校验 validate_bars
```python
def validate_bars(rows: list[tuple]) -> list[tuple]
# 输入：原始 rows（vt_symbol, freq, ts, o, h, l, c, vol, amount, ...）
# 校验：剔 open/high/low/close=0 的行 + 标 ts 断点（记 warning，不剔）
# 输出：清洗后 rows
# 位置：db.py:55 save_bars() 开头调 validate_bars(rows)
```

### ③ Broker.get_credentials
```python
def get_credentials(market: str) -> dict
# market: "astock"/"convertible"/"etf"（XTP）或 "binance_perp"/"okx_perp"（加密）
# 返回 XTP: {app_id, app_secret, md_server, td_server}
# 返回 OKX: {api_key, api_secret, passphrase}
# 位置：broker.py Broker 基类 + XTPBroker/BinanceBroker/OKXBroker 实现（PT5 已有 _BaseBroker.get_credentials）
# PI3：XTPAdapter.__init__ 改用 Broker.get_credentials（当前 adapters.py:77 用 .env）
```

## migration 编号（0017 head，新表 0018+）

| migration | 表 | 模块 |
|---|---|---|
| 0018 | backtest_runs + backtest_symbols + pools + pool_symbols | B2 |
| 0019 | data_source_usage | A4 |
| 0020 | llm_budget | D5 |
| 0021 | strategy_account | C6 |
| 0022 | bar_1min / bar_5min（或加 init-schema） | A1 |

**注意**：待办 #19 写"migration 0010 回测组表"是**旧编号**（0010 已被 simplify_llm_gateway 占用）。回测组 = **0018**。

## 5 子模块 + 拆任务（含现状位置 + 可并行 + 检查点）

### A 数据模块
**可并行**：A2/A4/A5 互不依赖（A1 完成后可并行做）
**检查点**：A 结束 = 分钟线入库 + 完整性看板能看 + 备份脚本就绪

| # | 任务 | 现状在哪改 | 输出 | 测试 |
|---|---|---|---|---|
| A1 ✅ | 分钟线（#23） | tushare_adapter.py:109 pull_minute（已有）；db.py save_bars（复用）；sync_config 加 astock_minute 类型 | bar_1min/5min 表 + sync | mock Tushare |
| A2 ✅ | 质量校验（#30） | db.py:55 save_bars() 开头加 validate_bars | 剔0/断点 hook | mock rows |
| A3 ✅ | 完整性看板（#9） | main.py 加 /api/data-integrity；前端新页 | 端点+前端 | DB 聚合 |
| A4 ✅ | 用量监控（#36） | 新 data_source_usage 表（0019）+ DataSource.record_usage 写 DB | 表+端点 | 真 DB |
| A5 ✅ | 备份（#26） | 新 scripts/backup-db.sh + crontab | 脚本 | 脚本验证 |

### B 策略回测模块
**可并行**：B5/B7/B8 互不依赖（B1-B4 完成后可并行）
**检查点**：B 结束 = 回测组能跑 + 实时可视化 + 策略表单 + 绩效扩展

| # | 任务 | 现状在哪改 | 输出 | 测试 |
|---|---|---|---|---|
| B1 ✅ | on_bar 回调（#18） | backtest.py:95 run() 加 on_bar_callback 参数 | callback hook | 引擎单测 |
| B2 ✅ | migration 0018（#19/#22） | 新表 backtest_runs/symbols+pools/pool_symbols（task_id 关联 tasks） | 表+端点 | DB |
| B3 ✅ | 回测 API+SSE（#1） | main.py 加 /api/backtest；tasks.py 加 Celery backtest task | 端点+Celery+SSE | routes+import |
| B4 推迟 | 前端回测组页（#20） | 新 BacktestList/Run/View.vue + Pool.vue 完善 | 前端页 | build |
| B5 ✅ | 绩效扩展（#21） | backtest.py:200 _calculate() 加 波动率+索提诺（α/β/信息率/基准 TODO 需基准） | 扩展指标 | 计算 |
| B6 推迟 | 策略表单（#3） | main.py 加 /api/strategies 端点；前端策略表单（Factor+权重+DSL） | 端点+前端 | DSL |
| B7 推迟 | 因子缓存（#28） | factor.py 向量化重构（大工程，回测组/实盘优化时做） | cache | 性能 |
| B8 ✅ | 风控覆写（#29） | risk.py RiskDecision 加 adjusted；strategy.place_order 用 adjusted | adjusted | import |

### C 实盘模块
**可并行**：C5/C6 互不依赖（C1-C4 完成后可并行）
**检查点**：C 结束 = strategy_runner 能起 + Dashboard 持仓 + 断连自愈
**服务器测前置**（C2/C4）：
- C2：vnpy_xtp 服务器 cp311 + XTP 测试账户（253191001822）+ 交易时段（9:30-15:00）+ broker_config 配
- C4：XTP 连接 + query_position 实盘 + 交易时段
- 本地 mock 到什么程度能交：C2 mock XTPAdapter（send_order/cancel/query）+ strategy_runner 入口逻辑；C4 mock query_position 返回 + 前端接

| # | 任务 | 现状在哪改 | 输出 | 测试 |
|---|---|---|---|---|
| C1 | polkit+systemd（#14） | 新 scripts/systemd/quant-strategy@.service + polkit 规则 | 规则文件+模板 | 服务器配 |
| C2 | strategy_runner（#4） | 新 src/strategy_runner.py（入口）；adapters.py:77 XTPAdapter | 入口脚本 | mock+服务器 |
| C3 | Broker DB（PI3） | broker.py _BaseBroker.get_credentials（PT5 已有）；adapters.py:77 XTPAdapter.__init__ 改用 Broker | 凭证 DB | mock |
| C4 | Dashboard 持仓（#6） | main.py /api/position（已有？查）；前端 Dashboard.vue 接 | 前端接 | 服务器测 |
| C5 | 断连自愈（#31） | 新逻辑（heartbeat 检测 + pull_daily 补缺口） | 补采+重算 | mock |
| C6 🔄 | 账户绑定（#27） | migration 0021 建 strategy_account 表（端点待 C2） | 表 | DB |

### D AI 模块
**可并行**：D3/D4 互不依赖（D1/D2/D5 完成后可并行）
**检查点**：D 结束 = WS流式 + 分钟研判 + AI解读/归因 + 预算预警

| # | 任务 | 现状在哪改 | 输出 | 测试 |
|---|---|---|---|---|
| D1 ✅ | WS 流式（#24） | gateway.chat_stream（已有）+ main.py /ws/chat WebSocket | WS 端点 | routes |
| D2 | 分钟研判（#32） | astock_analysis/analysis.py MinuteAnalysisEngine（占位）；依赖 A1 分钟线 | 引擎 | mock |
| D3 | 条款解读（#33） | cb_basic_info 条款 + gateway.chat | LLM 解读 | mock LLM |
| D4 | 日志归因（#34） | 异常日志 + gateway.chat | LLM 归因 | mock |
| D5 🔄 | 预算预警（#38） | migration 0020 建 llm_budget 表（端点+告警待） | 表+预警 | DB |

### E 运维集成模块
**可并行**：E1/E2/E3 互不依赖
**检查点**：E 结束 = 分时休眠 + 告警配额 + i18n + 场景串联 + 全链路测试通过

| # | 任务 | 现状在哪改 | 输出 | 测试 |
|---|---|---|---|---|
| E1 | 分时休眠（#25） | scheduler/app.py beat schedule + tasks.py 判断时段 | beat 判断 | mock |
| E2 | 告警配额（#39） | alert_notify notify.py（PI1 已部分 _quota_exceeded） | 日配额+聚合 | mock |
| E3 | i18n（#12） | locales/index.js + MainLayout.vue 全改 t() | zh/en+t() | build |
| E4 | 场景串联（#11） | 新逻辑（选股->池 / 回测->实盘） | 串联 | 集成 |
| E5 | 全链路测试（#35） | A-D 组装 | 集成测试报告 | 集成 |

## 依赖图（标可并行块）

```
A1(分钟线) ──> A3(完整性) ─┐
A2(质量) ──────────────────┤
A4(用量) ──────────────────┤ A 检查点
A5(备份) ──────────────────┘
         │
         ├── A2/A4/A5 可并行 ──┘
         │
B1(on_bar) ──> B3(回测API) ──> B4(前端)
B2(migration) ──┘
B5(绩效) | B7(缓存) | B8(覆写)  ← 可并行（B1-B4 后）
B6(策略表单)
         │ B 检查点
         │
C1(polkit) ──> C2(strategy_runner) ──> C4(Dashboard)
C3(Broker DB) ──┘
C5(断连) | C6(账户绑定)  ← 可并行
         │ C 检查点
         │
D1(WS流式) ──> D2(分钟研判, 依赖 A1)
D3(条款) | D4(归因)  ← 可并行
D5(预算)
         │ D 检查点
         │
E1(分时) | E2(配额) | E3(i18n)  ← 可并行
E4(场景串联) ──> E5(全链路测试)
```

## 跳过/推迟
- #15 代码框 / #42 bge-m3 RAG（未来）
- #41 加密 CTA（外部 gate）
- #40 漂移监控（实盘后）
