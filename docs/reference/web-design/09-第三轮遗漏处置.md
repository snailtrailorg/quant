# 09 · 第三轮遗漏处置（三途径交叉：站点 / 文档 / 源码）

> 2026-08-29 · 三路核查员背靠背扫描，按"全量接收、只分类不否决"原则处置。C 类仅当"功能本就不该有"才可忽略，不得因"未实现"而忽略。

## 0. 重要校准：线上前端 08-28 已更新

第一轮审计基于 8-21 构建；08-28 三批上产后部分 UI 断言过时，**以源码为准**：

| 06 原断言 | 校准 |
|---|---|
| B#11 健康页缺磁盘表/hub 维度 | **已渲染**（磁盘表+hub 心跳/订阅数在） |
| B#1 风控页缺总闸红警条 | **已渲染**（总闸红条+三级开关说明在） |
| B#8 转债条款解读"无消费入口" | **AI 条款解读按钮已渲染**（/convertible/terms 已消费） |
| 漂移检查 [C]"#40 推迟" | **beat 三任务在产**（disk-monitor/data-continuity/drift-check），仅缺 Web 落地页 → 升 [B] |

（行为级断言仍有效：backtest 500、Pool 清空 bug、监控共用曲线等——盲审二实测于 8-29。）

## 1. A 类 · 补进原型（8 项）

| # | 功能 | 归属 | 来源 |
|---|---|---|---|
| A1 | **顶栏语言切换**（zh/en，1950 行双语文案+邮件/条款随 lang） | 顶栏 | 三路交叉 |
| A2 | **个人中心**：头像三态（36 系统图标/上传+裁剪 2MB 校验）/昵称/改密/自助注销（脱敏+末位 admin 保护） | 设置 · 个人资料 tab | 三路交叉 |
| A3 | **邀请管理**：4 态记录+撤销+《条款》强制滚动阅读 | 设置 · 账号与邀请 tab | 三路交叉 |
| A4 | **交易账户页**：账户 CRUD（后端端点齐备无 UI）+ API 密钥表（名称/交易所/hint 回显） | 集成中心 · 账户 tab | 站点+代码 |
| A5 | **邮件链路完整面**：SMTP 表单+测试+发件箱（指数退避 6 次/下次尝试/last_error）+通知历史 | 集成中心 · 邮件 tab | 三路交叉 |
| A6 | **IM 授权与扫码**：per-bot 用户授权列表（ou_xxx+角色+增删）+ interactive 扫码 onboarding 向导 | 集成中心 · IM tab 扩 | 站点+代码 |
| A7 | **调度任务四件套**：卡死检测（手动触发）/终止/强制删除（高危确认）/执行日志 50 条 | 数据中心 · 调度任务 tab 细化 | 三路交叉 |
| A8 | **beat 产物落地**：md-hub 维护模式横幅+L3 调和记录（期望表三源/漂移/跳过/退避）+ 磁盘/断连/漂移三任务报告位 | 健康与日志 | 代码+文档 |

## 2. B 类 · 补进 05/06 文档（14 项）

1. 限流**单参覆写 rate_limits + 时段乘数 rate_time_overrides** 编辑 UI（06 B#9 只列了选档）
2. **多策略并行监控看板**（F-MON-001 高优先级：每任务实时 K 线+买卖/止损标注+因子实时值）→ 并入实盘任务详情
3. LLM **主备路由+熔断三态可见** + caller 维度用量归因（9 值）
4. 券商**通道用量**（/api/broker-usage）+ **6h 定时健康探测**标红
5. **因子热重载影响面**：factor:recalc:triggered——因子编辑页"影响 N 个在跑任务"横幅（对齐策略快照隔离提示）
6. **通知语义设置**：日配额 100 超限静默/1min 去重合并/留存清理（确认>7d·全量>30d·历史 1000 条）透出
7. **DB 备份可见性**（每日·保留 7d）+恢复说明 → 设置页一行+帮助
8. 忘记/重置密码页 + 注册条款流（认证侧页面族）
9. **K 线弹窗指标全集**：日/周/月+主图 MA/BOLL/SAR+副图 VOL/MACD/KDJ/RSI/WR/CCI/BIAS/OBV（05 仅"保留 K 线"一句——能力需明示；详情页缺指标与弹窗有能力是两个组件，详情页方案应吸收弹窗能力）
10. **hub_shadow_symbols 影子标的清单**配置项（切流行情的灰度标的）
11. 库表 top_list（龙虎榜）/margin_detail（两融）已同步未消费——详情页 8 Tab 外的候选数据
12. **ICP 备案 Footer**（合规硬项，新布局保留）
13. 止盈止损/移动止盈（F-EXE-001，后端未实现）——执行规则预留位，文档裁定缓
14. 06 未消费清单补全账号域端点族：invite/revoke、avatar、deactivate、change-password、forgot/reset、terms、email-outbox、ack-all、im-bots/onboarding、broker-usage、tasks/detect-stuck·force-delete、sync/all·symbol、llm-budget/check

## 3. C 类 · 记录理由（4 项）

- 死代码清理（startStrategy/stopStrategy 零调用、feishu/* 假端点、FeishuConnect 无路由、stockSearch 死导出）——清理项非设计项
- .env 开发/测试开关（SMTP_DEV、XTP_TEST_*）——运维级不上 Web
- /feishu 独立路由与 im-bots 并存——合并进 IM tab 即可
- 密码强度校验/静态清单同步/通知清理 beat 等实现细节——无需设计位

## 4. 总评

研究链+风控对账两域方案已挖深；真正的盲区是**平台壳层**（语言/账户/个人中心/邮件/邀请条款——约占遗漏半数）与 **admin 长尾**（配置键全集/beat 产物落地/任务运维四件套）。A 类 8 项进原型 v2.2，B 类 14 项随文档与后续迭代。
