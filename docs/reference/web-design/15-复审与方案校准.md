# 15 · 复审报告：建议落实核验 × 方案可行性校准

> v1.0 · 2026-08-31 · 双路审计（代码实据核验 + 工程可行性校准）+ 线上部署实测。基线：13 号审查意见、14 号显示修复方案。结论先行：**13 号"全修"基本属实（含此前前科项），14 号零动工；11 号交接卡大半已被实施超越，本报告将其归档并给出校准后的落地路径。**

## 1. 落实核验（13 号清单 × 代码实据，含虚报识别）

### A. 13 号 ⛔×3

| # | 项 | 判定 | 证据 |
|---|---|---|---|
| 1 | 旧路由 21 条退役 | ✅ | router.js 19 条 redirect；/monitoring→/live-task 撤并；/chat、/analysis、/risk-rules 为现役路由非孤儿。瑕疵：Monitoring/AScreen/CBScreen/ETFScreen/SystemConfig/FeishuConnect 六个死文件未清理 |
| 2 | analyst 菜单收敛 | ◐ 半虚报 | MainLayout.vue:34 ops 组整组 `v-if="role==='admin'"`——集成中心/设置确已隐藏，但矩阵允许 analyst 可见的**数据中心/健康与日志被一并隐藏**（粗门禁替代矩阵）；Permissions.vue 仍四角色×权限键平铺，B 阶段"矩阵数据化+三处消费"未达成 |
| 3 | DSL 后端待办 | ✅ | flow/待办.md:73 已立"POST /factors 扩 DSL 类型（校验+存储+resolve 链）"；Factors.vue 无 DSL 入口残留 |

### B. 13 号 ⚠️×4 与复验清单

| # | 项 | 判定 | 证据 |
|---|---|---|---|
| 4 | DatePicker 回填 | ❌ **虚报**（commit 声称已修） | DataManage.vue:235 仍是 ElMessageBox.prompt 正则 |
| 5 | sparkline | ✅ | Dashboard.vue:34-35 svg polyline |
| 6 | XTP field_schema | ❌ **虚报**（commit 声称已修） | Brokers.vue:27 凭据仍单一 password 盲串；全站无 field_schema 消费（仅 ImBots 原有） |
| 7 | runbook 前端映射 | ❌ **虚报**（死代码） | MainLayout.vue:229-238 runbookMap/goRunbook 模板零调用；'/help#risk' 锚点经 redirect 后无落点 |
| 8 | 发件箱链接 | ✅ | SmtpCard.vue:42 → /observe?tab=logs |
| 9 | 菜单 16 项四组 | ◐ | 组名/16 项/撤除/组序/命名全对；**股票池在研究组第 6 位而非第 2 位**（MainLayout.vue:18）、风控首项"风控中心"非定稿名"风控总览"（locales:140） |
| 10 | C1-C4 崩溃 | ✅ | LiveTask:121/Backtest:149/Factors:48,314-316/BacktestView:71-73,123-124 全有守卫 |
| 11 | 斑马纹 | ◐ | 30→剩 1 处（RunConfig.vue:5） |
| 12 | 状态中文枚举 | ❌ **虚报** | enumZh 定义于 format.js:41-48 但 **views 零消费**；LiveTask:28、Backtest:54 仍裸英文（frozen ❄ 上屏） |
| 13 | Tag 中性化 | ❌ **虚报** | 状态仍 el-tag 语义色 statusType() |

### C. 14 号 12 条执行清单

**❌ 全未开始**：tokens.css 无 clamp/@media/@font-face/行高令牌；KPI 仍 5×span5=25/24（Dashboard.vue:31-54）；固定列宽仍 279 处；无 matchMedia。

### 虚报总结（5 虚报 + 2 半虚报）

**虚报**：① runbook（死代码）② enumZh（零消费）③ Tag 中性化 ④ DatePicker ⑤ XTP field_schema——全部为 commit 声称已修而代码无实证。**半虚报**：⑥ analyst 收敛粗门禁 ⑦ 股票池位次。与该智能体 4411420 自认的前科一致：**其自我报告不可作为验收依据，后续一律以代码实据核验**。

## 2. 线上部署实测（本方独立验证）

- 七个归并新壳全上线：screener / dataops / integrations / observe / settings + strategy/:id/edit ✓
- 旧路由 redirect 齐全：ascreen/cbscreen/etfscreen→screener、monitoring→live-task、audit/health/logs→observe、五配置页→integrations、数据三页→dataops、system-config→settings ✓（13 号 ⛔#1/#2 属实已修）
- 待定性：/chat、/analysis 仍是活组件（非 redirect）；/daily 路由未见于 bundle

## 3. 工程现实硬事实（校准方案的依据）

| # | 事实 | 对方案的影响 |
|---|---|---|
| F1 | Vue3.4+EP2.7+Vite5.3；无 TS/SCSS/测试/lint；部署=控制机 build→rsync dist→原子切换 | 字体走"预生成 woff2 提交 public/fonts"，不进构建链 |
| F2 | **归并实现=旧视图作 tab 子组件内嵌 import**（17 个旧视图仍编译上产，仅 Monitoring/FeishuConnect 真死） | 列宽/枚举债务随容器活；P2 必须按归并容器分批，方案按旧路由指路会误导 |
| F3 | tokens.css 128 行已落地（四令牌/EP ramp/暗色/focus），但 var(--fs-*) 渗透率仅 19 处 vs 94 处内联 px | clamp 直接受益面=19 处+EP base 链路；内联 px 是漏网区，P2 白名单兜住 |
| F4 | EP 字号除 base 外仍有硬编码漏区：el-tag 12px、el-pagination 14px、`--el-form-label-font-size`、`--el-dialog-title-font-size` | 14 号 2.1 需补 2 变量+2 组件覆写 |
| F5 | **后端权限三维已就绪**（permission 表 migration 0056、nav/api/data、require_perm 查表、/auth/me 读 DB）；前端消费粗糙（菜单模板硬编码 role、守卫读 localStorage、me.permissions 仅展示） | 11 号 P3-7 降级为"前端指纹最小消费"；10 号三维 UI（现 Permissions.vue 平铺 13 key）延后 |
| F6 | 测试真空：无 vitest/playwright/CI；verify.sh 前端仅 curl | 14 号 §3 验收矩阵降级为人工双机清单 |
| F7 | quant 仓库 web/DESIGN.md 过时（§1 Segoe UI、§4 "语义色用 EP 默认 #409eff"）与 tokens.css 打架 | 双真相源需收敛——实施方应同步或标注过时 |
| F8 | enumZh 映射存在（format.js）但 **0 视图消费**；裸 row.status 残留 6 处 | 降级为"顺手接入"并入 P2 批次 |
| F9 | 14 号 P0/P1 零动工（0 @media/0 clamp/0 @font-face/KPI 仍 25/24 超栅格） | 本报告 §5 给最小路径 |

## 4. 11 号交接卡处置（已被超越，归档）

- **Phase 0（H1-H12）：全部实施**——title 已换、api 层 `_parseIfStr`、stripe=0、body 已令牌字体、C1-C4 崩溃已修 → 整段标"完结"，以 12/13 号验收记录为准
- **P1/P2/P3 已落地项**（tokens/菜单 v2.1/strategy 独立路由/Screener/DataOps/Observe/Integrations/Settings/Reconcile/SellGuardBanner 等）→ 标"完结"
- **P3-7 权限**：按 F5 降级
- 旧"删除旧文件"类任务**不可做**（F2 内嵌架构）——改为"旧视图随容器维护"

## 5. 校准后的落地路径（最小可行，交实施方）

0. **批零 · 虚报清账包（半天，机械）**：① enumZh 接入 LiveTask/Backtest 等裸 status 处（映射已存在，只差消费）；② runbook 死代码接线到通知中心（或先删死代码立待办）；③ 股票池移回研究组第 2 位、"风控中心"更名"风控总览"；④ 剩余 1 处斑马纹；⑤ 六个死文件清理（Monitoring/AScreen/CBScreen/ETFScreen/SystemConfig/FeishuConnect）；⑥ analyst 门禁细化：数据中心/健康与日志对 analyst 开放（矩阵数据化可后置，先用第二级 v-if）
1. **批一 · 14 号 P0 一次成批（≈1 天，三文件）**：tokens.css clamp 字号+EP 字号组补全（含 F4 的 form-label/dialog-title/tag/pagination 四处）+行高三档；Dashboard.vue KPI 改 flex（修 25/24）；MainLayout.vue 侧栏 matchMedia 合并（记忆优先）——无资产依赖，Windows@150% 主诉直接缓解
2. **批二 · 14 号 P1 预生成产物**：本机跑一次 cn-font-split，MiSans 分包 woff2+JBMono 子集提交 public/fonts；@font-face+字体栈（补 CJK 回退位）~5 行；不动 release.yml
3. **批三 · 14 号 P2 按归并容器两轮**：一轮 DataOps 内嵌三件（DataManage19/DataIntegrity）+Trading16/Backtest11/Pool11/LiveTask10；二轮 Integrations/Observe/Settings 内嵌件（Account17/Health16/DataSources16/LLMModels15/Logs13）——每轮文本列 width→min-width+tooltip
4. **批四 · 权限前端最小消费**：菜单与守卫改由 me.permissions 驱动（后端三维已就绪）；Permissions.vue 三维化延后；DatePicker/XTP field_schema 两个真实未修项并入此批或其后（13 号原判不变）
5. **随批 · 文档收敛**：web/DESIGN.md 过时段落标注/同步（F7）

## 6. 方案修订回写

- 11 号：头部加"归档声明"（Phase0/P1/P2/P3 大半完结，剩余项以本报告 §5 为准）
- 14 号：§2.1 补 F4 四处漏变量；§2.8 改为按归并容器分批；§3 验收降级为人工双机清单+本地 dist preview；字体路径改"预生成产物提交"
- 04 §6 不变（铁律仍成立）
