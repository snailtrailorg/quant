# 17 · 复验记录：五项清尾（16 号 §3 遗留）

> v1.0 · 2026-09-01 · 方法：线上 CSS/断点数值级比对 + 源码逐项实据（16 号 §4 规矩）。对象：d135985/ad23ce7/20676e4/8f5afde，prod 202609012215-8f5afde。

| # | 项 | 判定 | 证据 |
|---|---|---|---|
| 1 | clamp 根修 | ✅ **数值级通过** | 线上 index-05ZezFve.css 实测：`--fs-body: clamp(12.5px, calc(9.5px + .234vw), 14px)` 三特征齐全逐字合规；六档全挂 body（kpi ×1.92/page ×1.38/card ×1/label ×.92/foot ×.87）；EP 四变量（base/small/extra-small/large）全挂令牌 |
| 2 | Dashboard 响应式 | ✅ | 二排 :md=15/9、底排 4×(:xs24/:sm12/:md6)；.kpi-cell `flex:1 1 0; min-width:0` + klabel/kpi-num 三防（overflow/ellipsis/nowrap）；三断点 1279（KPI 3+2 换行）/991/767（单列）——降级策略合理 |
| 3 | DatePicker | ✅（第三次点名终落地） | DataManage:101 el-date-picker + value-format=YYYYMMDD 对齐 engine 契约，组件级校验天然拦非法日期 |
| 4 | 批三两轮 | ✅ 质量 / ◐ 对账 | 抽样 Trading.vue 三分类执行正确（symbol 文本列 min-width+tooltip；direction/volume/frozen/cost/lastPrice 数字列固定宽+`class-name="num"`）；min-width 23→29、tooltip 文件 16→37。**但计数口径三方不一致**（我 15 号 279 / 它 131→126 / 本轮我精确正则 227）——见下 |
| 5 | DESIGN.md 标注 | ✅ 超规范 | 不止加"已取代"警示，还做了旧文→令牌的映射表 |

**主动判断裁定**：`--fs-card: calc(var(--fs-body) * 1)` vs 14 号原文 `var(--fs-body)`——数学等价，minifier 化简为 `* 1` 属正常产物。接受照抄工作单的处理。

## 遗留一条：列宽计数口径未统一（下轮对账前提）

三方数字不可比：15 号 279（含 min-width 子串误吞）/ 实施方 131→126（口径未注明）/ 本轮精确正则（el-table-column 非减号前缀 width="数字"）= 227。
**定义标准口径**（18 号修订：范围必须含 `src/views/*.vue` **与 `src/components/*.vue`**——v1.0 漏 components 致基线 227≠232，差 5=RunConfig.vue；grep 单行匹配 el-table-column，`-E` 别漏否则 `+` 不生效）：
1. 固定列：`el-table-column` 行内 `[^-]width="[0-9]+"`
2. 弹性列：`min-width="[0-9]+"`
3. 截断列：`show-overflow-tooltip`（出现次数，非文件数）
目标态不是把固定列清零——数字/代码/时间/操作列**保留固定宽**是规范（等宽字体下可预期），文本列归零弹性化才是终点。

## 本轮评价

五项全部真实落地，且实施方把 16 号 §4 的数值级自查方法内化成了标准动作（commit 自带 dist+线上双验输出）、提交边界卫生（三/四批混合时先回退再分批提交）——协作机制在收敛。**跨平台显示修复方案（14 号）至此 P0/P1/P2 主体完结**，剩批三尾量按新口径续清。
