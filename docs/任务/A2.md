# A2 · 三档收尾项 18：监控接新表（data_continuity / 告警 / metrics）

> （P3 回写 2026-08-20 建）本文件是 `flow/规范/任务模板.md` 末尾引用的正式样本（原死链）。
> 模板 §四 内嵌的 "A2=#30 数据质量校验" 试写示例已完成（#30 ✅），仅存于模板文档；
> 本文件按模板以**真实未做待办**（17 号 §7 项 18，`flow/待办.md` 当前焦点 0 剩项之一）重造。

### A2 监控接新表（17 号 §7 项 18）
- **目标**：把三档 19 张新表（一档 9 + 二档 10）纳入监控面——`data_continuity_check` 新鲜度检测 + `notify` 告警 + `/metrics` 指标，新表断供 24h 内可见（现状：监控只盯 bar_1D，19 表静默失明）。
- **依赖（就绪）**：
  - 迁移 0045（一档 9 表）/0046（二档 10 表）✅（表已在 schema_expectations 基线）
  - sync_log 必写日志护栏 ✅（tier1 9 sync_id 每轮落 sync_log；pool_data FATAL/skipped 也落）
  - `data_continuity_check` 任务在跑（beat 1h）✅；`health_monitor.collect/render_prometheus` 在跑（beat 30s）✅
- **产出**：
  - `server/src/scheduler/tasks.py`：新增 `_check_tier_freshness() -> list[dict]`，`data_continuity_check` 内调用（结果并入 issues + 断供超阈值调 `notify`）
  - `server/src/health_monitor/collector.py`：`collect()` 快照加 `tier_freshness` 块；`render_prometheus` 加 `quant_tier_table_staleness_hours{sync_id=...}` gauge
  - 新建 `server/tests/test_tier_freshness.py`（≥4 例：新鲜/陈旧一档/二档游标冻结/异常不抛）
- **限定范围**：只改 `scheduler/tasks.py` + `health_monitor/collector.py` + 新建 `tests/test_tier_freshness.py`；**不碰** `data_sync/engine.py`/`pool_data.py`/迁移/beat 表。
- **接口契约**：
  - 新加（scheduler/tasks.py）：
    ```python
    def _check_tier_freshness() -> list[dict]
    # 读 sync_log：一档按 9 个 sync_id（stk_limit_sync/.../concept_sync，见接口契约 §三档同步契约）
    #   每个取最新 status='success' 行的 ts；二档按 sync_id='pool_data' 最新 status='done' 行
    #   + pool_data_cursor 表各表 cursor 日期。超时阈值：一档 48h（盘后族日频）、二档 8 天（财务季频）
    # 返回 [{"sync_id": str, "last_ts": datetime|None, "age_hours": float|None, "kind": "tier1"|"tier2"}]（仅超阈值条目）
    ```
  - 新加（health_monitor/collector.py）：`collect()` 返回 dict 增键 `tier_freshness: list[dict]`（同上结构）；`render_prometheus(snap)` 追加 gauge 行（无数据块时省略该指标，不抛）。
  - 现有调用（签名 + 位置）：
    - `data_continuity_check()`（tasks.py:330，`@app.task(name="src.scheduler.tasks.data_continuity_check")`，beat 1h queue=risk）
    - `notify(level: Level, category: Category, title: str, body: str = "", source_ref: str | None = None) -> int | None`（alert_notify/notify.py:55；落 PG+外部推送，失败不抛）
    - `collect(now: float | None = None) -> dict`（collector.py:62）/ `render_prometheus(snap: dict) -> str`（collector.py:121）
    - `get_conn()`（data_platform/db.py，`with` 退出还池）
  - 引用：接口契约 §三档数据同步契约（tier1 9 sync_id 清单 + `pool_data.sync_pools_data` 返回/日志语义）
- **验收标准**：
  - `pytest server/tests/test_tier_freshness.py -q`（新鲜不报/一档 48h 陈旧报/二档游标 8 天冻结报/`get_conn` 异常返回空不抛，4 例过）
  - `python -c "from src.scheduler.tasks import _check_tier_freshness; from src.health_monitor.collector import collect, render_prometheus; print('OK')"`
  - `pytest server/tests/ -q` 全套不回归
  - 本地起服务后 `curl -s localhost:8000/metrics | grep quant_tier_table_staleness` 出指标行（表新鲜时值为 0 或缺省）
- **mock 方式**：
  - `patch("src.scheduler.tasks.get_conn", return_value=mock_conn)`，`mock_conn.execute.return_value.fetchall.return_value = [("stk_limit_sync", datetime(2026,8,20,17,5),)]` → 新鲜不报；把 ts 改 3 天前 → 报
  - 二档：`fetchall.return_value = [("pool_data", None,)]` + 游标表空 → 报"从未同步"
  - `patch("src.scheduler.tasks.notify", return_value=None)` 断言陈旧时被调、新鲜时不调
  - 纯 mock 不连真实 PG/Valkey（对齐任务模板 §二 mock 规则：fake 行对齐 sync_log 真实列名）
- **参考文档**（≤2 份）：
  - `docs/architecture/17-三档数据与详情页.md` §3-§4（一档 9 表/二档 10 表与 sync_id 语义）+ §7 项 18
  - `docs/architecture/模块契约/health_monitor.md`（collect/render_prometheus/告警路由）
