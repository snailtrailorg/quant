# P2-1 · XTP 查询等待超时可观测（等待原语审计真缺陷修复）

> 立项：2026-08-27 深夜等待原语审计（33 处分诊）。步 2 双盲（A/B 双同"需修后进"无 P0）
> 修订：①撤 is_alive 方案（Thread.start() 返回即置位，零信息）改 wrapper entered Event；
> ②测试去线程化（预塞/side_effect 同步法）+稳定窗常量化防同频 flake；③空仓零回报不告警
> （trading.py:105 是 60s 循环，空仓合法常态，warning 会每分钟刷屏）。

- **目标**：XTP 查询等待的超时从"静默落空拿部分当全量"改为可观测（布尔返回+分级告警+部分快照标注）。
- **依赖（就绪）**：`adapters.py` `_wait_update`（:251，staticmethod）/`query_position` 稳定窗（:210-218）；调用方全仓核实：唯一生产调用 `query_account`（:239，忽略返回值）+`test_position_snapshot.py:93` patch 桩（O-S1 后为死桩，均兼容）。
- **产出**：
  1. `server/src/strategy_framework/adapters.py`：
     - `_wait_update(cache, before, timeout=2.0) -> bool`：True=有更新；False=超时且已 `logger.warning`（带超时秒数+缓存项数）
     - 稳定窗秒数提为模块常量 `POSITION_STABLE_WINDOW_S = 2.0`（测试可 patch）；`query_position` 超时分支分级：**行数>0 未稳定→warning"部分快照"（带行数）；行数==0→info"无回报（空仓或无响应）"**（空仓合法常态不刷屏）
     - 顺手：:204 注释残留（O-S1 前语义提 `_wait_update`）修正
  2. `server/tests/test_xtp_adapter_query.py` 新建 5 测（见 mock 方式）
- **限定范围**：只改 `adapters.py` 上述两段+常量+注释；**不碰** send_order/connect；**不做** Condition 事件化（O-S1 陷阱）。测试仅新建 1 文件。
- **接口契约**：
  - 改签名：`_wait_update(cache: dict, before: set, timeout: float = 2.0) -> bool`（原 None）
  - 新常量：`POSITION_STABLE_WINDOW_S: float = 2.0`（模块级）
  - 不变：`query_position() -> list[Position]`
- **验收标准**：
  - `pytest tests/test_xtp_adapter_query.py -q` → 5 passed
  - 全量 `pytest tests/ -q` 绿（693+5=698）；pyflakes 改动文件零新增
  - 收工回写：模块契约 `strategy_framework.md:219` 的 `_wait_update` 签名
- **mock 方式**（`__new__` 绕构造，先例=test_position_snapshot.py；`_positions` 行对象用 SimpleNamespace：`ns(vt_symbol="600000.SSE", volume=100, price=9.05, pnl=0.0, direction=SimpleNamespace(value="long"), frozen=0, yd_volume=0)`）：
  - `_wait_update` True：**零线程**——cache 预塞 `{"x":1}`、`before=set()` → 首拍立即 True
  - `_wait_update` False：空 cache + `timeout=0.3` → False + caplog 有"超时"
  - `query_position` 稳定：**零线程**——`_gateway=MagicMock()` 其 `query_position.side_effect` 同步往 `adapter._positions` 塞 2 行（clear 后首拍前执行）→ 两拍键集相同 → 稳定返回 2 行无 warning
  - `query_position` 部分快照：patch `POSITION_STABLE_WINDOW_S=0.4`，后台线程每 **0.05s**（≥2 倍轮询拍 0.1s，防同频 flake）加一行持续 0.6s → 0.4s 窗内不稳定 → 返回部分行 + caplog"部分快照"（warning 级断言）
  - `query_position` 空回报：不塞任何行 + patch 窗=0.2s → 返回 `[]` + caplog **无 warning**（info 不断言级别）
- **参考文档**：`docs/architecture/模块契约/strategy_framework.md` §adapters（:16/:115/:219）
