# docs/任务/ — 自包含任务文件目录

> （P3 回写 2026-08-20 新建：本目录此前不存在，`flow/规范/任务模板.md` 引用的 `docs/任务/A2.md` 为死链——2026-08-20 双盲审计 P3 项）

- **用途**：每个待办一个 `<id>.md` 独立文件（8 字段模板），做任务时只读「任务文件 + `docs/architecture/接口契约.md` + 本模块契约」即可动手，零代码阅读。
- **写法规范**：`flow/规范/任务模板.md`（8 字段 + mock 模式库；硬约束：限定范围 ≤3 文件、参考 ≤2 份）。
- **索引**：`flow/待办.md` 是状态单一真相源，任务文件只写"怎么做"，不重复列状态。
- **样本**：`A2.md`（17 号 §7 项 18 监控接入）。
- **批次任务**：`批1-SDK状态机…` 至 `批6-收口…`（运行时重构六批）；`限流治理吸收.md`/`P2-1`/`P2-2`（P2 整改系列，八步法完整走通的范式样本）。
