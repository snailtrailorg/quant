# W5 权限收官:双轨统一+数据执行面+nav 守卫+虚拟滚动(2026-09-01;完美系统战役第五批)

> 承 W4(双轨缺口半修/数据执行面归本批/readonly 守卫归本批)。D 阶段中**账户域多人归属**显式缓做(见"不做的")。

## 产出 1:require_role → require_perm 统一迁移(100 处)

- **映射表**(双盲修订版;read 门 68→60):
  - **身份豁免 8 处保留 require_role(盲审 B-P0——迁移=deny-read 用户锁死登出/改密/注销)**:/auth/me、/auth/logout、profile GET/POST、avatar、deactivate、change-password
  - `require_role("viewer","analyst","trader","admin")`(余 58 处)→ `require_perm("read")`;**写混入改判(盲审 A-P1)**:/api/chat·/api/log/analyze→strategy_control(viewer 不白烧 LLM 预算);ack-all/budget-check 轻写维持 read(声明)
  - `require_role("admin")`(23 处)→ 按域:**mgmt 数据源/通道/券商/points-tier/rate-limit(11)→system_config;force-delete·detect-stuck(2 运维)+health components/events(2)→system_config;permissions 管理 3→user_mgmt(锁键地板);llm 模型/预算(含 chat:276)→llm_config;im_bots→im_bots_config**;**已存 require_perm 不动**(SMTP 族现 user_mgmt,勿翻转)
  - `require_role("analyst","trader","admin")`(6)→ `require_perm("strategy_control")`(GET runbook 随批迁,跨域耦合记录)
  - `require_role("trader","admin")`(3)→ **reconcile verify/ignore(2)→trade(处置权=交易员,零漂移)**;halt 急停→halt
- **鉴权语义变化声明**:迁移后经 permission 表(表空回退字典=零漂移);require_role 仅存身份豁免 8 处
- 回归面:**全端点矩阵测**(TestClient+patch verify_jwt 遍历 app.routes——模式 test_put_to_post.py;conftest 补 app fixture)替代抽验

## 产出 2:data 维执行面脱敏

- **文件**:`src/web_api/routes/risk.py`(risk_log)+`trading.py`(position/orders)
- 新 helper `data_sensitivity(username)`(auth.py):user deny>user allow>role(同 effective;data 维)→ 'detail'|'aggregated'|'count'(缺省 detail=现行为零变化)
- `/api/risk/log`:响应增 `sensitivity` 判别键(盲审 A-P2);count → {items:[], count, first_ts, last_ts};aggregated → 按 action 聚合计数
- `/api/position`+`/api/orders`:count → {count, total_value}(账户级保留=设计判断);aggregated → 聚合总市值/总盈亏,**剔 symbol/strategy_id/order id(盲审 B-P1 聚合键)**
- **脱敏旁路集(盲审 B-P1)**:/api/reconcile/issues(按 symbol 返数量=持仓明细)·/api/reconcile 同三态;/api/pnl·/api/dashboard 本身聚合态——count 档维持+声明(账户级总览=设计判断)
- 前端:持仓/风控日志/对账页按 sensitivity 字段分支渲染(空态"权限受限,显示计数")

## 产出 3:nav 维消费(readonly 守卫)

- `/auth/me` 增 `nav`:本用户 nav 三态映射(user deny>role)
- 守卫时序方案(盲审 A-P1):**模块级 me promise 缓存**(router.js 守卫首航 await 它;MainLayout 载入前守卫先行不丢 nav)
- `router.beforeEach`:nav[route]=='hidden'→拒(redirect 首页+提示);'readonly'→放行+provide readonly 标志
- **readonly 接入面(盲审 B-P2 声明边界)**:readonly=**UI 提示层**(写按钮灰——重点页 Strategy/LiveTask/Pool/BacktestRun/RiskRules/DataOps),执行面=api 维(服务端拒);nav 维非安全边界(直连 API 不受 nav 限=设计,与 api 维职责划分)
- readwrite/无配置→现行为

## 产出 4:el-table-v2 虚拟滚动(#2b)

- **对象重定向(盲审 B-P1:Logs 服务端 LIMIT 200/Audit LIMIT 100 已裁剪,v2 过度工程)**:**风控日志页(risk_log ≤1000)+对账处置台(reconcile_issues ≤500)**
- el-table-v2(EP 内置);**v2 硬约束(盲审 A-P2)**:height 必填+每列 width 必填+#default→cellRenderer(i18n 入 JS);其余页维持原裁定

## 不做的
- **账户域多人归属**(D 阶段原案):当前系统单操作者+admin,多账户归属无消费场景——数据模型(account.owner)+执行面全链 speculative;触发条件=出现第二个交易员账号。显式缓做记录 decisions
- 虚拟滚动铺全站(其余页有分页/上限,原"数据量<1000 不紧迫"裁定对其余页维持)

## 验收
1. pytest 全绿(+迁移基线测:每映射类抽 2 端点断言鉴权;脱敏三态测;nav 守卫 helper 测)
2. 本地:deny trader 用户 data sensitivity=count → risk_log 返回计数形态;position 计数;撤恢复
3. 前端:nav hidden 路由拒+readonly 写按钮灰;Logs/Audit 虚拟滚动渲染 500 行流畅(build+手查)
4. smoke+build 绿

## 风险
- 100 处迁移的漏改/错映射:grep 清单驱动+基线测抽验;require_role→require_perm 行为差异(表空时回退字典=同角色门,零漂移)
- 脱敏聚合的口径:aggregated 持仓聚合=Σ市值/Σ盈亏(浮点);count=行数——前端两形态兼容
- 路由守卫与现存 meta.admin 守卫(路由表)叠加:meta.admin 保留(双保险),nav 维是细化不是替换
