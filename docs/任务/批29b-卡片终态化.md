# 批29b · 卡片终态化

> 2026-09-16 立项。起因：批29 上产后用户真机验收通过（27-4 全链路闭环），但发现 UX 尾巴——点击取消/确认后按钮短暂 disable 又复活可反复点。用户指令："点击后把卡片更新成终态——这个要修"。

## 一、根因

- 回调（CARD 帧→patch→_card_gates）处理完 **ACK 200 空体**，卡片本体不更新；卡片 `update_multi: true` 允许多次交互 → 按钮回到可点态。
- 功能安全（60s 时效+event_id 去重+exec 卡级去重三闸兜底，同卡只执行一次），但两个真洞：**①点过取消后仍可点确认且真会执行**（取消在服务端无记忆）；**②60s 过期后点击零反馈**（闸门静默拒，用户不知道为什么没反应）。

## 二、设计

**任一被处理的点击 → 卡片进入终态**（PATCH `/open-apis/im/v1/messages/{mid}` 原地更新：摘按钮+状态文案）。选 PATCH 而非回调响应内联更新：不依赖 SDK ws 响应管线（我们的 on_card 返回 None=ACK 空体，改造回传链路风险高）；mid（open_message_id）已在回调上下文提取（exec 去重键主成分），现成。

| 点击场景 | 现行为 | 批29b 行为 |
|---|---|---|
| confirm·闸门全过 | 执行+文本回执"✅ 已执行熔断" | 同左 **+ 卡片终态"已执行"**（文本回执保留=已测语义不动） |
| cancel | 静默 no-op（闸门开头 return） | **卡片终态"已取消"**（不执行） |
| 时效外（>60s/无 ts） | 静默拒（仅日志） | **卡片终态"已过期"** |
| 身份解析失败 | 静默拒 | **卡片终态"账号不可用"** |
| 权限键不足 | 静默拒 | **卡片终态"权限不足"** |
| exec 去重命中（重复点确认） | 文本"已经执行过" | 同左 **+ 卡片终态"已执行"**（幂等重刷） |
| mid 缺失（理论回退态） | — | 不尝试更新（update 跳过，闸门行为不变——fail-soft） |

**PATCH 失败 fail-soft**：update_card 返 False 仅 warning 日志，不影响执行链（去重闸仍兜底复点）；真机若 4xx（权限 scope 不足）→ 用户复测即暴露。

## 三、子项

### 29b-1 `FeishuClient.update_card(message_id, card) -> bool`
- `src/im_bot/feishu_client.py`，httpx.PATCH 同构 send_card 先例（token/超时 10s/响应校验 code==0 落 warning 返 bool）
- URL：`https://open.feishu.cn/open-apis/im/v1/messages/{message_id}`，body `{"content": json.dumps(card)}`（SDK PatchMessageRequestBody 实证 content:str）

### 29b-2 `build_terminal_card(tool_name, status, args=None, note="") -> dict`
- 同文件，CardKit 2.0 结构（schema/config/header/body 与 build_confirm_card 同构）——**无按钮**，body 单 markdown 行
- status ∈ executed/cancelled/expired/denied/unavailable；header template：executed=green 余=grey；文案过文案师（§六）
- build_confirm_card 不动（发卡面零变更）

### 29b-3 `_card_gates` 接线
- `src/feishu_bot/ws_client.py`：mid 已入参；各拒答/执行分支后调 `get_feishu_client(fid).update_card(mid, build_terminal_card(...))`（mid 空则跳过）
- cancel 分支从"开头 return"改为"终态化后 return"；exec 成功分支在 execute_confirmed_tool 后终态化
- 闸门顺序/去重/权限判定零变更

## 四、限定范围

只改：`src/im_bot/feishu_client.py`、`src/feishu_bot/ws_client.py`、`tests/test_batch27_card_ws.py`（≤3 文件）。
不碰：build_confirm_card（发卡）、handle_incoming、router 桩、HTTP 面、前端、DB。

## 五、验收标准

- `pytest tests/test_batch27_card_ws.py -q` 全绿（26+新增 ~6 = 32±1）
- `pytest tests/ -q` 全绿（1096+新增，按实际报）
- 部署后真机：点取消→卡片变"已取消"无按钮；点确认→执行+卡片变"已执行"；过期点击→卡片变"已过期"

## 六、mock 方式

- httpx.PATCH：patch `src.im_bot.feishu_client.httpx.patch` 返回 SimpleNamespace(status_code=200) + `.json()` 返 {"code":0}
- 闸门测试沿用 _gates_env/_run_gates（fc MagicMock 自带 update_card 属性，断言 call_args 的 mid 与终态 status）
- mid 缺失负例：_run_gates(mid="") → fc.update_card.not_called

## 七、参考

- 模块契约 im_bot.md/feishu_bot.md（随改）；本文件 §一§二
- SDK 实证：lark_oapi.api.im.v1.model.PatchMessageRequestBody(content: str)；feishu_client.send_card httpx 先例 :138

## 八、文案（待文案师终审）

- executed：标题"✅ 已执行"｜正文"操作 {tool} 已按你的确认执行。"
- cancelled：标题"已取消"｜正文"你点了取消，这次操作没有执行。"
- expired：标题"已过期"｜正文"这张确认卡已过有效期（60 秒），操作没有执行。需要的话请重新发起。"
- denied：标题"未执行"｜正文"你的账号没有这项操作的权限。"
- unavailable：标题"未执行"｜正文"你的账号当前不可用，操作没有执行。"

## 修订记录
- v1（2026-09-16）：初版
