# 批33a · 权限单源化（user 维退役）（2026-09-16 立项/2026-09-17 开工，勘察报告全量吸收）

## 背景与裁定

用户裁定（2026-09-16）：**权限只留用户组维度**——用户权限完全追随组，新组合=建新组（消除 user/组覆盖优先级复杂度）；生产复核 `subject_type='user'` 行=0（2026-09-17）→ 裁定 B「直接清」生效。锁键 admin 地板**保留组层**。33b（资源注册表化）另批。

## 核心策略：签名保留（勘察风险点 1 的杠杆）

`load_effective_permissions`/`market_op_allowed` **保留函数签名与返回形状**（effective/perm_sources 全 role-base/denied=[]）→ 5 个测试文件的 patch、IM 身份链、risk 卡口、profile 五键**零改动**；激进删参数则波及 13+ 点。玻璃盒 perm_sources 后端保留恒 role-base 一版，前端零逻辑改动只删三态视觉。

## 改动面（勘察 18 项归组）

### 33a-1 迁移 0082（号已钉死：34=0081 先编码、33a=0082 随后；down_revision=当时已合 head，禁指向本地未合迁移）

保险 `DELETE FROM permission WHERE subject_type='user'`（幂等，生产已零行）+ `CHECK (subject_type='role')`。**单步式直接 ADD**（表仅几十行微秒级排他锁窗口归零，盲审 A-P2-5 择 a；NOT VALID 两步式防锁收益为零反留 VALIDATE 窗自愈复杂度）。列保留（删列需重建 UNIQUE 索引+破坏 downgrade 语义）；downgrade=DROP CHECK 对称（不恢复 user 行——本就为空；如需恢复写路径须手动 downgrade，回滚面注记，盲审 B-P2-3）。**effect='deny' 列不动**（role 层 market_op 仍在用）。迁移先行窄窗说明：管道阶段 5 alembic 先于阶段 6 flip，旧代码 update_user_override（全仓唯一 user 写点 :334）窗内仍活，撞 CHECK=500 失败模式安全；部署后预检 SQL：staging+prod 各跑 `SELECT count(*) FROM permission WHERE subject_type='user'`（批27-30 先例）。

### 33a-2 perms.py 简化

- `load_effective_permissions` :158-185：user 分支 :169-178 整删；合并序塌缩为 role 单源；**返回形状不变**（perm_sources 全 role-base、denied=[]）
- `_load_user_api_overrides` :122-132 整删；`load_nav_map` :150-152 user 覆盖循环删；`market_op_allowed` :42-75 收窄 role 单层（:60 查询/:67-72 _layer/:74 user or role——签名保留）；`invalidate_perm_cache` :119 users 槽删
- 锁键常量 :34-35（LOCKED_PERM_KEYS/ADMIN_ROLE_FLOOR）不动

### 33a-3 auth_routes.py

- `POST /api/permissions/user/{username}` :285-340 **整端点退役**（路由删=404；锁键拒/SELF_LOCK_RISK/写/审计码全随删——**SELF_LOCK_RISK 退役=封死唯一绕过面，防线增强**，勘察风险点 6）
- `GET /api/permissions` :181-190 user_overrides 查询块+:198 返回键删
- `me` :113 perm_sources/denied 保留恒空态（前端零改动）；组层锁键/admin 地板 :228-237 不动；组 rename/delete 级联（:857,865,901）role 层保留

### 33a-4 前端

- `Permissions.vue` 整删（连带 `Settings.vue:20` import 清——build 门兜底）；`Settings.vue` perm tab 删（:10,:28 admin tabs=[run,perm,alerts]→[run,alerts]）
- `router.js:38` /permissions → **redirect `/users?tab=groups`**（TabsShell queryKey 'tab' 已验证同步）；`?tab=perm` 深链落点：字符串 redirect 整替 query 落 groups 页签；`/settings?tab=perm` 由 TabsShell 静默回落 run 页签（无 404，预期落点防验收误判，盲审 A-P2-7）
- `MainLayout.vue:176` navReadonly 'permissions'→'users' 映射行删（redirect 先行无组件再以 /permissions 渲染，8 个 inject 视图语义不变——盲审 A 核实 7）；`Profile.vue` **删橙态**：permCls 的 perm-ovr 分支（:452/:464）+legendOrange 词条（:266）+.perm-ovr 样式（:825）——**perm-denied 类保留**（market_op 拒绝态 :285 继续用，盲审 A-P2-4）；数据管道恒空态保留
- locales 死键清（盲审 A-P2-3/B-P2-1 合并）：modeUser/pickUser/overrideNote/addOverride/clear/dim/resource/effect（zh ~:1181-1191/en ~:2392-2402）+**孤儿键** perm.title/perm.note/perm.modeRole（Permissions.vue 专属）+settings.tabs.perm+nav.permissions（存量死键顺手清）；PermMatrix 共用词条与 permGroups.js 不动（与待删键零交集已核实）
- `PermMatrix.vue`（纯 role 三维）与 `UserManagement.vue` 组弹窗**零改动**（后者=保留唯一入口）

### 33a-5 测试

`test_permissions_w4.py` 14 例：TestEffective 4 例改写（role 单源断言）/TestOverrideCrud 3 例删；TestBaseline/TestLockedKeys/TestAuthMatrixW5 保留（GET 端点语义不变）。`test_market_op.py` 18 例：user 层 2 例（:43-48,:50-55）改写。**其余 6 文件零改动**（签名保留红利——batch11b/batch19/batch20_profile/im_owner_11c/sb_risk/sc_orders 全部 patch 整函数或 dict.update 补键，盲审 A-P2-1/B 正面清单逐一核实）。动例口径：**改 6+删 3**（1118→1115）。

### 33a-6 文档回写

CLAUDE.md RBAC 段（:50——user 维/优先级链/双路径/对照页描述全改）；模块契约 web_api.md（:11,51-52,122,127,217,239-241——:239-241 顺手修"PERMISSIONS 字典"过时指称）；08-web-admin.md:129-141,194；web-design/10 号历史设计仅批注不重写。

### 33a-2 补充（盲审 P2 吸收）

- `perms.py:28` `_PERM_CACHE` 字面量 `users:{}` 键与 :119 invalidate 同删（两处，盲审 A-P2-6）
- 路径全称：`server/src/web_api/routes/auth_routes.py`（方案原文简写）

## 验收

- pytest 全量绿（动 9 例=改 6+删 3，总数 1118→1115）+pyflakes 零新增+分层绿（test_layering）——八步法标配（盲审 B-P2-4）
- 迁移 up/down 本地双向重放；grep 守门：`subject_type.*user` server/src 零残留（迁移除外）；`modeUser|pickUser|addOverride|perm\.modeRole|settings\.tabs\.perm` web/src 零残留
- build+双守门绿+smoke-web.sh；`/permissions` 深链 redirect 实测落 `/users?tab=groups`
- 用户验收：设置页无 perm tab/组弹窗矩阵正常/个人中心权限 tab 恒 role-base

## 流程

~~方案双盲审~~ ✅（A 零 P0/P1+P2×7 / B P1×1+P2×6 全吸收——撞号钉死 33a=0082、词条补列、迁移单步式、验收口径修正）→ 编码 → 代码双盲审 → 单测 → 提交 → staging 彩排（迁移随版+预检 SQL）→ prod（部署窗）→ 33b 另批。
