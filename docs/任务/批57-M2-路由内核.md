# 批 57：M2 路由内核（RoutingTable+resolve）

> 29 号 §五=主规格。K 线试点接 resolve，其余品类 58 批切。

## 目标
resolve 五步全链（硬过滤/软排序/bulkhead 闸门/审计采样/供给消费分叉）+dry-run 试算器操作面。

## 依赖（就绪）
- 批 56a/56b ✅（类型+SM+market_hours——resolve 消费 covers/sessions）
- 前置：SMClient 三表合并回填可查（56a 验收①）

## 产出
routing_policy/routing_decision 两表（0096——号随实际链顺延）+data_platform/routing.py（RoutingTable/CandidateChain.fetch 链式下移五步/bulkhead Valkey 计数/memoization 双键/热更新订阅+周期对账兜底）+路由策略页（weights 编辑+候选链试算器）+词条（文案师）+err 码 pairing。

## 限定范围
不实现 adapter fetch（只选不拉）；不管流/交易。K 线同步试点：_get_kline_adapter 路径并行接 resolve（feature flag）。

## 接口契约
resolve(DataRequest)->CandidateChain；Candidate{adapter,account,is_local}；HealthSource.of(adapter)->HealthState。关键立法：local_pg 豁免权限（c.is_local or perms.allows）；审计 cause 四枚举；供给模式排除 local_pg。

## 验收标准
①dry-run bar_daily+600000+backtest→[local_pg,tushare①] ②position 改动下 epoch 生效 ③熔断账号沉底（注入） ④审计含 failover/skip_busy 两类 ⑤供给模式链无 local_pg ⑥审计 req_summary 可读。pytest 新钉+dev 真库实测。

## mock 方式
_FakeConn 构造账号行；Valkey 计数 mock redis；dry-run 端点 TestClient。

## 参考文档
29 号 §五；28 号 §六。
