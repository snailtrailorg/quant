# 批 58b：竞价条并入首根（独立数据变更批——dataset_version+1）

> 29 号 §六验收 2。与 58 行为等价收编严格分离（数据语义变更走版本化路径）。

## 目标
Tushare 分钟链启用竞价条并入（量/额累加+open 取竞价条+high/low 与竞价价取 max/min）+存量分钟 merge 脚本一次性迁移。

## 依赖（就绪）
- 批 58 ✅（收编完成，preserve 开关在位）

## 产出
preserve_current_normalization 切 False+bar_minute dataset_version+1+存量 merge 脚本（按日批量 09:30 条并入 09:31）+契约测试新钉（首根含竞价量）+已知差异白名单机制（对照脚本豁免清单）。

## 限定范围
只动 bar_minute 域；日线不受竞价条影响（日线接口无竞价条）。

## 接口契约
merge_auction_into_first(df)->df（28 §5.2 已给语义）。

## 验收标准
①新契约钉绿（首根 volume 含竞价量样本）②存量 merge 后抽样 10 标的首根与官方日线量差 <0.1%③dataset_version 推进+血缘可查 ④对照脚本白名单外零差异。

## mock 方式
构造含 09:30 竞价条的样本 df 单测；merge 脚本跑 staging 真库对账。

## 参考文档
29 号 §六；28 号 §3.2（竞价条四件套立法）。
