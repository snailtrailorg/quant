# 批6：收口（ST7 门禁过→阶段 1 切换→direct 退役 + 全量联合验证）（骨架稿）

> 12 号 §2.10 原文："收口：ST7 门禁过→阶段 1 切换→direct 退役+全量联合验证+模块契约回写"。
> **节奏跟 ST7 门禁**——本文件现阶段为步 1 骨架（预研方向+待定量项），
> **步 2 双盲审等门禁临近（≥4/5 干净日）再发**，避免设计过早过期。
> 前置硬 gate：ST7 影子门禁 5 干净交易日（08-27 起算，批 4/5 部署日如有 task 波重启会作废当日）。

## 已知事实（预研基础）

### ST7 影子门禁现状
- 双轨在跑：hub→bar_hub（阶段 0 影子表）+ 任务 8 direct→bar_shadow
- **计数规则**：5 个**干净**交易日（双轨都无缺口/无重启），有效日从批 4/5 部署后的第一个完整交易日起算
- diff 门禁：R-BR20（`bar_hub` vs `bar_shadow` 逐 bar 对比，容差待定——设计 14 号 v2 §5）

### 阶段 1 切换的已知机制
- `md_mode`：system_config 全局键（现=direct），任务级 `params.md_mode` 可覆盖
- 切换=任务 8 的 md_mode 改 hub → runner direct 主循环不再启动 → worker（hub_worker.run）消费 Valkey Streams
- **批 4 已把 worker 迁上骨架**：11 钩子接线+XReadSleeper+trading 九单元共享——阶段 1 切换即改一个配置键+重启任务

### direct 退役的范围
- main.py direct 主循环段（~679 行中约 300 行）：sleep(10)/counter 循环+MD 会话+订阅重放
- **不退役**：入口分派/`_run_hub_mode`/`_warmup_history`/SA4 退出码/依赖探活（worker 共用）
- `XtpMdSession`+`md_api_guard` 在 hub 侧仍用（hub 的 MD 会话）——退役的是 runner direct 的 MD 面

## 骨架：阶段 1 切换预案（步 1 待定稿项）

### 切换步骤（草案，步 2 双盲审锤炼）
1. **门禁确认**：5 干净日 diff 过 → 产出 diff 报告（R-BR20 断言脚本）
2. **预检**：hub 心跳/心跳历史/流分发正常；worker 冒烟（run_worker_smoke）绿
3. **切换**：任务 8 `params.md_mode='hub'`（Web 或 SQL）→ systemctl restart quant-live-task@8
4. **验证**：worker 心跳 7 字段+ts / bar 消费恢复 / TD 登录正常 / 启动对账行使
5. **观察**：一交易日（G4 完成定义）
6. **direct 退役**：删 direct 主循环段 → 全量测试绿 → 部署 → 观察
7. **bar_shadow/bar_hub 表处置**：阶段 1 后 shadow 侧无写入——保留历史供回溯，停止写入（worker 只写 bar_1min？——待确认 14 号 v2 口径）

### 回滚（草案）
- 阶段 1 切换回滚=md_mode 改回 direct+restart（配置级，秒级）
- direct 退役回滚=批 3 管道 revert+重发布（代码级）
- **切换与退役分两个 commit/发布**——切换稳定后才退役，两步各有独立回滚

### 待定量项（步 1 定稿前须回答）
| # | 问题 | 依据 |
|---|---|---|
| Q1 | diff 容差具体值（价格精确相等 or 容差内）？聚合口径差异（hub MinuteAggregator vs runner BarGenerator） | 14 号 v2 §5 有初稿？ |
| Q2 | bar_shadow 表切换后处置（保留/归档/停写） | 14 号 v2 |
| Q3 | 任务 8 以外的未来任务（新开任务默认 hub 模式？） | 14 号 v2 阶段 1 定义 |
| Q4 | direct 退役后 `XtpMdSession`/`md_api_guard` 在 runner 侧的引用面（main.py direct 段删后 import 链） | 代码核 |
| Q5 | 5 干净日的"干净"判定脚本（自动化 diff+缺口检测，不是人眼） | 新写 R-BR20 断言 |

### 全量联合验证（批 6 收尾）
- 全引擎冒烟：hub postverify+worker smoke+run_md_lifecycle 三件全绿
- 六场景注入回归（批 3 管道）
- 一交易日生产观察（hub/worker/direct-退役后无事故）
- 文档收尾：12 号 §2.9-2.10 状态归档+模块契约全量一致性核

## 参考文档
1. `docs/architecture/13-需求书.md` + `14-设计.md` v2（ST7 全案——阶段 1 定义/diff 门禁 R-BR20）
2. `docs/architecture/12-实盘稳定性设计.md` §2.9/2.10（批次表）
3. `docs/任务/批4-worker迁移与trading解耦.md`（worker 已就位的骨架）
