# 批 60 · M5 契约修订清单（编码前照单执行）

> 状态：由冲突双审（v9 与 28/29 引用链 / 与代码·前批·分层·CLAUDE.md）产出，共 **A 组（28/29 引用链 15 处）+ B 组（代码/契约/文档 回写）+ C 组（联动缺口）**。M5 编码落地时逐条执行，未清完不得收官。

## A 组 · 28/29 号引用链修订（冲突审①）

### A-高（不修则两份文档打架）
| # | 位置 | 修订 |
|---|---|---|
| A1 | 29 §十一 L388 | 契约聚合行 `M3/M4/M5/M6 → M2` 去掉 M5 → `M3/M4/M6 → M2`；L394 `M5→M2 resolve` 行删 |
| A2 | 28 §十七 L807 | 差距表流协议行改「✅ hub gen/租约；❌ 水位线 | 三键+意图切换+水位线衔接器→M5」，删「影子切换」 |
| A3 | 28 §十四 L715-722 | R3b 五步 runbook 整章重写为 v9 §五 T1-T4（起 B 待命→confirm 单 Lua→A 让位 B 接管→收尾校验） |
| A4 | 28 §十三 L683 | stream_registry 配置行删，改「Valkey 三键（gen/lease/intent/surrendered）| switch.py | hub/worker | Valkey」；核对 L691 闭环断言 |
| A5 | 29 §七 L304 | 「切换协议推进（M5/数据变更批）」→「数据变更批（58b）推进，M5 不推进」 |

### A-中
| # | 位置 | 修订 |
|---|---|---|
| A6 | 29 §八 L311-332 | §八整章重写：职责（删 fencing/影子切换执行器）、删 stream_registry DDL、双租约→单租约+intent、删影子流命名、验收改「旧 gen 消息被拒」、删影子期特别纪律 |
| A7 | 28 §8.2 L477-486 + 文档头 L3 + §16.3 L787 | N1 fencing token 推翻→加版本化注记「N1 已被批 60 v9 推翻（跨存储不可实现+盘后回补兜底替代）」；§16.4 评审存档保留原文不改历史 |
| A8 | 28 §12.1 L664 | XTP 参数表「影子预热→gen+1」→「intent+INCR 单 Lua 原子切」 |
| A9 | 29 §二 L66 + §八 L332 | 影子期残留点（v9 曾误写成 §十三）——两处「staging 影子期」删除 |
| A10 | 29 §二 L61-63 | 依赖图 M5 从 M2 分叉点摘下（改挂 M4 或独立，因 M5 不再依赖 resolve） |

### A-低
| # | 位置 | 修订 |
|---|---|---|
| A11 | 29 §十一 L400 | 「否则 CI 红」澄清为「否则须版本化修订」（现无真门，存量） |
| A12 | 29 L49 | 命名示例去掉 `stream_registry` |
| A13 | 29 §八 L332 / §二 L66 | 状态注记改「方案集已定稿（批 60 v9）」 |
| A14 | `docs/任务/批60-M5-流协议.md` | 旧占位文件标注「已被批60-方案集.md v9 取代/退役」 |

## B 组 · 代码/契约/文档回写（冲突审②）

| # | 位置 | 修订 |
|---|---|---|
| B1 | `quant-md-hub@.service` | 模板 `Environment`/`ExecStart` 用 `%i` 注入 `HUB_INTERFACE_ROW`（B 实例 env 绑定，**不修 B 起不来**） |
| B2 | `strategy_framework/broker.py` | 新增「按 external_interface 行 id 取 XTP 凭证」路径（现状 `get_broker` 只取 position 第一行，B 无入口） |
| B3 | `docs/architecture/模块契约/md_hub.md` | 回写：A/B 角色分派、`_lease_boot` B 待命 NX 不退、intent/surrendered 键 + T2 单 Lua、退出码增 exit(0)、心跳 B 实例后缀键、读写表（switch.py 写 audit_log） |
| B4 | `docs/architecture/模块契约/data_platform.md` | 补 databus.py 行（批59 遗留）+ subscribe 真实现 + `_watermark` 连续无缺 + 读 Valkey `hub:bars:*` 依赖 |
| B5 | CLAUDE.md:109 | 「hub 单实例」→「单活双实例（A/B 待命继任）」；「影子期 diff 门禁」短语删除 |
| B6 | 14 号 §2.4/§2.5/§10 | 加演进注记：A 运行期轮询 intent+gen→surrender、boot 前置闸只拦无角色进程、心跳 B 实例后缀键 |

## C 组 · 联动缺口（非冲突，落地必做）

| # | 项 | 动作 |
|---|---|---|
| C1 | `quant:hb:md-hub:2` 无监控消费方 + collector CORE_UNITS 不含 quant2 + deploy 波次不含 B unit | 部署/监控侧补 B 实例单元（collector、deploy 波次、hbcheck 如需） |
| C2 | 租约逻辑零测试覆盖 | 编码时补 A surrender/boot 闸/B 待命的单测（现有 SA/SB/SC/ST 不碰 lease，M5 改动无回归防护） |
| C3 | `test_databus.py` 无 subscribe 断言 | 补 subscribe 真实现测试（守住 `test_get_bars_local_hit` 的 `wm is not None`） |
| C4 | 行为级验收依赖 B 账号 | 判据 1/2/3/5 标注「等 B 账号补验」；切换次日 9:16 首根 bar 告警列为 B 到位后首个运维项 |
