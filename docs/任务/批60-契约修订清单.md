# 批 60 · M5 契约修订清单（编码前照单执行）

> 状态：由冲突双审（v9 与 28/29 引用链 / 与代码·前批·分层·CLAUDE.md）产出，**经 v10 复审② + v11 复审① 两轮同步**（去「待命」措辞 + 补 active_instance/原子 Lua/switch.py 权限）。共 **A 组（28/29 引用链 16 处）+ B 组（代码/契约/文档回写 7 处）+ C 组（联动缺口 6 处）**。M5 编码落地时逐条执行，未清完不得收官。

## A 组 · 28/29 号引用链修订（冲突审① + v10 复审②）

### A-高（不修则两份文档打架）
| # | 位置 | 修订 |
|---|---|---|
| A1 | 29 §十一 L388 | 契约聚合行 `M3/M4/M5/M6 → M2` 去掉 M5 → `M3/M4/M6 → M2`；L394 `M5→M2 resolve` 行删 |
| A2 | 28 §十七 L807 | 差距表流协议行改「✅ hub gen/租约；❌ 水位线 | 四键+意图切换+水位线衔接器→M5」，删「影子切换」 |
| A3 | 28 §十四 L715-722 | R3b 五步 runbook 整章重写为 v11 §五 T1-T4（confirm setsid 绝缘 → SET intent → A 让位 → 按需启动 B（原子 Lua 接管）→ SET active_instance → 收尾校验） |
| A4 | 28 §十三 L683 | stream_registry 配置行删，改「Valkey 四键（gen/lease/intent/surrendered/active_instance）| switch.py | hub/worker | Valkey」；核对 L691 闭环断言 |
| A5 | 29 §七 L304 | 「切换协议推进（M5/数据变更批）」→「数据变更批（58b）推进，M5 不推进」 |

### A-中
| # | 位置 | 修订 |
|---|---|---|
| A6 | 29 §八 L311-332 | §八整章重写：职责（删 fencing/影子切换执行器）、删 stream_registry DDL、双租约→单租约+intent+active_instance、删影子流命名、验收改「旧 gen 消息被拒」、删影子期特别纪律 |
| A7 | 28 §8.2 L477-486 + 文档头 L3 + §16.3 L787 | N1 fencing token 推翻→加版本化注记「N1 已被批 60 v9 推翻（跨存储不可实现+盘后回补兜底替代）」；§16.4 评审存档保留原文不改历史 |
| A8 | 28 §12.1 L664 | XTP 参数表「影子预热→gen+1」→「T2 单 Lua SET intent=快照 G（不 INCR）+ B 原子 Lua `_lease_acquire_guarded` 条件推进」 |
| A9 | 29 §二 L66 + §八 L332 | 影子期残留点（v9 曾误写成 §十三）——两处「staging 影子期」删除 |
| A10 | 29 §二 L61-63 | 依赖图 M5 从 M2 分叉点摘下（改挂 M4 或独立，因 M5 不再依赖 resolve） |
| A15 | 28 §8.2 + 29 §八 | **「计划切换零丢包→盘后回补兜底 + 口径 diff」版本化注记**（v10 砍影子后 B 按需启动走故障冷切路径，真空窗靠盘后回补兜底——明写取舍理由，防后人以为仍零丢包） |
| A16 | 29 §十一 L395 | **「M5→worker ContractEvent(gen,seq,payload)」信封挂账注记**（v11 沿用 14 号裸字段，语义等价、信封化挂账 M7） |

### A-低
| # | 位置 | 修订 |
|---|---|---|
| A11 | 29 §十一 L400 | 「否则 CI 红」澄清为「否则须版本化修订」（现无真门，存量） |
| A12 | 29 L49 | 命名示例去掉 `stream_registry` |
| A13 | 29 §八 L332 / §二 L66 | 状态注记改「方案集已定稿（批 60 v11）」 |
| A14 | `docs/任务/批60-M5-流协议.md` | 旧占位文件标注「已被批60-方案集.md v11 取代/退役」 |

## B 组 · 代码/契约/文档回写（冲突审② + v10 复审②）

| # | 位置 | 修订 |
|---|---|---|
| B1 | `quant-md-hub@.service` | **B 单元 drop-in 直写 `HUB_INTERFACE_ROW=<B行id>` + `HUB_UUID=<switch.py 生成的 uuid>`**（不用 `%i`——实例名 quant2 与行 id 命名空间错位；**不修 B 起不来**） |
| B2 | `strategy_framework/broker.py` | 新增「按 external_interface 行 id 取 XTP 凭证」路径（现状 `get_broker` 只取 position 第一行，B 无入口——**v11 §九 实现模块表已列，勿漏**） |
| B3 | `docs/architecture/模块契约/md_hub.md` | 回写：A/B 角色分派、**boot 双重闸（active_instance 闸 + intent 闸）**、**原子 Lua `_lease_acquire_guarded`（首接/重启/污染三态）**、intent/surrendered/active_instance 键、退出码 exit(0)/exit(3)、读写表（switch.py 写 audit_log） |
| B4 | `docs/architecture/模块契约/data_platform.md` | 补 databus.py 行（批59 遗留）+ subscribe 真实现 + `_watermark` 连续无缺 + 读 Valkey `hub:bars:*` 依赖 |
| B5 | CLAUDE.md:109 | 「hub 单实例」→「单活双实例（A 现任 + B 按需启动 + active_instance 仲裁）」；「影子期 diff 门禁」短语删除 |
| B6 | 14 号 §2.4/§2.5/§10 | 加演进注记：A 运行期轮询 intent→surrender、boot active_instance 闸 + intent 闸、B 原子 Lua 条件推进（**删 v9 的「心跳 B 实例后缀键」——v11 无 :2 后缀**） |
| B7 | 14 号 §2.2/§2.3 | 回写 `hub:active_instance` 键（无 TTL 现任标记）+ `_lease_acquire_guarded` 原子 Lua（与现行 `_lease_acquire` 并存，B 接管专用） |

## C 组 · 联动缺口（非冲突，落地必做）

| # | 项 | 动作 |
|---|---|---|
| C1 | collector CORE_UNITS 不含 quant2 + deploy 波次不含 B unit | 部署/监控侧补 B 实例单元（collector、deploy 波次、hbcheck 如需） |
| C2 | 租约逻辑零测试覆盖 | 编码时补单测：A surrender / boot 双闸（active_instance+intent）/ **B 原子 Lua 三态（首接 INCR+抢 lease / 重启只抢 lease / 污染拒零污染）** / `_lease_renew` 前置查 surrendered（现有 SA/SB/SC/ST 不碰 lease，M5 改动无回归防护） |
| C3 | `test_databus.py` 无 subscribe 断言 | 补 subscribe 真实现测试（守住 `test_get_bars_local_hit` 的 `wm is not None`） |
| C4 | 行为级验收依赖 B 账号 | 判据 1/2/3/4/5 标注「等 B 账号补验」；切换次日 9:16 首根 bar 告警列为 B 到位后首个运维项 |
| C5 | **switch.py 权限模型 + SIGHUP 绝缘** | 定义 switch.py 运行身份 + `systemctl start quant2` 走 quant-svc wrapper 特权通道（sudoers 白名单）；confirm 用 setsid/nohup 绝缘 + 幂等可重跑（半途死→重跑从判定表续） |
| C6 | **intent-poll 与 lease-renew 双 5s hook 时序** | 明确 A 主循环 intent-poll 注册位置，保证 surrendered 键恒先于 DEL lease（铁律 4 load-bearing），不与 `_lease_renew` 同拍竞争 |
