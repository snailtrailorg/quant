# 批 62：M7 质量闭环（shadow+白名单+血缘）

> A04 §十（旧 29 号）=主规格。前置=批 58/59（血缘值由消费链写入、shadow 需 fetch(kind)）。

## 目标
shadow 采样对账引擎+口径白名单治理+signal_log/回测结果血缘三列+SM 自身对账。

## 依赖（就绪）
- 批 58/59 ✅

## 产出
shadow_policy 表+对账引擎（白名单先归一再 diff——白名单=代码仓版本化文件+验证义务+命中率监控三条治理）+0098 血缘三列迁移+SM 对账（金标准源=routing_policy 指定）+diff 报表页。

## 限定范围
不生产 restate_event（58 建、修订同步链写）；不做路由决策（消费 M2 审计）。

## 验收标准
①双源 diff 报表（含口径归一命中统计）②审计链贯通（一笔 signal→源→fetched_at→版本全链可溯）③SM 对账差集报告 ④白名单命中率监控上线。

## mock 方式
对账引擎 mock 双源帧；贯通查询用 staging。

## 参考文档
A04 §十（旧 29 号）；A03 §十五（旧 28 号）。
