# 批 65：D26-A TD provider 插件化（builder 下沉+ST6 容量算账）

> 八步法步 1 方案 · **v2 返修版**（方案双盲 A P1×3+P2×8 / B **P0×1**+P1×7+P2×5 同判全修——P0=存量测试 test_d5_account_binding 断言 main._TD_BUILDERS 搬走必断→main re-export 兼容；键域守门改锚 PROVIDER_MARKET（Broker._REGISTRY 无 emt_emq=P4 前向地雷）；ST6 补采加密 hub 实测+下界口径）。
> 制度符合性（W6）：无表变更无迁移；无权限/通道/部署制度变更；分层守门（同包平级模块）。

## 目标

①TD builder 从 main.py（入口+编排混合大文件）下沉至专模块+注册机制（`register_td_builder`）——批 63 P4 接 EMT TD 时**零动 main.py**（只加条目）；②ST6 容量算账收口（prod 一手实测→D26-B 前置判定）。

## 现状（勘察实证）

| 事实 | 锚点 |
|---|---|
| `_build_xtp_runtime`（ThinTdGateway+XtpTdApi+XTPAdapter+连接窗，约 60 行）与 `_TD_BUILDERS={"xtp":...}` 都在 main.py | `strategy_runner/main.py:125-188`（B-P1-1 行号修正） |
| **存量测试直接断言 `main_mod._TD_BUILDERS`**（搬走必红——方案 v1 勘察漏） | `tests/test_d5_account_binding.py:55-58` |
| main.py:26 `build_xtp_setting as _build_xtp_setting, runner_client_id` 兼容别名（tests 4 处引用，禁删） | `main.py:26`+`test_strategy_runner_integration.py:20,45,71,83` |
| 分发已动态（D5 落地）：builder 查询+stub 硬闸+`try/except → sys.exit(EX_CONFIG)`（78=RestartPrevent 豁免防重启风暴——**异常处理必须留在 main 调用点**，B-P1-7/A-P2-3） | `main.py:227-253` |
| 键域真源选型：`PROVIDER_MARKET`（markets.py）含 emt_emq ✅；`Broker._REGISTRY`={xtp,binance_perp,okx_perp} **无 emt_emq**（键域守门若锚此=P4 前向地雷，A-P1-2） | `quant_common/markets.py:81`+`broker.py:68-72` |
| **ST6 一手实测（2026-09-26 周六采集——非交易日窗外，TD 未连接=下界口径）**：hub 模式 TD-only worker（live-task@8 running）RSS=**86M**（旧预估 400-600M=direct/vnpy 全家桶数字，D12:96+D13:18 出处——R-BR1/R-CAP1 精简红利兑现）；**hub 实测：@quant 235M、@4 加密 88M、@5 加密 95M（已在跑，含于 used 内非「加入」）**；Mem 1870M used 1680M available 190M+swap 2982M 用 581M；feishu ×2=266M、celery ×2=238M、uvicorn 123M、PG 126M | ansible 采集（本批 65b） |

## 65a：TD builder 下沉+注册机制（3 文件）

### a-1 新模块 `strategy_runner/td_registry.py`

```python
"""D26-A：TD provider 插件注册表（批 65a 下沉自 main.py:125-188）。

立法（D5+D26 §零底座同质化）：加 provider=只加 builder 条目，禁 if provider== 硬编码
（M3 守门）。批 63 P4 EMT TD=本注册表第二实例（零动 main.py）。
**单文件自足约定（B-P2-2）**：新 builder 必须注册于本模块内（import 即注册；独立文件
注册=空表静默）。本模块不 sys.exit（EX_CONFIG 异常处理留在 main 调用点——78 豁免语义）。
"""
import logging
from typing import Callable
from src.strategy_framework.broker import build_xtp_setting, runner_client_id   # 下行层 2（B-P1-3 头部依赖）

logger = logging.getLogger("strategy_runner.td_registry")

TD_BUILDERS: dict[str, Callable] = {}

def register_td_builder(provider: str):
    """decorator 注册（重复注册=启动期 AssertionError 防静默覆盖）。"""
    def _wrap(fn):
        assert provider not in TD_BUILDERS, f"TD builder 重复注册: {provider}"
        TD_BUILDERS[provider] = fn
        return fn
    return _wrap

def build_td_runtime(provider, ee, tid, account_id, boot_epoch) -> dict | None:
    """单入口分发：未注册返 None（调用方走 stub 硬闸分支）。"""
    builder = TD_BUILDERS.get(provider)
    return builder(ee, tid, account_id, boot_epoch) if builder else None

@register_td_builder("xtp")
def _build_xtp_runtime(ee, tid, account_id, boot_epoch) -> dict:
    ...（main.py:125-184 搬入；适配点三处：import 源改本模块头部/logger 重绑/模块归属）
```

### a-2 main.py 瘦身（含两处兼容保留）

- 删 `_build_xtp_runtime`+`_TD_BUILDERS`（:125-188）→ `from src.strategy_runner.td_registry import build_td_runtime, TD_BUILDERS`（绝对导入对齐本文件风格）。
- **兼容 re-export（B-P0-1）**：`_TD_BUILDERS = TD_BUILDERS`（test_d5_account_binding.py:55-58 断言 main._TD_BUILDERS——保持 3 文件范围不碰该测试）。
- **main.py:26 兼容别名行加 `# noqa: F401`**（B-P1-2：tests 4 处引用禁删；pyflakes 零新增达成）。
- `_run_hub_mode` 分发改：**try/except EX_CONFIG 保留原位**（:249-253），体内 `rt = build_td_runtime(provider, ee, tid, account_id, boot_epoch)`；`rt is None` → stub 硬闸分支（原逻辑不动）。
- ThinTdGateway 随 builder 搬入（builder 私有件，全仓无外部引用——B 审核实）。

### a-3 测试 `tests/test_td_registry.py`（新）

- 注册机制：decorator 注册/重复注册 AssertionError/未注册返 None（模块级 dict 可变——注入后自清，A-P2-8 注记）。
- **键域守门（A-P1-2 改锚）**：`set(TD_BUILDERS) ⊆ set(PROVIDER_MARKET)`（配置域真源，emt_emq 在列=P4 无地雷；Broker._REGISTRY 无 emt 键不可锚）+xtp 条目存在。
- builder 可调性：patch **`src.strategy_runner.td_registry.build_xtp_setting`**（模块级 import 后名字绑定在本命名空间——patch 源模块不生效，B-P1-4）+XtpTdApi/XTPAdapter+**md_session 三函数**（is_trading_day/load_xtp_window_cfg/xtp_session_window_open——不 patch 则真连 DB+时钟 flaky，B-P1-5），断言八键形状。
- **分发等价性两用例（B-P2-1）**：build_td_runtime 返 None（未注册）→stub 分支可达性；返 dict→八键消费形状。

## 65b：ST6 容量算账收口（0 代码，结论落待办）

**一手实测（2026-09-26 周六——非交易日窗外采样，TD 未连接=下界口径，B-P1-6）**：
- TD-only worker **86M/任务（下界）**；hub 实测 @quant 235M / 加密 @4 88M @5 95M（**已在跑**含于 used 非加入，A-P1-3 修正）；available 190M+swap 余 2401M。
- live_task 单元 `MemoryMax=1G` 系合约表尖峰时代设定（远高于 86M 现实——留缩档线索，A-P2-7）。
**结论**（写入待办 ST6 条目收口）：
- hub 模式精简红利实测兑现——TD 任务容量 **≥86M/任务（下界）**；单样本单日（HOLD 轻载）**不做线性外推**（旧盲审准则），**周一（09-28）盘中复核 TD 连接态 RSS 后终收**。
- 现配 1.8G 增 1 任务可容（available 190M 贴边+swap 垫底）；**≥2 新增任务或新 hub 实例（A股 hub 重档 235M 级）=升配判定**（4G=云主机常规升配档，非实测依据）。
- D26-B 前置判定：**满足**（结论存在性成立）——per-account hub N 化在现配下=加密 hub 已在跑零增量；新增 A 股 hub（235M 级）即触升配档判定（挂账用户裁定）。
- 待办:13 与 H15「扩条目」字面顺手改写为「机制就绪、EMT 条目=批 63 P4 落地」（A-P2-6 消歧）。

## 限定范围

**65a（3 文件）**：`strategy_runner/td_registry.py`（新）、`strategy_runner/main.py`（瘦身）、`tests/test_td_registry.py`（新）。
**65b（1 文件）**：`flow/待办.md`（ST6 条目收口）。
**不碰**：broker.py、adapters.py、hub_worker.py、md_session.py、main.py 其余编排逻辑（stub 硬闸分支原样）、web。

**明确不做（挂账）**：EMT TD builder 本体（批 63 P4=注册表第一消费者）；TD 注册表与 IM `ONBOARDING_METHODS` 式方式注册合并（两域语义不同不合并）；stub 分支重构（保持原样——D26-B 观测面 N 化时再动）。

## 验收标准（本机 dev，cwd=server/，venv=./venv）

1. `./venv/bin/python -m pytest tests/test_td_registry.py tests/test_layering.py -q` 绿（注册机制+键域+八键形状+分层）
2. `./venv/bin/python -m pytest tests/ -q` 全绿（存量 crypto 失败预存已备案）——main.py 瘦身不破现有 worker 测试（test_strategy_runner*/test_strategy_task_separation）
3. `./venv/bin/python -m pyflakes src/strategy_runner/main.py src/strategy_runner/td_registry.py` 零新增
4. import 形状：`python -c "from src.strategy_runner.main import _run_hub_mode"` 可导入（模块级无环）
5. ST6 条目收口落待办（65b）

## mock 方式

- builder 可调性：patch `src.strategy_runner.td_registry.build_xtp_setting`（模块级 import 名字绑定本命名空间——patch 源 broker 模块不生效）+md_session 三函数（is_trading_day/load_xtp_window_cfg/xtp_session_window_open）+XtpTdApi/XTPAdapter（函数级 import 搬入，patch 源模块有效）
- EventEngine：MagicMock（不真 start）

## 风险面

- **搬迁忠实**：约 60 行搬入（适配点三处：import 源/logger/模块归属）（含 docstring/注释），分发点唯一改动=字典查询单入口化——diff 可逐行核对
- **启动期 assert**：register 的 AssertionError 在 import 时触发=启动失败（fail-fast 预期行为，防静默覆盖）
- **无行为变化**：本批零行为变更（纯结构下沉）——staging 彩排+prod 后任务重启验证（现有任务 8 重启走新路径）

## 参考文档（≤2）

1. `docs/design/D26-市场接入架构.md` §零（底座同质化/注册即行）+§3.4（内存账）
2. `server/src/strategy_runner/main.py:130-252`（下沉对象+分发点现状）
