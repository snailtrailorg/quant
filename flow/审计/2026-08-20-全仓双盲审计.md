# 全仓双盲审计(2026-08-20)

> 审计指令(用户):双盲审核文档和代码,找出①不合理之处②不符合之处③与业界最佳实践不一致之处。
> 机制:7 个双盲对(14 个互不可见子代理)× 统一任务书;结果交叉比对——一致发现采信,分歧由主会话读码仲裁。
> 本文档为**审计报告**,修复另立批次走八段线。

## 审计单元

| 波 | 单元 | 维度侧重 |
|---|---|---|
| 1 | 架构与分层 ×2 | 分层铁律/CLAUDE.md 约束 vs 代码 |
| 1 | 文档体系 ×2 | 文档互矛盾/契约 vs 代码签名 |
| 2 | 数据层 ×2 | 事务/批量/游标(锁链病灶区) |
| 2 | 调度+运行时 ×2 | beat 一致性/幂等/生命周期 |
| 2 | 核心交易域 ×2 | 因子安全/回测正确性/风控/资金语义 |
| 3 | 服务层 ×2 | web_api 上帝模块/权限/错误码/API 安全 |
| 3 | 质量面 ×2 | 测试体系/前端安全与规范/依赖健康 |

## 汇总结果(逐对填充)

### 原始发现暂存(B 盲侧先行,A 盲到齐后交叉裁决)

**[B-架构]**(已到):PASS 面=quant_common 零业务依赖/290 处 import 无未豁免越界/运行时 DDL 清零(唯一豁免 bar_{freq} ensure_table)/配置驱动/LLM 国内面。FAIL 面=①A1【高】main.py 3153 行 159 处裸 SQL 上帝模块+入口层滞留数据访问;②B1【中】"组合根不寄生业务" vs 181 处入口层 SQL+strategy_config 双份实现(main.py:747 与 feishu_bot/bot.py:262);B2 错误码化 64:24(19 裸 HTTPException+5 裸 dict+2 处 str(e) 直出);B3 六大接口 4/6(Task/LLMProvider 无基类);B4 main.py:1547 测试端点绕过 LLM 网关;C1 importlib 字符串逃过分层守卫;A2 data_platform⇄alert_notify 制度化环(PG 挂=告警链同挂);A3 DAILY_BASIC_DDL 死常量。

**[B-数据层]**(已到):强项=25 写点全显式 commit/零 HTTP 进事务/零未提交还池/池配置合格(pre_ping+recycle)。要害=①A1【高】pool_minute.py:153 `_log_pool` INSERT 列名错配(start/end/pulled/saved vs 真实 start_date/end_date/rows_pulled/saved_rows)被 except 吞→**sync_log 从未写入成功**("断 11 天"教训同型再犯);②A2【中高】handler 硬崩只标 idle 不标 failed 无告警;A3 trade_cal 伪成功硬编码 365;A5 Tushare 全挂时单标的报"uptodate";C1 _sync_astock_minute sleep(0.15) 弃用限速注册表(潜伏);F1【中】bar_{freq} 表名拼接 get_bars 路径零校验(现状不可利用,纵深缺失);D1 Tushare 全链零超时零重试;db.py _VALID_FREQS 与 schema.Freq Literal 矛盾;A4 缺口双写;A6 saved 计数虚高;A7 回补语义分裂(全局 DO NOTHING vs 单标的 overwrite);E1 _ondemand 异常无负缓存;逐行 INSERT 清单(engine 5 处+pool_data)。

**[B-服务层]**(已到):0 面=3153 行/165 端点/平均 18.4 行(长度健康),async def 仅 6。**C1【Critical 可达时/High 现状】/lark/card/callback 无验签+execute_confirmed_tool 不校验授权→伪造卡片可触发 risk_resume/emergency_halt/strategy 启停;现靠两意外挡住:nginx 不代理 /lark+router.py:92 threading 未 import(确认卡片功能本身 NameError 坏死——修复前必须先验签)**;C2 verify_signature 未配 token 时 fail-open+webhook 无重放防护(与卡片入口防护不对称);A1【High】asyncio 未 import:SSE gen()/WS 用 await asyncio.sleep→回测 SSE/WS 行情一帧即断(功能坏死);A2/A3【High】6 个 async def 内同步 bcrypt/DB/Redis 阻塞事件循环;限流全站为零(OWASP API4:登录爆破/邮件轰炸/LLM 计费端点不限);F1 invite token 完整 URL 入 INFO 日志;F2 base_url 缺省信 Host 头(投毒邮件链接域);F3 forgot-password 无速率限制;A4 角色无白名单;A7 terminate_task 不杀进程;A5 query 参数未验证群;A9 XFF 全串可伪造;B1-B3 LLM 熔断无时间窗/单模型 fallback 自加倍失败计数/_truncate 不截 system;D2 /metrics 靠 nginx 单层兜底;E1 stuck 误标 300s;E2 force_delete 硬删审计证据;C4 聊天内容 print 入日志。做得对:token 一次性+防枚举+CORS 白名单+AST 校验+头像白名单。

**[A-核心域]**(已到,27 项:高8/中11/低8):**F1【高·RCE】AST 校验不拦 ast.Attribute——`().__class__.__base__.__subclasses__()` 属性链逃逸,web /api/factors/preview 在 web 进程执行用户代码即可 RCE**(黑名单只拦 Import/裸 Name,受限 namespace 拦不住字面量穿越);F2 无超时/资源限制(while True 挂死/内存炸弹);F3 自定义因子可遮蔽预置名(注册表直接覆盖,delete 可删预置);**F4【高·资金】double_low 真实现是死代码**(ctx.symbol 永空→"双低"实为"最低价"+接通时 bar_1d 无 ts<=当前bar 约束=前视);F5 DSL needs_history=0 语义错(选股静默 0.0);**B1【高·资金】slippage 死参**(存储全程未用,回测系统性乐观);**B2【高】幻影成交**(资金不足跳过但 trades/胜率照录);B3 monkey-patch 非线程安全;B4 防未来校验恒"✓"+官方示例签名被门禁拦死;B5 同 bar 收盘成交(应 next-bar-open)无印花税最低佣金;B9 批量回测恒 100 股不模拟 volume_type/品种子类(违背回测实盘一致声明);**S1【高】R-F1 方向感知只在基类路径**(两预置策略覆写 on_bar 绕行+Python 模式不暴露);**S2【高·资金】SELL 口径含冻结仓**(SUM(volume) 不减 frozen/yd——T+1 当日买入 ALL_IN 超卖→拒单→止损失效);S3 BUY 可用资金高估;S4 `or default` 吞合法 0/False;S5 整百取整对小仓/crypto 恒 0;S6 自定义因子 params 配置不生效;R1 杠杆上限空转(order 无 leverage 键恒 1);R2 update_rules 死代码;R7 截断不成手变废单;BR1 凭证静默降级(DB 缺→.env 测试号无告警);BR2 LIMIT 1 无 ORDER BY;正面:fail-closed 与三级开关 AND 链核对通过/凭证不泄漏。

**[A-调度运行时]**(已到:高3/中8/低观察若干):**A1【高·资金】hub worker 重启窗口流重放无跨进程幂等**(xgroup_del 仅 break 路径/os._exit 跳过 finally;`>` 从旧组水位续消费停机窗 30-40 根 bar;max_ts 进程内存重启失忆;direct 有 _last_bar 幂等 hub 没有)→崩溃重启后 **SELL 重复下单**;**A2【高】sync_config DB cron 全链 UTC 错位 8 小时**("工作日 16:30"实际北京次日 00:30 触发,8 条任务全在凌晨跑,cron 1-5 按 UTC 星期;09 号文档自己警告过时区坑,beat 层防了 DB 层复现);**A3【高】live_task 状态只在 Web 路径回写**(systemd stop/崩溃/重启后 status 永久残留 running→Web 假运行中+hub 永续订阅——退订闭环依赖的真相源本身失真);B1 md_hub 双 flush 窗口 5s<主循环 10s 约 50% 概率永久错过收盘末桶(无告警);B2 主 worker 也消费 risk 队列,F-48 隔离失效(与单元声明矛盾);B3 退订 flush 半分钟桶按可信发布(缺尾 bar 入库);B4 drift_check 量纲错位(因子 score vs 回测收益率直接比,告警全噪/全哑);B5 data_continuity 当日必误报(告警疲劳);B6 task_stuck_check 无调度死代码;B7 弃用任务仍每日跑;B8 expires 仅 3/18 beat 有(连环补跑面);C:_TIER1_TIME_LIMITS 死代码;RestartSec=30==租约 TTL 30 隐式耦合;14 号文档仍写"断流自杀"(三份文档矛盾);celery 无 acks_late/max_tasks_per_child;beat 无 Watchdog。总评:单点机制防御密度高,**三条跨边界链路各断一环**(DB cron 时区/hub 重启幂等/live_task 状态)——"单点质量高、接缝质量低"。

**[A-文档体系]**(已到,26 项失真):根因=**18 份契约中 13 份修订记录停在 08-09~11,此后三轮大改(SA-SE/模块归位/三档)只贴增量小节不回写主文**——约七成失真同源。最危险:【高】web_api.md 失真密度最高(文件清单错——crypto/email 已迁/errors.py 错误码机制只字未提/85 端点实际 161/1494 行实际 3153/admin123 误导登录/CORS "*" 实为白名单);【高】astock_analysis.md **新旧两套算法并存**(§六不变量写旧 ±0.3 阈值+旧权重公式 vs 代码=分位+注册表——照契约做任务必错);【高】**三份契约仍教写已禁的运行时 DDL**(接口契约 464/496+llm_gateway/scheduler 契约——照做即违反铁律);【高】docs/任务/ 目录不存在(待办自包含流程无处落文件);【中】CLAUDE.md 外部 gate 滞后 10 天(XTP/Tushare 待办已答)/17 号三档状态过时/17 号自身 §5 vs §7 矛盾(前端待做 vs ✅)/md_hub 契约主节"自杀"残留 vs S6 已删+**14 号文档同款三份矛盾**/接口契约"待实现"全过时+章号重复×2+crypto re-export 桩不存在/web_api.md 与接口契约对 /api/position 数据源各执一词(XTP vs 快照,代码=快照);【低】"17 份"实 18/61 表实 73/scheduler 契约"缩进错乱"警告已修未撤+漏 4 任务(含 health_monitor_check)/data_sync _HANDLERS 10 实 20+markdown 破损;业界差距:契约无 commit 戳/无 drift 检测/状态四处声明违背"待办单一真相源"。建议:"契约回写日"+核实戳+状态类只留待办指针(消 80% 矛盾源)。

**[A-服务层]**(已到):**与 B-服务层交叉一致(采信)**:①飞书卡片回调链【严重】两盲同判——无验签+execute_confirmed_tool 不校验 operator 授权/发起人绑定,event_id/ts 可自造→未认证第三方可触发 emergency_halt/risk_resume/策略启停(B 补:现靠 nginx 不代理+threading 未 import 两意外挡住,A 补:bot.py:297 args 未取 id 单元名永远畸形=操作类工具本就不可用);②asyncio 未 import【高】SSE/WS 一帧即断两盲同判(A 补:3115/3133 ws 参数缺 WebSocket 注解→accept() 对 str 调用,/ws/chat 按现状不可用);③限流全站为零两盲同判;④错误码化长尾两盲同判;⑤熔断单模型 fallback 双打两盲同判(B2)。**A 独有(待仲裁/高置信直接采信)**:LLM 流式用量记 0【高·计费】(gateway.py:332 未开 include_usage,ws_chat token 全不入账,预算检查系统性低估);/api/account 三件套 CRUD 零审计【高】(对照组 llm-models 全有审计);162 端点/3153 行(契约记 85/1494 一个周期翻倍);函数内 import 103 处;notify _quota_exceeded 无 try 与注释 fail-open 矛盾;verify_jwt 每请求新建 Redis+PG RTT;user_tokens 明文存 token;register TOCTOU;enhance_with_llm 串行无缓存;正面:astock_analysis 重写质量好(参数化/无注入/分位/行级降权全对齐)。**B 独有**:invite token 完整 URL 入 INFO 日志;base_url 信 Host 头投毒;forgot 无速率限制;task force_delete 删审计证据;stuck 300s 误标;starttls 无 SSL context;/lark/test 无鉴权。

**[B-核心域]**(已到):**与 A-核心域交叉一致(采信)**:①F1 沙箱逃逸两盲同判且 B **实测复现**(`ctx.__init__.__globals__["__builtins__"]["__import__"]` 通过校验拿到真 __import__,194 类可达→Popen/os;A 给属性链理论)——Analyst 经 /api/factors/preview 即 web 进程 RCE【P0】;②slippage 死参两盲同判(P1);③幻影成交(资金不足跳过但明细照录+清仓不记交易不扣佣)两盲同判;④double_low 前视(A:代码死+接通前视/B:直接判未来函数 P1)两盲同判;⑤R-F1 只在基类两盲同判;⑥整百手数(转债 10 张错 10 倍/crypto 恒 0/风控截断不成手废单)两盲同判;⑦杠杆空转+update_rules 死代码+or default 吞 0 两盲同判。**B 独有(高置信)**:F1.2【P0】**Python 策略保存路径零 AST 校验**(create/update 不调 _check_ast_blacklist,每 bar 裸 exec——比 preview 更广的 RCE 面);F3.3【P1·功能坏死】**长假 gap 预检 false-positive**(diff>7 天即断点判死——跨国庆/春节的回测全部 failed);F5.2【P1】**总回撤熔断连 SELL/止损一起拒**(与 F-31 修正精神冲突,回撤触线后只能持仓看戏);F5.3【P1】风控快照无新鲜度校验(陈旧 fail-open——runner 死后风控永远用最后一帧);F5.1【P1】single_position_pct 只有默认值**零判定**(SC3 遗留确认:规则表展示存在但闸不存在);F4.1【P1】PERCENT/ALL_IN 资金口径=全账户非策略级(多策略共账户合计超配);F4.4 GTC 死参(读了不传);F4.2 sizing fail-open vs 风控 fail-closed 哲学冲突(巧合性安全);F6.2 PERCENT 卖零股残留;F6.3 最低佣金/印花税全链未建模;F3.4 Sharpe 252/多频即错;F1.8 因子值不归一直接加权;F3.6 成交配账 ts 全扫 O(n²)。

**[A-架构]**(已到):**与 B-架构交叉一致(采信)**:①main.py 上帝文件两盲同判(A:3153 行/163 端点/48 资源组/19 模型/156 处 execute=全后端 20% 代码量,除 feishu_router 零拆分;B:159 SQL+181 处入口层 SQL)——"组合根"名实不符;②LLM 国内化零 enforcement 两盲同判(provider 自由 str 无白名单,CLAUDE.md 声明 vs 平台化开放配置两份声明打架);③六大接口 4/6 两盲同判(Task/LLMProvider 无基类);④alert_notify⇄data_platform 制度化环两盲同判;⑤DAILY_BASIC_DDL 死常量两盲同判。**A 独有(高置信)**:**A1【高】test_layering 正则盲区——`(?:from|import)\s+src` 不匹配跨包相对 import**(`from ..data_platform.db import`,factor.py:151/190/231/503/504 五处实际在用此通道)——守门测试自己漏一条已使用的通道,"铁律只锁了正门";B1【中】md_hub(MD-only 声明)lazy import strategy_framework.broker.build_xtp_setting 取连接配置——依赖方向与 13/14 号设计相反,归位只修一半;B3 RBAC 真相源三处分散(auth.py/gateway.py/feishu env 表);C1 SQL 散布 6+ 包全景(main 156+tasks 32+runner 16+auth 18+hub 6+feishu 7 处 execute)——无 Repository 边界,"数据中台"名实不符;C3 测试白盒化(48 文件 154 处私有成员访问/15 monkeypatch,重构成本被测试套件放大);C2 全局单例无 DI(与"售出独立实例"商业模式相悖);正面:quant_common 纯度/入口零反向依赖/三级开关实证/alembic 唯一真相源全过。

**[A-数据层]**(已到,健康度 5/10):**与 B-数据层交叉一致(采信)**:①pool_minute `_log_pool` 列名错配 sync_log 永写不进【高】两盲同判(A 补:tests/test_pool_minute 全 MagicMock=踩坑"MagicMock 掩盖"**三犯**直达生产);②逐行 INSERT 群两盲同判(A 计 5 处:tier1 工厂单日 5000 行/pool_data/list/basic/rebuild+DELETE 全表单事务持锁随行数涨);③trade_cal 伪成功 365/tier1 进度恒 100% 两盲同判;④_fetch_and_save 吞异常成"empty"两盲同判;⑤限速注册表建而不用两盲同判(astock_minute sleep 0.15s vs 注册表 3600s/次冲突 1800 倍);⑥全项目零 SQL 超时(statement/lock/idle_in_transaction 全缺)两盲同判。**A 独有(高置信)**:**A2【高】`last_status=='running'` 无崩溃恢复——OOM/SIGKILL 后永久停摆**(无 stale 判定,复位入口需人肉全量重建;与 B 的"硬崩只标 idle"为同一问题链两面);**A3【高】sync_lock.py:47 Valkey 客户端无 socket_timeout(四处唯一裸奔,抖动时 worker 卡死在拿锁)**;A4 缺口分支双写+adj_factor 恒 NULL(F-F2 只修回补漏自动路径);A7 引擎零超时;A10 rows_saved 系统性虚高;B5 stock_detail 直连 get_pro 绕过 DataSource 统一入口(用量监控口径缺口);C4 top_list 无 ts_code 二级索引(详情页全表扫);C2 iterrows 构造热点。**B 独有**:Freq Literal 与 _VALID_FREQS 矛盾;E1 _ondemand 异常无负缓存;F1 bar_{freq} get_bars 路径零校验。**A 附注(重要修正)**:当前代码 astock_basic 已非 49min 单事务(逐日+executemany)——锁链病灶或为服务器旧版残留,佐证"超时上界比逐点找病灶更根治"。

**[B-文档]**(已到):**与 A-文档交叉一致(采信)**:①web_api.md 数字级失真最重两盲同判(85→163 端点/1494→3153 行/27→45 组/整组缺失);②接口契约"待实现"节全过时两盲同判(on_bar_callback/validate_bars 已实现,同文档自相矛盾);③三份契约仍教已禁运行时 DDL 两盲同判;④17 号 §5 vs §7 项 16 矛盾两盲同判;⑤schema 表数四方打架(50/61/71/73,实际 73)两盲同判;⑥scheduler 契约漏 4 任务(含 health_monitor_check beat 30s)两盲同判+缩进警告已修未撤;⑦data_sync _HANDLERS 10→19/20+markdown 破损两盲同判;⑧md_hub"自杀"残留两盲同判;⑨易腐数字手抄而非生成式引用+契约无机器校验(业界差距)两盲同判。**B 独有(高置信)**:**risk_control.md 正文两处写"读失败返回 0.0 无风险放行"——实际 fail-closed 拒单,风控行为认知方向颠倒【最高危失真,恰在实盘安全路径】**(文末指针式追认未回改正文,双层真相);**接口契约 live_task 单元名写旧架构 `quant-strategy@<id>`(实际 quant-live-task@{tid})——照契约拼 systemctl 操作错对象**;task_manager.md 与 web_api.md 同端点权限各说各话(均 admin vs viewer+/trader+);notify_on_failure 契约写走 MessageChannel 实际走通知中心;记忆 MEMORY.md 索引滞后记忆正文;scheduler 契约反向依赖描述过时(已迁 budget.py)。**A 独有**(已入 A-文档节):docs/任务/ 缺失/astock_analysis 新旧算法并存/外部 gate 滞后/crypto re-export 桩不存在。正例:strategy_runner/llm_gateway/data_sync 三份契约达"签名逐字可核对"高水位。根因共识:不是写作质量而是**收敛机制缺失**("今日语义变更"附录式追认无人回改正文)。

**[B-质量面]**(已到,14 项):强项=资金路径测试上乘(risk fail-closed/下单 WAL 时序行为级,无断言 mock 假绿)/quant_common 零死代码/路由懒加载 100%/一致性违规少。**真缺陷**:①【高】bcrypt 5.0 下 >72 字节密码抛 ValueError→注册/重置/改密 500(实测复现,validate 无上限校验);②【中】**WS 聊天前后端认证契约不匹配**(前端不带 ?token=发 auth 帧,后端要求 Query token 且不读 auth 帧→流式握手必败静默 fallback,功能死亡);③【中】passlib 死依赖+bcrypt 未声明靠传递带入(删 passlib 即断,venv 组合已损坏);④【中】加密密钥回退链落到公开常量且仅实盘模式拦截(非实盘部署真实 API Key 可被已知密钥加密);⑤【中】zh/en 22 键不平衡(en 用户见原始键名);⑥【中】依赖全 >= 浮动无锁定(实测漂移:fastapi 0.110→0.141/redis 5→8/cryptography 42→50,部署不可复现——交易系统显著风险);⑦【中】BacktestEngine 权益计算零正确性断言(只数回调次数);低:size=small 13 处/无 type 按钮 1/el-tag 裸色 5/marked+v-html 无 DOMPurify/401 无去重/monaco 3.9MB 静态进 2 页。归因:4 处真缺陷全是"写了规范没写边界测试"同型病。

**[A-质量面]**(已到):**与 B-质量面交叉一致(采信)**:①i18n zh/en 22 键不平衡两盲同判同数;②requirements 全 >= 浮动零 pin 零锁文件两盲同判(A 补:pandas/numpy 直接依赖未声明靠传递带入;B 补:实测漂移 fastapi 0.110→0.141/redis 5→8);③回测引擎资金计算零数值断言两盲同判(A 补:全仓无 commission/slippage/PnL 正确性测试+实盘记账 SQL 恒被 mock 只测调用顺序);④monaco ~10 倍浪费两盲同判;⑤Help v-html 无 DOMPurify 两盲同判;⑥前端零测试设施两盲同判;⑦加密密钥回退公开常量两盲同判(A 的 B1=B 的 ④);⑧size=small 违例群两盲同判(12/13 处微差)。**A 独有(高置信)**:**C1【最高】api.js:169 `export const getLiveTrading = api.get('/live-trading')` 缺箭头——import 即发全员请求+导出 Promise 被当函数调用必抛→Risk.vue catch 静默吞→实盘分项开关恒 false/总闸 null**(注释上一行正是"链条打磨#21 修复恒 false"——**修上一个恒 false 引入下一个,第三次同型事故**,且 /api/live-trading 响应形状无契约测试两次都没网);**C2【高】apiErr 采用率 10/39 view(26%)**——错误码本地化链 74% 页面失效,违反"前端 apiErr 优先"约定(DESIGN.md §7 自己也停在旧写法);C3【高】Trading 持仓/订单/盈亏加载失败 catch{} 静默空表(交易系统假空显示);A2【高】源码文本断言 10 处(字符串 in src——假绿前科现存变体);A4 真 DB 冒烟静默 skip;B2 quant_common 层 0 含业务概念(terms 条款/A股时段知识);C7 路由守卫仅 admin 一档(后端兜底在位,UX 缺口);A6 正例:风控 fail-closed 全家桶/WAL 时序契约/layering 守卫是真质量,383 测试 13.58s 全绿。

**[B-调度运行时]**(已到):**与 A-调度运行时交叉一致(采信)**:①md_hub 双 flush 相位缺陷【高】两盲同判(5s 窗口<10s 主循环,~50% 实例永不在 11:30:05/15:00:05 触发;B 补深:收盘桶次日被跨日 clear 丢弃=**流+bar_hub 双失静默**);②task_stuck_check 注册无 beat 死接线两盲同判(B 补:backtest 成功路径从不 complete_task→tasks 表回测行永久 running 靠永不运行的巡检收尸);③pool_minute sync_log 列名错配【高】**三盲同判**(数据层 A/B+本份——最可靠发现);④_heartbeat_ok 死标志三盲同判(锁静默过期防重破口);⑤expires 仅 3 条两盲同判;⑥弃用任务群两盲同判。**B 独有(高置信)**:**broker_health_check 永假告警【高】**(裸 cls() 构造 broker 无凭证必 test_connection=False→每 6h 报"全通道离线",告警疲劳;正确入口 get_broker() 从未被用);**notify _quota_exceeded 未 fail-open【高】**(注释自称全 fail-open 但配额检查无 try——Valkey 故障时 critical 告警外推必死,恰违背 D-F1);**风控拒绝零痕迹【中】**(check_order 不过直接 return,signal_log 只在通过后写→三账对账"信号无委托"看不到被风控禁足的策略);发单真实失败零通知(仅 journal);sync 兜底 except 连 SoftTimeLimitExceeded 吞(超时保护失效);static_list_sync 一次瞬时失败丢一周+docstring 称标退市实际永不标;daily-report 用 crontab 其余用 float 时刻漂移双标准;_is_trading_hours 930/1300 与 session 931/1301 不一致;backtest_symbol_task NameError 死码;退出码 os._exit(0) 稀释 OnFailure;safe_notify 全归 system 类(trader 看不到交易类告警);PGWriter 丢批仅 warning(影子门禁数据流失)。正面:20 beat×task 名全对齐零悬空路由;BUY fail-closed 双模式/伪单号识别/gen fencing 高于业余水准。

---

## 终版裁决汇总(七对双盲交叉完成)

### P0·资金与安全(立即修,共 9)
| # | 问题 | 来源(盲数) |
|---|---|---|
| 1 | **因子沙箱属性链逃逸 RCE**(preview 端点+Python 策略保存路径零校验,实测复现) | 核心域 A+B(2) |
| 2 | **飞书卡片回调无验签+确认执行无授权**(伪造卡片可触发熔断/恢复/策略启停;现靠两意外挡) | 服务层 A+B(2) |
| 3 | **hub worker 重启窗流重放无跨进程幂等**(SELL 重复下单) | 调度 A(1,代码级证据链完整) |
| 4 | **SELL 口径含 T+1 冻结仓超卖**(R-F1 只落在基类;两预置策略绕行) | 核心域 A+B(2) |
| 5 | **总回撤熔断连 SELL/止损一起拒**(与 F-31 冲突,触线后只能持仓看戏) | 核心域 B(1) |
| 6 | **回测失真三连**(slippage 死参/幻影成交/长假 gap 预检判死)——backtest_verified 在给假数字放行 | 核心域 A+B(2) |
| 7 | **api.js:169 getLiveTrading 缺箭头**(实盘开关恒 false,第三次同型事故) | 质量 A(1,代码级) |
| 8 | **asyncio 未 import+WS 参数无注解**(SSE/WS 流式一帧即断,功能坏死) | 服务层 A+B(2) |
| 9 | **bcrypt>72 字节密码 500**(注册/重置/改密全断) | 质量 B(1,实测复现) |

### P1·观测护栏本身是坏的(第二批,共 8)
pool_minute sync_log 列名错配被吞(**三盲同判**)/broker_health 永假告警/notify quota 未 fail-open(critical 告警在 Valkey 故障时外推必死)/DB cron UTC 错 8h(8 任务凌晨 00:30 跑)/running 无崩溃恢复(OOM 后永久停摆)/live_task 状态只在 Web 回写(systemd 死后假 running+hub 永续订阅)/md_hub 双 flush 相位(50% 收盘桶蒸发)/风控快照无新鲜度(陈旧 fail-open)。

### P2·功能与正确性(第三批,共 ~14)
LLM 流式用量记 0(计费级)/WS 聊天前后端认证契约断裂/passlib 死依赖+bcrypt 未声明/依赖零 pin 零锁+pandas 未声明/apiErr 采用率 26%/Trading 假空表/Freq Literal 矛盾/缺口补拉 adj 恒 NULL/double_low 死代码+前视/single_position_pct 零判定/GTC 死参/PERCENT 全账户口径/卖零股残留/drift_check 量纲错位。

### P3·文档契约(第四批"契约回写日")
risk_control.md **风控语义反写(最高危)**/live_task 单元名旧架构/web_api.md 数字级失真/astock_analysis 新旧算法并存/三份契约教已禁 DDL/docs/任务/ 缺失/17 号 §5vs§7/表数四方打架/接口契约"待实现"全过时/章号混乱。根因=**收敛机制缺失**(附录式追认无人回改正文)。

### P4·结构债(第五批)
main.py 3153 行 163 端点拆 router/分层守卫正则盲区(相对 import)/SQL 散布 6+ 包无 Repository/限流全站为零/密钥回退公开常量/RBAC 三处分散/源码文本断言 10 处+MagicMock 三犯/monaco 10 倍浪费/六大接口 4/6。

### 系统性病根(五模式,跨七对)
1. **承诺-实现脱节**(docstring/契约/规则表说了代码没做):slippage/GTC/single_position_pct/quota fail-open/退市标记/PGWriter 告警——同一形态反复出现
2. **观测护栏腐化**(护栏自己坏了没人看):sync_log 列错/假告警/伪成功/进度恒满
3. **单点质量高、接缝质量低**:时区/重启幂等/状态回写/前后端契约全断在模块边界
4. **同型事故三犯**:恒 false×2+MagicMock×3+文本断言——无机制阻断复发
5. **文档无收敛机制**:增量附录式追认,主文永不回写

### 待用户确认清单(审计过程积累,未阻塞)
1. 修复批次按 P0→P4 排序执行?(资金安全优先原则)
2. 沙箱方案选型:子进程隔离(简单)vs 容器(彻底)——涉及成本
3. LLM 国内化:加 provider 白名单 enforcement,还是降格为运营约定(CLAUDE.md 声明与平台化开放配置两份声明打架,需拍板)
4. 文档"契约回写日"是否立项专门批次(消 80% 矛盾源)
5. DB 统一规划(待办 0.5)与本审计 P1 批合并做还是分批
6. md_hub 双 flush 修复方案(主循环改 5s 步长 or flush 窗口加宽)——小设计决策
7. main.py 拆 router 是大手术,何时做(可与 P4 其他项一起排期)

## 待用户确认清单(审计过程积累,不阻塞执行)

(空)

## 修复批次建议(审计产出后排序)

(待汇总后填)
