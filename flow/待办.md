# 待办跟踪表

> 单一真相源。所有待办项 + 状态在此。进展日志/decisions 不再重复列待办，指向本表。
> 状态：⬜未开始 / 🔄进行中 / ✅完成 / ⛔阻塞（外部 gate）

---

## M2 闭环线（回测/策略/因子）

| # | 事项 | 状态 | 说明 |
|---|---|---|---|
| 1 | 回测 API + 组（并行/串行）+ 实时可视化 | ⬜ | 两层级(run+symbol)，单只/组统一。详见 docs/回测组设计.md + docs/回测实时可视化设计.md + 记忆 backtest-visualization。子任务 #18/#19/#20/#21/#22 |
| 2 | 因子 API（`GET /api/factors`） | ⬜ | 因子注册表就绪，只差端点 |
| 3 | 策略创建/编辑表单（配置驱动，阶段一） | ⬜ | Factor 选择+权重+DSL 编辑器+参数表单，生成 ActionSignal 契约（volume_type/price_type）。详见记忆 strategy-framework-architecture |
| 4 | 策略实盘化（XTPAdapter 接入真实策略端到端） | ⬜ | 实盘对接已完成。架构定：每策略独立子进程（systemd `quant-strategy@<id>` + 修正版 B），入口 `src.strategy_runner`，详见记忆 strategy-live-architecture。子任务：strategy_runner 入口 / systemd 模板 / polkit(#14) / 行情驱动(XtpGateway实时+PG暖机，已定) |
| 15 | 高级模式 Python 代码框（阶段二，未来） | ⬜ | DSL 不够时加（ML/复杂状态机）。前提：安全沙箱+风控网关+ActionSignal+子进程隔离。详见记忆 strategy-framework-architecture |
| 18 | BacktestEngine 加每 bar 回调 | ⬜ | on_bar_callback(hook) 推 progress/bar/trade/position/equity/log（Celery publish Valkey）|
| 19 | migration 0010 回测组两层级表 | ⬜ | backtest_runs(任务级)+backtest_symbols(标的级)+pools+pool_symbols(标的池)。替代单表 backtest_tasks，单只是组N=1 |
| 20 | 前端回测组页面 | ⬜ | BacktestList(run列表)+BacktestRun(任务详情+汇总+标的排名+选中展开)+BacktestView(单只可视化)+Pool.vue完善(池内标的) |
| 21 | 回测绩效扩展 | ⬜ | 补 α/β/索提诺/信息率/波动率/基准收益（现有净值/胜率/回撤/夏普）|
| 22 | 标的池后端 + 回测汇总 | ⬜ | pools/pool_symbols 端点 + 回测组汇总指标(平均+排名)计算 |

## 实盘/风控线

| # | 事项 | 状态 | 说明 |
|---|---|---|---|
| 5 | 实盘对接（XTPAdapter + 三级开关） | ✅ | 2026-08-03 服务器验证通过（下单成交+撤单+查询全接口） |
| 6 | 实盘持仓/盈亏对接 Dashboard | 🔄 | query_account/position 已通，Dashboard 占位未接真实数据 |
| 7 | 三账对账 API（`GET /api/reconcile`） | ⬜ | 风控闭环需要（broker/策略/系统三账） |
| 8 | data_continuity_check 定时任务 | ⬜ | 历史遗留 bug（`expected=5` 写死不准） |
| 14 | systemd polkit 配置（Web 控制 quant-strategy@*） | ⬜ | polkit 规则允许 quant 用户 `systemctl start/stop/restart quant-strategy@*`，策略实盘化前置基础设施（详见记忆 strategy-live-architecture） |
| 16 | 飞书机器人权限界面设置 | ✅ | register_app 默认最末级权限，界面要能设置/提升 scope（研究飞书 API 改 scope 或引导开放平台配） |
| 17 | 飞书多机器人配置 | ✅ | feishu_config 多行 + 每机器人独立长连接（systemd quant-feishu-bot@<id>）+ 界面列表管理（增删/启停） |

## 运营/可视化线

| # | 事项 | 状态 | 说明 |
|---|---|---|---|
| 9 | 数据完整性看板 | ⬜ | 每标的完整性百分比（预期 vs 已有天数） |
| 10 | Dashboard 量化指标（PnL/Sharpe/最大回撤） | ⬜ | 总览看板从占位变真实 |
| 11 | 场景串联（选股->标的池，回测->实盘一键部署） | ⬜ | 打通功能孤岛 |
| 12 | i18n nav 翻译补全 | ⬜ | 菜单当前直接中文，未走 t() |

## 安全/质量线

| # | 事项 | 状态 | 说明 |
|---|---|---|---|
| 13 | 密码改 bcrypt | ⬜ | `auth.py` 现用 sha256，`passlib[bcrypt]` 已装没用上 |

---

## LLM 网关简化线（2026-08-07 决策）

> 评审 `docs/LLM网关设计.md` + 代码核实后简化。详见 `flow/decisions.md` 2026-08-07。

| # | 事项 | 状态 | 说明 |
|---|---|---|---|
| L1 | 移除 tier 机制 | ✅ | 删 `Tier` 类型 / `llm_model_config.tier` 字段 / `chat`+`chat_stream` 的 `tier` 参数 / `_resolve_model(tier)`；`_load_models_from_db` 返回按 `priority` 全局排序列表，主备取前两个 |
| L2 | 移除语言注入（+飞书同步去 lang） | ✅ | 删 `_inject_lang` / `lang` 参数；飞书端同步：`feishu_config.lang` 字段（migration）+ `bot.py` 传 lang + 前端设置弹窗 lang 项 + `register_app` 传 `navigator.language` |
| L3 | 修复工具过滤越权漏洞（P0.3） | ✅ | `_filter_tools` 传入 `tools` 时与角色白名单取交集（当前传入 tools 覆盖白名单，viewer 可越权 emergency_halt） |
| L4 | 熔断并发安全（P0.4） | ✅ | `_failed_counts`/`_last_fail_time` 加 `threading.Lock`（单例+线程池并发竞争） |
| L5 | gateway Role 对齐 RBAC 四角色 | ✅ | `viewer/operator/admin` -> `viewer/analyst/trader/admin`；`_filter_tools` 细化（trader: READ+halt+strategy_start/stop；admin: +resume）。当前 trader 被当 viewer 不能 halt（违反"Trader 可熔断"） |

## 平台化通用架构线（2026-08-08 决策）

> 系统平台化：6 大接口抽象（DataSource/Broker/MessageChannel/Task/RiskRule/LLMProvider）+ 管理设置类菜单通用化。业务菜单保留。详见 `flow/decisions.md` 2026-08-08。实现可简化，结构接口按通用方向。

| # | 事项 | 状态 | 说明 |
|---|---|---|---|
| PT1 | 后台任务管理（统一 tasks/task_logs + 卡死检测 + 强制删除 + 故障定位） | ✅ | P0 平台化核心，回测/同步/AI/策略统一纳入。复用 SyncLock 心跳 |
| PT2 | AI 用量监控看板（llm_usage 前端图表 Token/成本/延迟/预算） | ✅ | P0 数据现成（migration 0011），前端看板 |
| PT3 | DataSource 接口 + 数据源配置 DB 化（Tushare .env->DB） | ✅ | P0 平台化数据层，DataSource 基类 + Tushare 实现 |
| PT4 | MessageChannel 接口（统一 alert_notify + 飞书到统一接口） | ✅ | P1 平台化消息层 |
| PT5 | Broker 接口 + 交易通道 DB 化（XTP .env->DB） | ✅ | P1 平台化交易层，ExecutionAdapter 基类已有 |
| PT6 | RiskRule 接口 + 规则 DB 化 | ✅ | P1 平台化风控 |
| PT7 | 跨层联动（限额->任务失败->高亮->统一告警） | ✅ | P2，PT1 后台任务管理后做 |
| PT8 | 管理设置类菜单重组（系统设置集中配置类 + 通用化命名） | ✅ | P2，UI 归类理顺（业务菜单不动） |

## 平台化集成（PT1-PT8 接口已建，现有代码迁移）

> 接口抽象已落地（PT1-PT8 ✅），但现有代码仍用旧实现（硬编码/.env）。迁移到新接口强化平台化，避免接口与实现两套。

| # | 事项 | 状态 | 说明 |
|---|---|---|---|
| PI1 | alert_notify 迁移到 MessageChannel | ⬜ | `_channels` 硬编码 -> channel_config DB（PT4），alert_notify 用 get_channel |
| PI2 | risk_control 迁移到 RiskRule | ⬜ | DEFAULT_RULES 硬编码 -> risk_rules DB（PT6），check_order 调 load_rules_from_db |
| PI3 | XTPAdapter 凭证迁移到 Broker DB | ⬜ | .env -> broker_config（PT5），strategy_runner 用 Broker.get_credentials（影响 #4） |
| PI4 | 回测组纳入 tasks 表 | ⬜ | #1/#19 实施：backtest_runs.task_id 关联 tasks.id（PT1 统一任务管理 + 卡死检测 + 告警） |
| PI5 | i18n 含新菜单 | ⬜ | #12 补：数据源/消息通道/交易通道/风控规则/后台任务/系统设置/AI 模型（PT8 重组） |

## 外部 gate（不阻塞开发，影响实盘/上线）

| 事项 | 状态 | 说明 |
|---|---|---|
| 中泰 XTP 门槛/品种放行/费率 | 🔄 | 测试账户已能交易（2026-08-03 验证），正式账户门槛/品种待客户经理确认 |
| Tushare 接入 | ✅ | 积分 200，日线够用（历史数据管理）；分钟线从 XTP 测试平台获取 |
| DeepSeek/GLM 具体型号 | ⛔ | V4/GLM-5.2 的 context/supports_tools，接入按官方 |
| 币安/欧易 API 接口 | ⛔ | 未开通，加密实盘待接（开发用测试网/占位）|
| 中泰 XTP 正式账户 | 🔄 | 测试账户够开发，正式账户门槛/品种/费率待客户经理确认（影响生产实盘）|

---

## 已完成里程碑（参考）

- 2026-08-03 实盘对接全接口验证（XTPAdapter + 三级开关 + A 股进 astock 分项）
- 2026-08-03 vnpy_xtp 双环境编译（本地 cp310 + 服务器 cp311）
- 2026-08-02 vnpy_xtp 服务器编译成功（-j1 防 OOM）
- 2026-07-31 alembic schema 版本管理引入
- 2026-07-22 架构 12 份文档定稿
