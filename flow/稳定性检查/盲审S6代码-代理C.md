# 盲审 · S6 修订代码 · 代理 C（2026-08-18，实施级）

> 评审对象：commit 70ec2f9（断流不自杀+BUY 判定挪下单时刻+监控 Phase 1 中的 S6 部分）。
> 结论：**2 致命 2 严重，全数修复随同日部署**。F1 是新引入的回归（赋值顺序），F2 是被测试平台夜间回放证伪的设计假设。

## 致命
- **F1 开盘/午后首根 bar 的 BUY 确定性误拒**：`handle_msg` 中 `strategy.on_bar`（内含 place_order→buy_ok_check）先执行、`last_bar_wall` 锚后更新——下单时刻读到上一根 bar 的到达墙钟；09:31 首根锚是昨夜（>300s）→ 拒。修：锚+max_ts 去重水位移到 on_bar **之前**（事实先于副作用）。
- **F2 夜间回放窗口 BUY 真放行**："非交易时段天然无信号"假设在 XTP 测试平台（夜间回放白昼行情）为假——worker 傍晚重启后 max_ts 失忆不 R-DL1 去重，回放 bar 真实驱动下单；且 direct 模式根本没包 send_order。修：buy_ok_check 加交易时段门（时段外拒 BUY 属业务正确——A 股时段外委托不可成交）；direct 模式补同语义门（时段+tick 新鲜）。
- **S1 删自杀后"半开连接/线程挂死但主循环活着"失去唯一自愈**：watchdog 不触发（主循环活着照喂狗）、health_monitor 无 tick 流量规则。修：monitor 加 R6"交易时段 sess_ticks 零增长 ≥2 轮→critical"（告警抓手；自动重启留待数据说话后再议末档）。
- **S2 测试没锁核心机制**：纯函数测试过了但接线零覆盖，F1 恰是接线缺陷。修：补 session 参数/链条测试；沿检测抽 session_edge 共享三处。

## 陷阱核对（要点）
- 沿初值/重复触发正确；沿在循环粒度内迟到 ≤1 根 bar 观测盲区，自愈无害 ✓
- stale 告警标题含秒数致 notify 同标题去重永不命中 → 标题去秒数（挪 body）✓ 采纳
- 旧 worker+新 hub 混跑窗口安全（旧 worker 不解析新 hb 字段；gen+1 走既有重暖机）✓
- frozen["now"] 改名 blind：暂缓（hb 字段名兼容成本>收益，文档说明其纯观测语义）

## 简化机会（采纳）
session_edge 三处共享；告警标题去秒数。未采纳：锚与 sess_bar_wall 合一（保留双锚，语义清晰）。

（裁定：F1/F2/S1/S2 全确认全修，commit 见 2026-08-18 第二批）
