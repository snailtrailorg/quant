# 稳定性检查矩阵（P-1 骨架 · 2026-08-17 建）

> 方法论：`flow/规范/稳定性检查方法论.md`。本矩阵是检查的唯一覆盖账本。
> 图例：✅=已有机制（附证据）| ❌=发现缺陷（F-编号，P-6 汇总报告引用）| ⬜=待查（P-2/P-3/P-4 填）| N/A=不适用（必写理由）
> 已知发现来源：2026-08-17 七雷 + `docs/architecture/12-实盘稳定性设计.md` D1-D4/S1-S4/G1-G4。
> 盲审子代理结果**未并入**（P-4 合并差异项，防污染）。

## A. 组件 × 故障模式（核心矩阵）

### A1. live-task runner（现状：每任务一进程）

| 故障模式 | 结论 | 证据/说明 |
|---|---|---|
| crash | ✅❌ | Restart=always 自愈 ✅（12号雷5后验证）；❌ F-1 无 StartLimit → 无限重启风暴（今日 counter=59 实测）；❌ F-2 重启后 history 清零（bar_1min 空表，暖机无效）→ 因子窗口内策略行为漂移 |
| hang（停转） | ❌ F-3 | 无 WatchdogSec/sd_notify，无心跳（S1）；SIGSTOP 不死也不告警 |
| 变慢 | ⬜ | P-2：on_bar 内 PG 同步写延迟（信号落库）× PG 抖动阻塞行情线程；CPUQuota=50% 节流影响 |
| 部分失败 | ❌ F-4 | 下单"先发后写"崩溃窗口重复下单（D1，strategy.py:256 时序实锤） |
| 断连/分区 | ❌❌ | ✅ 每 10s 检查——**但是死代码**（`hasattr(gateway,"is_connected")` 而 XtpGateway 无此方法，xtp_gateway.py grep 零命中）；❌ **F-25 灾难级**：gateway `onDisconnected→login_server()` 单发自愈（xtp_gateway.py:291-297），隔夜/网络抖动一次失败即**永久断线永不再试**，且重连成功也不恢复订阅（无订阅登记）→ 表面健康实为永久失明；❌ **F-24 严重级**：`subscribe()` 在 login_status=False 时静默丢弃（xtp_gateway.py:447-450 无 else 无日志）→ 启动竞态=出生失明零报错；✅ PG 侧连接池 pre_ping+recycle 自愈（db.py:31-37） |
| 资源耗尽 | ✅❌ | MemoryMax=1G ✅（雷5）；❌ F-1 全局 OOM 随机受害者（D4，swap 只缓冲）；fd/连接数 ⬜ P-3 |
| 状态损坏 | ⬜ | P-2：history 列表并发访问（event 线程 append vs 主循环 warmup 重填 `history[:] =`）——**已知嫌疑，需代码确认是否有锁** |
| 时钟异常 | ⬜ | P-3：NTP 跳变对 BarGenerator 分钟切分/ts::date 查询影响 |
| 级联失败 | ⬜ | P-2：PG 挂 → runner 依赖面（live_task 读/status 查/snapshot 写/risk check） |
| 权限/配额 | ✅ | polkit 修复后 Web 启停全通（雷2/3） |
| 原生库异常 | ✅ | teardown ABRT 已修 os._exit(0)（雷6）；segfault 死锁 ⬜ P-5 注入 |

### A2. 下单路径（strategy→risk→XTPAdapter→gateway）

| 故障模式 | 结论 | 证据/说明 |
|---|---|---|
| 状态机完整性 | ❌ F-7 | order_log 无 status 流转（submitting→submitted→filled/cancelled/rejected 仅 submitted 一个值）；**trade_log 全库零写入**（#46）→ 成交/撤单/拒单状态无记录，positions 恒空（D2） |
| 部分失败 | ❌ F-4 | 同 A1 部分失败：发单成功+落库失败=无痕委托 |
| 断连窗口 | ⬜ | P-2：委托在途时 TD 断线，回报丢失谁推进？（XTP 回报可靠性 P-3 研读） |
| 幂等/重复 | ❌ F-8 | 无唯一 client order ref 对账；重启后同信号再发（D1/D3 复合） |
| 级联 | ⬜ | P-2：PG 挂时 check_order 异常语义（fail-closed？直接 raise？调用方捕获？） |

### A3. 共享行情 hub + 策略 worker（改造后，ST7）

| 故障模式 | 结论 | 证据/说明 |
|---|---|---|
| hub 单点 | ✅ F-9 | ST7 已实现：hub 看门狗四件套+worker 心跳检测冻结（失联 BUY 拒/SELL 放） |
| IPC 丢/乱/重 | ✅ F-10 | ST7：Valkey Streams 消费组+gen分区seq+ts双去重+XAUTOCLAIM 重投 |
| 陈旧数据交易 | ✅ F-11 | ST7：pub_ts 超龄丢弃+bar 停更 300s 冻结+暖机 upto截断防未来 |
| 旧 hub 复活（脑裂） | ✅ F-12 | ST7：租约 NX/CAS+INCR gen，worker 拒旧 gen——生产实测 2 轮 |
| worker 退出清理 | ⬜ | 订阅注销/残留检测，设计时定 |
| 其余 | ⬜ | 沿用 A1 全故障模式在 hub/worker 各自查一遍（P-6 需求书） |

### A4. web-api / celery-worker / celery-beat / feishu-bot

| 故障模式 | 结论 | 证据/说明 |
|---|---|---|
| crash/hang | ✅❌ | systemd Restart ✅；❌ F-13 无 OnFailure 告警（任何服务挂了没人知道）；beat 抢 schedule 文件冲突（今日两 beat 实测，已清幽灵） |
| 资源耗尽 | ❌ F-14 | web-api/celery 无 MemoryMax，无 OOMScoreAdjust 保护（D4：OOM 可能杀 web-api 而不是 runner） |
| 级联 | ⬜ | P-2：PG 挂 → web-api 每请求 500？celery 任务积压行为；Valkey 挂 → celery 全停？JWT 黑名单失效（登出失效）？ |
| 变更冲击 | ❌ F-15 | 部署=全量重启三件套（S2）+ 无交易时段闸门（S3） |

### A5. PostgreSQL / Valkey / 文件

| 故障模式 | 结论 | 证据/说明 |
|---|---|---|
| PG 断连/重启 | ⬜ | P-2：各进程的 psycopg 重连语义（get_conn 每次新建？池？超时？） |
| PG 锁/DDL | ⬜ | P-3：迁移 DDL 锁表时长 × 运行中 runner 查询互堵 |
| schema 漂移 | ❌ F-16 | verify_schema 只查表不查列（#48，0038 实锤） |
| Valkey 数据丢失 | ⬜ | P-3：重启丢哪些 key（celery 任务队列！db3 持久化配置？）；TTL 语义 |
| 磁盘耗尽 | ❌ F-17 | journal 无上限配置（G4）+ bar 表增长无分区/清理策略 |
| 备份恢复 | ⬜ | P-4：backup-db.sh 存在（#26）但恢复演练从未做过 |

### A6. 外部依赖（XTP-MD/TD、SMTP、飞书、Tushare、未来币安/OKX+代理）

| 故障模式 | 结论 | 证据/说明 |
|---|---|---|
| XTP 行情断连 | ✅❌ | 重连逻辑 ✅（A1）；❌ F-5 重订阅待验证；tick 停更告警 ❌ F-18（交易时段 last_tick 无监控） |
| XTP TD 会话 | ⬜ | P-3：单会话约束/被踢通知语义/查询节流限制；TD 断线时在途委托语义 |
| TD 会话互踢 | ❌ F-19 | 双 runner 同账户（D3，无唯一性约束 ST3） |
| SMTP | ✅ | 发件箱+退避+可观测（0d38702），送达≠成功已知 |
| 飞书 | ✅ | 长连接+systemd；⬜ 断连重连行为 P-2 |
| Tushare | ✅ | 积分/限频已知；同步失败有 data_continuity_check |
| 币安/OKX+代理 | ❌ F-20（设计需求） | 大陆访问必须代理 → 代理稳定性=实盘前提（ST6 注记） |

### A7. 配置与变更（systemd/polkit/.env/fstab/nginx/alembic/部署脚本）

| 故障模式 | 结论 | 证据/说明 |
|---|---|---|
| unit 错误 | ✅ | Requires 服务名/polkit 加载/双模板错位已修（雷1/2/双模板） |
| 幽灵实例 | ✅ | 已根治+DB 真相源+快照输出（雷4）；⚠️ 残余机制文档化 ⬜ P-3（裸模板实例化规则的权威解释） |
| .env | ❌ F-21 | ENCRYPTION_KEY 未独立（#47）；.env 无版本管理（P4 里 rsync exclude）——漂移风险 ⬜ P-4 |
| 部署脚本 | ✅❌ | 防僵尸校验 ✅、michael 权限 ✅（雷3）；❌ F-15 闸门/选择性重启缺失（ST5） |
| 迁移 | ✅❌ | alembic 唯一真相源 ✅；❌ F-22 交易时段 DDL 无闸门；列漂移 F-16 |
| swap | ✅ | 已加+fstab；监控 F-18 相关（swap 水位无告警） |

## B. 生命周期专项（跨组件的状态转移）

| 实体 | 关键转移 | 结论 |
|---|---|---|
| 订单 | 意图→WAL→提交→部分成交→终态→对账 | ❌ F-4/F-7/F-8（启动对账、状态流转、幂等 ref 三缺） |
| 任务 | pending→running→stopped/error→删除 | ✅ 停止语义已修（雷7）；⬜ error 态谁写入？删除时 unit 残留/进程残留？P-2 |
| XTP 会话 | 断开→重连→**状态恢复**（重订阅/重查询/对账） | ❌ F-5 实锤+F-24/F-25：单发重连/无重订阅/is_connected 死代码/启动竞态丢订阅（P-2 vnpy_xtp 源码核）；查询恢复 ⬜ P-3 |
| bar 序列 | tick→bar→history→因子 | ❌ F-2（重启清零+空表暖机）；跨日边界 ⬜ P-3 |
| snapshot | query→写库→API 读 | ✅ 链路已验证；停更检测 ❌ F-18 |

## C. 时间尺度专项

| 尺度 | 已知缺口 |
|---|---|
| 秒级（下单/崩溃窗口） | F-4/F-8 |
| 分钟级（bar/快照/心跳） | F-2/F-3/F-18 |
| 日级（时段开闭/收盘对账） | ⬜ P-2：runner 对 11:30-13:00/收盘后行为（空转？重连风暴？）；收盘四账对账自动化 ❌（12号 §3 有清单未实现） |
| 变更时刻 | F-15/F-22 |

## E. P-4 盲审合并差异项（子代理 A，2026-08-17，全文见 `flow/稳定性检查/盲审报告-子代理A.md`）

**收敛项**（双方独立发现，置信度↑）：F-4/F-7/F-8/F-1/F-2/F-23/F-24/F-25、history 竞态（⬜→❌实锤）、hub 架构 F-9~F-12。
**新增项**（我方遗漏，编号续接；✅核=主会话已独立复核）：

### 灾难级（10 项）
| # | 发现 | 复核 | 备注 |
|---|---|---|---|
| F-26 | **vnpy EventEngine 事件线程零异常保护**（engine.py:61-72 只捕 Empty）——任何 handler 异常=永久静默失聪，**一切下单路径异常的共同放大器** | ✅核 | 系统性放大器① |
| F-27 | send_order 不查返回值：不支持的品种/断线时 gateway 返回 ""/伪造"0"，失败记成功→账面有单实际无单 | ⚠️ | 账实分离 |
| F-28 | Signal 默认 volume=0/price=0 + 聚合无归一化（RSI 0-100 对 ±0.3）+ max_trades_per_day/single_position_pct 从未检查 → **每根 bar 一单洪水** | ✅核（默认值+金额only） | 与 F-7 复合 |
| F-29 | **风控 _get_global_state fail-open**：PG 异常返回全零 RiskState——故障时回撤/日亏限制恰好消失 | ✅核 | 系统性放大器② |
| F-30 | 规则部分 dict（只建 global 不建 etf_conv）→ check_order KeyError → 杀事件线程（复合 F-26） | ✅核（下标访问） | |
| F-31 | **日亏限额拦死 SELL**：文案"仅平不开"，实现 approved=False 无差别——**无法止损** | ✅核 | |
| F-32 | JWT 默认密钥 quant-dev-secret-change-me 仅告警 + ENCRYPTION_KEY 派生链 → 伪造 admin token 全控+解密券商凭证 | ✅核 | 安全 |
| F-33 | **飞书 card/callback 零鉴权**可执行 emergency_halt/risk_resume/strategy_start（若 nginx 暴露 /lark） | ✅核（nginx 暴露面待确认） | 安全 |
| F-34 | account_snapshot 无任务维度混写 + 断线 fallback 写 initial_capital 当总资产 → 风控数据源被污染（回撤"归零回正"） | ✅核（fallback 分支） | 放大器②的燃料 |
| F-35 | Valkey **db0 大混装**（celery broker+熔断+锁+JWT 黑名单同库，.env:8）——一次故障全带走 | ✅核 | |

### 严重级（24 项，⚠️=引用可信未逐行复核）
F-36 已停任务 exit(0) 仍被 Restart=always 拉起→churn 循环（systemctl 失败被吞时）⚠️；F-37 停止延迟 60s+break 后 tick handler 仍可下单⚠️；F-38 BarGenerator 尾 bar 次日才触发+last_price=0 tick 静默丢⚠️；F-39 cancel_order 退化路径错 orderid⚠️；F-40 查询断线静默 no-op 返陈旧值⚠️；F-41 熔断不撤在场单⚠️；F-42 ETF 前缀表缺 588/58 绕分项开关⚠️；F-43 杠杆检查死代码+price=0 绕金额上限⚠️；F-44 verify 端点一键绕过回测门禁⚠️；F-45 禁用用户 token 24h 内仍有效⚠️；F-46 admin/admin123 恢复后自动复活⚠️；F-47 beat schedule 放 /tmp→重启全任务齐发风暴⚠️；F-48 单 worker 承载全队列→长同步饿死 risk/email 巡检⚠️；F-49 tushare 异常吞成空数据+暖机无新鲜度校验⚠️；F-50 reconcile 对 order_log 恒 submitted 的历史单永真告警⚠️；F-51 同步游标在未来→永久静默停摆⚠️；F-52 notify 依赖 Valkey——告警与被监控者共死⚠️；F-53 deploy 重启清单不含 live-task→版本劈叉⚠️；F-54 迁移在旧 runner 仍写旧 schema 时执行⚠️；F-55 factor:recalc 全局键单消费者抢删⚠️；F-56 client_id 共享+SDK 目录并发互写⚠️；F-57 用户 Python 策略无超时无包裹（while True 即失聪）⚠️；F-58 CORS * + credentials⚠️；F-59 verify_jwt 每请求建连，Valkey 挂=认证全瘫⚠️。

### hub 架构需求书增量素材（子代理 §4 精要）
盘中暖机应向 hub 回放（PG 分钟线是盘后同步的，盘中重启拿昨日数据→因子全错）；迁移期"先全停旧 runner 再起 hub"+hub 独立 client_id；熔断/撤单责任在 hub 与 worker 间显式划界；bar 带 (symbol,ts) 去重+序号+fencing token 三件套是硬前提。

## F. 修复状态（随批次更新）

| 批次 | 覆盖 F 项 | 状态 |
|---|---|---|
| SE1/SE2/SE3（2026-08-17 完成+部署+生产验证） | F-35（celery broker/backend 改读 CELERY_BROKER_URL/RESULT env，不再与业务 db0 混装；服务器迁 db3 待跑）/F-52（notify 去重降级：Valkey 挂跳过去重继续发）/F-47（beat schedule 出 /tmp → server/runtime/ 持久化，EXCLUDES 加 runtime/）/F-48（quant-celery-risk@ 专属 risk 队列 worker，告警/巡检不再被长同步饿死）/F-15/F-53/F-54（deploy 交易时段闸门 FORCE_DEPLOY=1 覆盖 + 代码指纹无变化跳过重启 + 代码变更自动重启实盘任务） | ✅ 全部收口：服务器 celery 实跑 db5/6（代码激活既有配置）；risk worker active+enabled；闸门/指纹选择性重启生产首验通过（代码无变化⏭️跳过三重启）；附带修正 clear-redis 默认 db2/3→4/5/6（防清错库） |
| SD1/SD2（2026-08-17 完成+部署+服务器修复） | F-32（默认 JWT_SECRET×实盘开关=拒绝启动；服务器实测拦截成功，已换 48 字节随机密钥）/F-46（实盘模式 admin 密码随机生成）/F-45（verify_jwt 查账号状态，禁用/注销 token 即时失效，users 不可读 fail-closed）/F-33（卡片 ts 60s 时效+event_id 5min 去重，无 ts 旧卡拒绝）/F-44（verify 需真实回测证据：run_id 校验或存在 done 回测）/F-58（CORS 白名单化，env 可覆盖） | ✅ 已部署；test_sd_security.py 13 例+回归 223 过。ENCRYPTION_KEY 已在服务器固化为旧派生值（XTP 凭证零断损），#47 留独立轮换工具待做 |
| SC1/SC2/SC3（2026-08-17 完成+部署） | F-4（WAL：submitting 先落库再发单，记账失败 fail-closed 不发单）/F-27（gateway 假委托号 ""/"XTP.0" 识别为失败→send_failed）/F-7（order_log 状态流转 submitting→submitted/send_failed + trade_log 成交写入 #46，EVENT_TRADE 落库 trade_ref 幂等）/F-8（bar 级幂等去重 + client_order_id 对账键）/F-28（源头拦截 0 量 0 价 + max_trades_per_day 实装）/SC2 启动对账（在场委托可见化+WAL 残留 submitting 告警+当日成交补录） | ✅ 已部署；test_sc_orders.py 8 例+回归 210 过。**遗留**：聚合归一化（F-28 后半）=策略语义变更需回测重验，转设计决策；single_position_pct 待 ST2 position_snapshot |
| SB1/SB2/SB3（2026-08-17 完成+部署） | F-29（_get_global_state fail-closed+无数据拒单）/F-34（runner 不再写假值快照）/F-31（日亏放行SELL）/F-41（熔断沿撤在场单）/F-30（规则DEFAULT合并永完整）/F-23（60s热加载）/F-42（ETF补58前缀）/F-43+F-28风控层（0价0量拒单） | ✅ 已部署；测试 test_sb_risk.py 14 例+回归202过 |
| SA1/SA2/SA3（2026-08-17 完成+部署） | F-26（我方 handler 全包异常+线程死亡检测退出）/F-57（异常面，超时待hub）/F-30（异常不再杀线程）/F-18（120s 告警+300s 退出）/F-24/F-25（订阅幂等重放+死代码清除+新鲜度驱动）/F-1（StartLimit 5/300s）/F-3（WatchdogSec 90s+sd_notify）/F-13（OnFailure→notify 飞书） | ✅ 已部署，待 P-5 注入实测；测试 test_sa_hardening.py 9 例 |
| SF1 长尾（2026-09-03 完成，未上产） | F-36（已停任务 exit(0) 不再被 on-failure 拉起，历史核实）/F-39（cancel_order 退化剥前缀得纯 orderid+非纯数字放弃盲撤）/F-40（query_account 断线清缓存返回空，不污染 account_snapshot）/F-51（同步游标在未来钳回 now，不再永久停摆）/F-55（factor:recalc 多 worker 各记 last_seen 不删全局键）/F-59（verify_jwt 复用连接池+Valkey 挂降级放行，不再认证全瘫） | ✅ 已修+测试 test_sf1_longtail.py 6 例+全量 789 绿；**知情接受**：F-37（5s 停止窗口）、F-38（last_price≤0 静默丢=vnpy 同款）、F-49（暖机靠 buy_ok_check 间接缓解）；F-50/F-56 见下一行 |
| F-50/F-56（2026-09-03 完成，未上产） | F-50（order_log 加 vt_orderid 列迁移 0063 + write_trade_log 重启后 vt_orderid 反查 + 「委托不成交」口径收紧排除 send_failed）/F-56（worker TD 独立 client_id=runner_client_id(task_id) 派生 2-99 防多任务撞号；SDK 目录判定不修——MD 写 quote.log TD 写 trade.log 文件名天然区分） | ✅ 已修+test_vt_orderid_fallback_resolves_order+test_runner_client_id_derivation；全量 791 绿 |

## D. 覆盖率统计（P-4/P-6 更新）


- **P-4 盲审合并后**：❌ 发现缺陷 **F-1~F-59（59 项）**，其中灾难级 18 项、严重级 ~33 项、设计需求 4 项
- 双盲收敛项 9 个（F-4/7/8/1/2/23/24/25+history 竞态）——独立枚举互证，置信度高
- 盲审新增 34 项中灾难级 10 项（8 项已独立复核 ✅核）
- ⬜ 待查剩余：~15（P-3 工具语义：XTP 回报可靠性/Valkey 持久化/NTP/systemd 细节；P-5 注入待做）
- 下一步：P-3 工具语义 → P-5 注入实测（F-26/F-25/F-28 可注入复现）→ P-6 汇总报告+hub 需求书+修复批次
