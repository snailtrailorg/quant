## 2026-09-24 · venue→account 部署事故（部署波次清单缺口 + RENAME 失败回滚致代码/schema 分裂）

1. **部署波次清单必须覆盖动态实例**：hub 波次 units 硬编码 `[quant-md-hub@quant.service]`，漏 D6 的加密 per-account hub 实例（@4/@5）——全量部署时加密 hub 钉旧版未重启，版本收敛断言失败触发回滚。规约：**新增「动态实例」服务（per-account/per-task 模板单元），deploy_waves 波次清单必须同步纳入或改动态采集**（task 波次已用 dbro 动态采集 live 实例，hub 应同法）。
2. **RENAME 破坏性迁移的失败回滚会造代码/schema 分裂**：allow_contract 的 rescue 只翻代码链不回滚 schema，RENAME 已前进 → 代码回旧版读旧列名报 `column does not exist` 生产分裂。规约：**RENAME 类迁移部署失败后不能停在回滚混合态**——要么先修好波次清单确保成功，要么失败后立即手动翻链回新版恢复一致。

## 2026-09-24 · 加密接入批双盲审两坑（日终 flush 在 24/7 截断 + 缺省选行市场无感知）

1. **「日终兜底」语义不能直接搬到 24/7**：`flush_rest`（pop 全部桶）是 A股日终语义（15:01 时无活跃桶所以安全），加密 24/7 复用它会每 5 分钟把「当前分钟刚开的活跃桶」提前 finalize → 同 symbol 同 ts 重复 bar（全量 bar 被 worker 当 dup 丢、半熟 bar 当可信）或 untrusted 误冻结。规约：**跨时段复用聚合器的收口函数，先想清「此刻有没有在途桶」**——24/7 要 flush_stale（只收 `minute < 当前分钟` 的陈旧桶），不碰在途桶。
2. **缺省选行必须按市场过滤**：`get_interface_row(None)` 的「域内 position 首行」在多市场（A股 + 加密）下，加密行 position 低时会污染 A股 hub（A股 hub 跑加密网关）。规约：**缺省选行加 `market='astock'` 过滤**；指定行（row_id=account_id）才跨市场直取。另：MD 数据面（hub）用空凭证，`required_fields` 校验（为 A股 B 实例「禁 .env fallback」设的）会误拒加密 MD 空凭证——`get_interface_row` 加 `md_only` 标志跳过。

## 2026-09-24 · D6 双盲审一坑（环境变量复用两个语义互相污染）

1. **一个环境变量不能承载两个语义**：`HUB_INTERFACE_ROW` 原本语义是「A股切换时 B 实例选另一 XTP 账号」（broker.py 注释实证），D6 复用它做「account 分键」——A股多账号切换时 quant2 注入数字 HUB_INTERFACE_ROW 会让 account_id=该行 id → 键分叉，与 A（全局键）不同键，切换握手失效；且 A股实例名 "quant" 非数字，`int("quant")` 直接 ValueError 崩（systemd 无限重启）。规约：**环境变量语义单一，新语义开新变量**（拆 ACCOUNT_ID 与 HUB_INTERFACE_ROW）；解析数字环境变量必须 `.isdigit()` 兜底（非数字=该语义不适用，不是崩溃）。另：模板实例名 %i 同时服务「名字（quant/quant2）」和「数字（account_id）」时，靠名字形似判断会误伤——停非期望单元要「解析纯数字 id」而非「名字像 hub 且 ≠ 单例」。

## 2026-09-24 · D5 双盲审一坑（fail-fast 异常未捕获 → exit 1 而非 78 重启风暴）

1. **永久配置错误 fail-fast 必须 exit 78（EX_CONFIG）而非裸 traceback exit 1**：`get_interface_row(row_id=...)`/`build_xtp_setting(row_id=...)` 的 docstring 写「消费方 exit 78」，但消费方 main.py 没 catch，raise 以未处理异常崩溃=exit 1。而 `quant-live-task@.service` 是 `Restart=on-failure` + `RestartPreventExitStatus=78`——exit 1 不在豁免集，systemd 每 30s 重启 churn 到 StartLimitBurst 才 Failed（本应单次干净退出 78 直接 Failed + OnFailure 告警）。规约：**所有「永久配置错误」fail-fast 点（读接口行/组装网关/建适配器）必须显式 try/except → `sys.exit(EX_CONFIG)`，不能靠 raise 让进程裸崩**；退出码分类（78=配置错不重启 / 75=瞬态重启 / 0=正常）要和 systemd RestartPreventExitStatus 对齐。另：守门立法「禁 if provider== 硬编码」是 M3 起的架构约束，TD 网关这类「各 provider 组装不同」的分支要走注册表（`_TD_BUILDERS` 映射 builder 函数），不能硬编码。

## 2026-09-24 · D4 双盲审两坑（枚举 value 落库飘值 + 总资产对账误报）

1. **枚举 `.value` 是 gettext 翻译文本，落库该用 `.name`（资源 id）**：vnpy `Direction` 枚举源码 `LONG = _("多")`（gettext），`.value` 随 locale 飘（中文"多/空/净"或英文"Long/Short/Net"），`.name` 才是稳定 `LONG/SHORT/NET`。adapters.py 原用 `.value` 落库 → 读方按 `'short'` 过滤恒真失效。规约：**枚举落库存资源 id（`.name` + `direction_` 前缀防全局冲突），显示层才做多语言**；gettext 的 value 是展示层用的，不是存储层用的。
2. **资金对账该用现金口径（available_cash）非总资产（total_value）**：total_value=现金+持仓市值，浮盈浮亏会让「本地推导 vs 券商」恒有差异（1 元阈值拦不住，系统性误报）。规约：**余额核对类对账选「不随未实现盈亏漂移」的口径**——现金对现金、持仓对持仓，别拿总资产对总资产（除非两边同时扣同一市值）。另：对账推导要按时间窗过滤成交流水（baseline 之后），否则历史成交双计/错位。

## 2026-09-24 · D3 双盲审一坑（SQL 模板 ORDER BY 结尾时 append AND 拼错位）

1. **SQL 模板以 ORDER BY 结尾，追加 AND 子句不能直接 append**：`BAR_TABLE_SELECT` 模板以 `ORDER BY ts ASC` 结尾，`select_sql += " AND source = %s"` 拼成 `... ORDER BY ts ASC AND source = %s`（PG 语法错）。且 mock 测试只断言 `"AND source = %s" in sql` 字符串包含，对「AND 在 ORDER BY 前/后」两种形态同为绿——正撞「行为级验收不可用 mock 绿替代」教训。规约：**拼接 SQL 追加条件用 `.replace("ORDER BY", "AND ... ORDER BY")` 或模板加 `{clause}` 占位符；回归钉断言 `sql.index("AND ...") < sql.index("ORDER BY")`**（位置断言，非仅字符串包含）。

## 2026-09-24 · D2 段2 双盲审三坑（写侧未接线 + NULL JOIN 语义 + account 单一真源）

1. **迁移加列后写侧 INSERT 必须同步接线**：0099 给 order_log/trade_log 加 account_id（可空），但 write_trade_log/_log_signal_order 的 INSERT 没补 account_id → D2 后所有新成交/委托 account_id 恒 NULL；而 position_snapshot.account_id 经 0100 回填 NOT NULL 恒非空。reconcile 的 FULL OUTER JOIN 每标的拆两行（NULL vs 非 NULL 不相等）→ 对账假阳性告警洪泛。规约：**给表加列（尤其可空）时，全仓 grep 该表所有 INSERT 点同步接线，不只改读方**；「回填历史 NULL」≠「写侧自动写新值」。
2. **`IS NOT DISTINCT FROM` 只在两侧都 NULL 时命中**：写注释「IS NOT DISTINCT FROM 保留 NULL 历史行」是误读——该算子要求 NULL↔NULL 才相等，NULL↔非 NULL 仍不命中。规约：**JOIN 键含 NULL 时先想清 NULL 分组语义**（NULL=未知 account 该归哪侧），不能靠注释自证。
3. **account_id 单一真源**：check_order 内三处取 account 源分叉（形参 vs `order.get("account_id")`）——当前唯一调用方两处同源侥幸一致，但依赖「参数与 order 键必须相等」的未约束约定，任一调用方漏设 order 键即 single_position_pct 静默 fail-open。规约：**同一数据一处真源**（形参是唯一真源，order 键仅透传），分市场检查复用已算的 state 免二次查库。

## 2026-09-23 · 批 63 EMQ 接入三坑（310 泄漏 + trampoline 参数 + rpath 转义）

1. **本地编译产物 .so 泄漏生产**：synchronize 从控制机文件系统 rsync（不看 .gitignore），本地 build.sh 验证编出的 `cpython-310.so` 随 src 切片同步进生产 release（生产 3.11 只认 311，310 冗余无害但脏）。规约：**ABI 绑定解释器版本的编译产物必须由目标环境编译生成，synchronize 源切片要 exclude 掉**（`--exclude=*.so` 仅 src 切片；vendor 切片的 SDK .so 保留——白名单切片要 per-item exclude 标记，不能全局 exclude 否则砍掉 vendor .so）。
2. **pybind11 trampoline 改参数量不能用 PYBIND11_OVERRIDE**：EMQ `OnDepthMarketData` 基类 7 参（含裸 int64_t[] 队列数组，pybind11 无法转换），Python 覆写只透传 3 参（结构体+两计数）——PYBIND11_OVERRIDE 会按覆写参数回退调基类同名方法（参数不匹配编译失败）。规约：**改参数量的覆写用手动 `py::get_override` + try/catch**（`discard_as_unraisable` 兜底，防 Python 异常穿透 SDK 回调帧 SIGABRT 全进程崩——SDK 守卫铁律，对标 XTP GuardedXtpMdApi）。
3. **rpath 转义是 `$` 不是 `$$`**：bash 单引号内 `$$` 不展开，链接器收到字面 `$$ORIGIN`（rpath 失效仍要 LD_LIBRARY_PATH）。规约：单引号内传 `$ORIGIN` 用单个 `$`。多路径 rpath 用冒号分隔（`$ORIGIN/...:/opt/rh/gcc-toolset-13/root/usr/lib64`），本地不存在的路径动态链接器自动忽略 fallback 系统 libstdc++，无副作用。

## 2026-09-22 · 批 60 M5 上产一坑（staging 彩排红：Prevent(3) 打死部署波重启）

1. **退出码进 Prevent 前必须枚举它的全部触发路径**：exit(3) 真让位有两条路径——①切换窗复活实例拦截 ②**正常重启的 30s lease 滞后窗**（SIGTERM 不 DEL lease，旧 lease TTL 内新进程 NX 失败 exit(3)，靠 on-failure 30s 重拉自愈——这是历史部署能过 dwell 的机制）。v15 只看到路径①把 3 放进 RestartPreventExitStatus，砍断了路径②的自愈 → 首次 staging 彩排红（hub 波重启→exit(3)→不重拉→永久死→dwell 判死自动回滚）。规约：**改退出码矩阵时先画全每码的触发路径表，一码多路径的「Prevent 决策」必须逐路径判定**；v15 的切换窗拦截其实已由 boot 闸 exit(6) 独立承接，exit(3) 退回重启码零损失。定位通道：hub 的 system_log 落库（journalctl 无权限时的救命通道）——`租约被持有（e0cc...），本实例让位退出` 一行定根因。另证：**部署窗闸拦 prod（08:56:53>8:55 截止）机制正确**，越窗须用户显式 `-e force=true` 裁定。

## 2026-09-21 · 批 58 真库对照两坑（bar 表共享 + adj_factor 限速遗漏）

4. **共享表对照必须按品类过滤**：bar_daily 三品类（stock/etf/convertible）共享 bar_1d 表，对照脚本 `DELETE FROM bar_1d WHERE <窗口>` 会误删其他品类，新旧快照错位（cb 对照删了股票/ETF）。规约：多 sync_id 共享一张表的对照/清理，**按 (kind,sub_kind) 用静态表 join 过滤 symbol 集合**（`asset_static_info`/`etf_basic_info`/`cb_basic_info` 的 ts_code→vt_symbol），不能裸删整表窗口。
5. **adj_factor 拉取无限速=限流遗漏 bug**：旧路径 `_daily_to_save_fn` 的 `pull_adj_factor` 在 rate_limit_context 外（归因拆分把 save_fn 整体放外，连带 adj_factor 网络拉取也丢了限速），连续 backfill 触发 Tushare 侧限流返回空→因子 NULL。规约：**每个网络拉取接口都要有自己的限速档**（backfill_adj_factor 已用 adj_factor 档，`_daily_to_save_fn` 却漏了）；收编 fetch 时把 adj_factor 拉取移入 rate_limit 内，也要**对齐独立档位**（adj_factor 档而非借用 daily 档）否则限速行为漂移。另：对照脚本连续跑新旧路径共 2N 次网络调用，超外部限流配额会导致"谁后跑谁失败"的非确定性差异——**真库对照要隔离外部限流**（低频/加 sleep/mock）。

## 2026-09-21 · 批 58 通用引擎循环三坑（双盲审 A/B 抓出）

1. **handler 式函数「无前置」必须 raise 不能 return error status**：`_sync_via_kind` 无 sync_kind_config 归置行时原 return `{"status":"error"}`，但 `sync()` 不读 `r.get("status")`，按缺省 failed_dates=[] 判 success 且无 last_success_date → 无条件推进游标跳过整个窗口=数据丢失（正是老 H-S1 防的那类假成功）。规约：回给 `sync()` 的 dict，**任何「不该继续」的失败一律 raise**（sync() 外层 except 收编→error+不动游标），不 return error status 赌调用方检查。
2. **行为等价对照 `SELECT *` 误含代理键**：bar 表有 `id BIGSERIAL`（INSERT 显式列不含 id），对照脚本两跑之间删窗口重插，序列不重置 id=N+1..2N，`SELECT *` 比对必误报不等价=验收工具永久失效。规约：快照/对照只 SELECT 契约列（BAR_COLUMNS 11 字段），代理键/摄取时间戳/dataset_version **落到 SELECT 列清单里显式剔除**，不能靠「表里没这列」的假设。
3. **adapter 内联富化要守空数据短路**：fetch 收编 `_daily_to_save_fn` 时把 `pull_adj_factor` 从旧路径「df 非空才调」漂移成无条件调——节假日 freq=B 拉到空 df 仍多打一次因子接口。规约：收编旧路径时逐条对齐「空数据短路」条件，不只对齐正常路径。

## 2026-09-20 晚 · 批 56a 盲审两坑（P0 根因+双盲交叉价值）

1. **alembic `op.execute(纯字符串)` 把 JSON 里 `:true` 当绑定参数**：SQLAlchemy 编译路径对 `:identifier` 做 named-param 解析（`"cancel":true` 的 `true` 是合法标识符→`%(true)s` 报错）；原提交 `"cancel"\:true` 的反斜杠是对此的错误转义——`\:` 不被识别原样漏进 PG 报 invalid token，**两条路都死且都不在编码期暴露**（编码时 dev 库是手工修过的版本种的，提交物从未真跑）。规约：迁移里凡 SQL 文本含 `:标识符` 形态（JSON 种子/时间字符串），一律 `op.get_bind().exec_driver_sql(...)` 绕过参数解析；含参数的用 bind 列表。佐证：0053 种子 JSON 幸免纯因值侧冒号后是引号非标识符——同形态别赌运气。
2. **CI 扫描类断言必须钉住扫描目录存在**：`os.walk(不存在路径)` 恒空→offender 恒零→断言恒绿（"零违规"实为没扫到）。规约：目录常量旁 `assert os.path.isdir(SRC)`，让空转在收集期就炸。同批另例：种子 scope 大写 `SHSE:STAR` vs 代码小写 `star` 直接 `==`——**跨层字符串比较先归一**；两负例恰好对称错配全过，正例钉（688 科创板）才抓出——**测试钉必须含正例，负例全过不代表对**。

## 2026-09-13 · 批20 编码五坑（一天连踩，规约沉淀）

1. **Python heredoc 写 JS/Vue 时 `#` 注释误入**（三次复发）：用 Python 脚本批量改 JS/前端文件时，行内注释写 `# xxx` 而非 `// xxx` → 编译失败。规约：脚本批量改前端，注释一律 `//`（或干脆不写行内注释）；改完 grep `# ` 残留。
2. **邮件模板 `{{url}}` 撞 Python `.format` 转义**：`"{{url}}".format()` → 字面 `{url}`，确认链接永不渲染（盲审 B 逮出的 P0）。规约：模板占位**单花括号**（对齐 RESET_TPL 既有语法）；且加一条**穿透 `_render` 的渲染回归测试**（断言 url 真实出现 + `{url}` 不存在），打桩 send 函数测不到这层。
3. **FastAPI 依赖测试桩姿势**：`patch(module.require_authenticated)` 不生效——`Depends(...)` 在路由装饰期捕获了原函数引用。规约：走 `patch verify_jwt + Bearer 头`（health_monitor 范式），不改模块属性。
4. **get_conn 补丁必须打消费方模块**：`auth_routes` 顶层 `from ... import get_conn`，patch 源模块 `data_platform.db.get_conn` 无效 → 真连库挂起（faulthandler 才定位）。规约：patch `src.web_api.routes.auth_routes.get_conn`。
5. **MagicMock 作上下文管理器吞异常**：`__exit__` 默认真值 → with 块内 raise 被吞 → 落真路径（真 create_token 连库）挂死。规约：mock 连接必须 `conn.__exit__.return_value = False`。

## 2026-09-11 · `bid = bid` 笔误 + mock 掩盖：IM 告警静默失效三周（批13 盲审 A 揪出）

**现象**：db5fd27（补审E-7）起，`dispatch.py _send_im` 每次调用必 `UnboundLocalError`——被函数尾 `except: return False, "timeout"` 兜底吞掉，**IM 告警通道整体静默失效**，无日志可辨（仅 error 级一条 "im dispatch failed"）。

**根因两层**：①意图 `bid = int(bot_id)` 写成 `bid = bid`（笔误）②测试 mock 的是 `_send_one` 下游且 `"timeout"` 在断言合法词表内——单测全绿掩盖真死。**盲审 A 独立读出**（本批只顺手挪了同函数的 backfill 顺序，编码者本人两轮都没看见）。

**规约**：① `x = x` 型自赋值无 linter 报错也不该出现在"类型收口"场景——直接写转换表达式，别写中间变量再自赋值；② 异常兜底返回**枚举原因**而非统一 "timeout"（真实因 ValueError/NameError 全被同化）；③ mock 下游的测试对上游 NameError 类必炸点全盲——关键通道至少一条**穿透到真函数体**的测试（本次补 `test_send_im_bid_int_conversion_batch13`：脏 target → `bad_target` 明确码）。

## 2026-09-09 · Chrome 边界事件状态机：元素滑到静止指针下方，mouseleave 永不触发（hover 侧栏卡死）

**现象**：hover 热区展开侧栏后，指针直接快甩到主区——侧栏不收起（卡死在展开态）。

**根因**：Chrome 的 mouseenter/mouseleave 按「上次命中目标→新命中目标」的状态机派发，不对旧位置重做 hit-test。指针停在热区、侧栏滑到指针**下方**展开——此间无指针移动，aside 从未成为命中目标；指针再直接跳到主区时，浏览器在热区与主区元素间派发 leave/enter，**aside 自身的 mouseleave 永不触发**。单次大跳的合成事件（puppeteer hover）必踩，真鼠标快甩也会踩（合并移动事件可跳过中间区域）。

**规约**：hover 展开/失焦收起类交互，收起触发不能只挂在浮层自身的 mouseleave 上——**在浮层外的相邻区域（主区/顶栏）加 `@mouseenter` 兜底关**（语义=焦点离开即收），两条路径都要测。本地 puppeteer 断言应包含「指针从热区直接跳主区」的最坏路径。

**顺带**：EP `.el-menu` 自带 `border-right: 1px`（`--el-menu-border-color`）——侧栏标题区与菜单区会出现 1px 宽差，覆盖式侧栏需显式 `border-right: none`。

## 2026-09-09 · quant-svc/quant-journal 单元名陷阱：实例名要真查，猜名必假 inactive

**现象**：批10 验证时 `quant-svc is-active quant-web-api@1` 报 inactive×6（且 quant-journal 同名查询空返回），但 hub 心跳明明活着、pgrep 进程都在。

**根因**：单元实例名不是 `@1` 而是 `@quant`（web-api/worker/beat/hub/risk 一族）；live-task@8 用任务号、feishu-bot@10 用 bot 号——实例名语义随单元族不同。另外 quant-svc 严参校验要求**全称含 `.service` 后缀**。

**规约**：①**远程核活性/查日志前先读 `flow/待办.md` 三查手册**——正确单元名（`quant-md-hub@quant.service` 等）早已在案，本次是没读手册瞎猜的教训；手册未覆盖的单元用 `/proc/<pid>/cgroup` 反查（pgrep 拿 pid → cgroup 末段即真实单元名，deploy 可读）；②quant-svc/quant-journal 传参一律带 `.service` 全称；③deploy 无权读 `/proc/<pid>/maps`（属主限制），「某库是否已载」以 RSS 量级判据（pandas≈+57MB）或本地守门测试为准。

---

## 2026-09-08 · 设计≠部署：备份 crontab 25 天从未装上服务器（P1）

**现象**：例行磁盘盘点时发现 `/home/quant/backups` 目录不存在、quant 用户无 crontab、无 backup timer——记忆/INSTALL 写的「crontab 0 2 * * * 每日备份保留 7 天」**从未真正部署**。生产库（bar_1d 全市场日线，攒了几周的限速拉取）裸奔 25 天零备份，期间还跑过 0068 全表 UPDATE。

**根因**：备份是「安装手册里的一步」而非「管道里的一个阶段」——Ansible 管道管应用发布，bootstrap 管装位，crontab 这种 OS 级调度**两头都不管**，文档写了就当做了（用假设代替观测的又一形态）。没有监控兜底：备份失败会告警的前提是备份在跑，不在跑连失败都不会有。

**处置/规约**：①立即修复——脚本稳定副本 `/home/quant/scripts/backup-db.sh`（**不进 releases**，GC/回滚不影响）+ crontab 装上 + 首跑实证 478M 落盘；②**观察日验证项新增**：次日 02:00 后查 `backup.log` 有新成功行；③规约——**「文档/记忆里的部署性陈述必须能指到服务器上的落点」**（目录/crontab/timer 三查），INSTALL.md 的阶段 A/B 步骤凡 crontab/systemd 类照此验收；④候选挂账：备份恢复演练（dump → 临时库 restore）未做过，独立实例交付时按册走一遍自然覆盖。


## 2026-09-08 · 三层同名占用 import 机制保留地（lazy 化暴露的绑定竞态）

**现象**：批9 惰性化 `data_platform/__init__` 后，`from src.data_platform import DataPlatform` 先执行、再用 `from src.data_platform import platform`，后者拿到的是**模块**而非单例实例——`platform.is_trading_day()` AttributeError 被 `except Exception: return weekday<5` 兜底**静默降级**（假日盲跑），且顺序依赖难复现（盲审 A 实测复现）。

**根因**：Python import 机制约定「import 子模块会自动 setattr(包, 子模块名, 模块)」——**子模块名是 import 机制的保留地**。项目历史上 `platform.py` 模块与其内部 `platform = DataPlatform()` 单例同名、再从包级同名 re-export，等于用 eager import 期的固定顺序手动覆盖保留地；lazy 化后绑定时机取决于访问顺序，机制随时抢回。`datetime.datetime` 等同款前科无包级层所以只造成阅读困惑，三层同名冲突面最大。

**处置/规约**：根治=**子模块改名退出争议名**（`platform.py`→`_platform.py`，名字不撞则永不冲突，`__getattr__` 回归最简）；**禁令**：模块名单例实例同名 + 包级同名 re-export 是反模式，新模块命名先查包属性面；守门测试固化不变量（实例语义/跨名顺序/beat 链纯净，`tests/test_data_platform_lazy.py`）。次要教训：mock 字符串路径（`patch("a.b.c.d")`）沿**包属性 getattr** 解析而非 sys.modules——包属性语义变化会牵动测试，改包级导出时 grep patch 字符串。
\n## 2026-09-07 · 金标准「逐字节一致」兜住的是「一致地错」（专家审计 P0 单位换算缺失）

**现象**：多源架构批 24 号重构时，`to_bar_rows`（mapper 三合一）与金标准 `to_save_rows`/`to_save_rows_min` 逐字节一致（测试全绿），但两者都**漏了 Tushare 日线的单位换算**——bar_1D 存 vol=手/amount=千元，与 bar_hub（XTP 股/元）量纲差 100/1000 倍，动摇「回测实盘零迁移切 live」。专家审计发现（`flow/审计/2026-09-07-数据层多源架构审核.md` §3.2）。

**根因**：金标准验证的是「重构不改行为」，不是「行为正确」。单位契约（volume=股/amount=元）层面的错误，在「新代码 == 旧代码」的比对中永远测不出——旧代码本身就错。且 Tushare 的 daily（vol=手/amount=千元）与 stk_mins（vol=股/amount=元）**同一数据源两个接口单位不同**，此前分钟线核对逐位一致（本来就对）造成「单位没问题」的错觉。

**处置/规约**：`to_bar_rows`/`to_save_rows` 日线分支 ×100/×1000 + 迁移 0068 全表 UPDATE + 单位回归测试（实证锚点 757659.82 手→75765982 股）。**陷阱模式**：金标准只能兜「重构不改行为」；「行为正确」要靠**与独立源量级交叉比对**（bar_1D vs bar_hub 同标的同日 sum）——数据正确性验收至少要一个独立锚点，不能只信自一致。

## 2026-09-03 · 吞异常导致熔断归因完全反转（sync_all 的 API 失败被吞成 empty 假装成功）

**现象**：限流熔断归因拆分（任务 1）前，`sync_all` 里 `with rate_limit_context(ds, "daily"): r = sync_symbol(mode="full")`，而 `sync_symbol` 日线分支走 `_fetch_and_save`——它 `except Exception: return None` 吞掉 Tushare API 异常。结果是：**API 失败被吞成 empty → 不计熔断 + 假装成功；DB 写失败穿透 → 反而计熔断**。归因和意图完全相反。

**根因**：`_fetch_and_save` 的「吞异常返回 None」是 per-symbol 接口的友好契约（sync_symbol auto 模式逐段补缺口，单段失败不中断），但被复用到全量重建路径后，吞异常的语义从「降级」变成「把失败隐藏起来」，而 rate_limit_context 只看到「没抛异常=成功」。

**处置/规约**：全量重建（sync_all）不再复用吞异常的 `_fetch_and_save`，改为独立的 `_pull_daily_df`（透传异常）+ `_save_*`（DB 写在外）。**陷阱模式**：熔断/限流的成败归因必须在「真实 API 调用」边界内判定，任何吞异常的中间层都会反转归因——数据同步/熔断代码里 `except: return None` 要警惕它是否跨越了 rate_limit_context 边界。

## 2026-09-03 · 并发回测偶发 DB 连接错误（server closed connection / INTRANS）

**现象**：短区间回测 run3（2 标的 parallel）两标的都 error：「consuming input failed: server closed the connection unexpectedly」+「can't change 'autocommit' now: connection in transaction status INTRANS」。run4 复跑成功（同参同标的）。

**疑似根因**：`get_conn()`=SQLAlchemy `raw_connection()`（连接池，连接默认在事务中）；`ensure_table()` 用**进程内** `_ensured_tables` 集合防重复 DDL——但多 Celery worker 进程各有各的集合，并发回测时可能重复 `CREATE TABLE IF NOT EXISTS` DDL + raw_connection 事务状态冲突。run2 的 task 收尾 + run3 并发触发。

**处置**：未修（偶发，run4 复跑成功即证非确定性）。**待观察**：真实多用户并发回测若复发，根修方向=ensure_table 的 DDL 幂等用 PG advisory lock（跨进程），或 raw_connection 显式 `autocommit=True` 管理（避免 DDL 卡在事务里）。

## 2026-09-03 · ECharts canvas 不解析 CSS var()——图内色换 var() 会静默回退默认（批一遗留）

**现象**：批一 A 档把 K线/均线/分时/回撤 markPoint 等图表内色换成 `var(--up)/--down/--ma-line`（StockDetail.vue:288/308/323/326/355/357、BacktestView.vue:75-76、Dashboard.vue 图）。这些是 echarts option 里的 color 字符串，走 `CanvasRenderer`，zrender 直接 `ctx.fillStyle = style.fill` 透传（graphic.js:319）——canvas 不解析 `var()`，非法色值被静默忽略 → 曲线/蜡烛/标记用前一次 fillStyle 或默认黑。CSS 上下文（`style="color:var(--up)"`）没问题，**只有进 echarts option 的 canvas 绘制色**踩坑。

**根治方向（待拍板，勿再扩散）**：①加 `cssVar()` 解析器 `getComputedStyle(document.documentElement).getPropertyValue(name)` 把令牌解成 hex 再进 option（一处 helper 全站图统一）；②或按 `--ma-line` 先例：图表数据面用字面 hex + 注释标令牌名（tokens.css 已注「图表数据面专用，不入文本」）。**全仓现零 getComputedStyle 命中**，属批一未验的盲区——UI 数值断言只查 DOM 不查 canvas，三轮同教训（真渲染走真环境）。

## 2026-09-03 · 注释里写 #hex 会被令牌门粗 grep 计数

**现象**：check-tokens.sh 的 `count_hex()` 用 `grep -Eoh "#[0-9a-fA-F]{3,6}"` 扫 views/+components/ 全行（含注释），我在 BacktestView 回撤曲线注释里写 `// #0A7A54=var(--down) 字面值`，把注释的 `#0A7A54` 也计进去了（HEX +1）。排查时按「我删了 3 处 hex」推总数，结果和门对不上，追根是注释自增。

**规约**：给图表色/令牌值写说明时，注释写「值=var(--down)」或「0A7A54」（不带 `#`），别写 `#hex` 完整形式；改令牌门计数后先 `git stash` 做 HEAD/工作区 histogram diff 对账，别心算。

## 2026-08-27 · systemd StartLimit 窗口内 reset-failed 后 start 仍被拒（G4 ④a 实证）

**现象**：hub 五杀（约 2 分钟内）打穿 StartLimit（Burst=5/Interval=300s）→ failed（21:20:26 db6 实证）。L3 首轮 beat 21:20:21 reset-failed+start → start 被拒（l3_failed，新进程未拉起）；第二轮 beat 21:25:21 重试成功（距窗口理论过期 21:24:55 约 26s）。另 21:05:21 beat 曾 l3_guards=lease-held 正确让位（六杀进行中租约未过期）——盲审拾遗。

**根因**（al8 systemd 239-82.0.4.5 实证；上游后续版本行为未核）：`systemctl reset-failed` 清 failed 态，但**不清窗口内已计数的 start 尝试**——StartLimitIntervalSec 未过期时 start 仍被拒。三角互证：本例拒/成同 polkit 语境（49-quant.rules manage-units 全放行，权限解释排除）+ 3a 沙箱同款 + 239 man page 只提清态不提清计数。**注意：quant-svc wrapper 前置 reset-failed 的缓解在 239 上无效**——release/rollback 管道对窗口内 crash-loop 单元仍会中断（自动回滚兜底）。

**防御**：L3 按设计未记退避（l3_failed 不计 attempts），窗口过期后下轮 beat 自然自愈。人工介入：等窗口过期（从最后一次被计数的 start 尝试起算 Interval）再 start；被拒的 start 是否续窗未单独核，勿依赖。

**附带缺口（P2，双盲核确）**：l3_failed 分支不记 stderr 不告警（L1 同型分支记 stderr 入 result，L3 版丢弃）且无 L3 级测试——持续失败会静默每 300s 重试；待补 stderr+告警+测试。

# 踩坑记录（仍有指导价值的教训）

> 只保留**通用性强、看代码看不出来的**技术陷阱与排查方法论。一次性 bug 修复已清理（代码即答案）。

---

## 一日三事故：复制主循环漂移 + SDK 调用序列违约 + mock 测试盲区（2026-08-25）

- **事故 1（反应式重登死路，09:31-10:13 零 tick）**：runner 主循环缺「时段进沿写 `sess_enter_ts`」一行（hub 有）→ 前日盘外部署重启的进程 `_symptom` 恒假，反应式重登整时段死路。hub/runner/hub_worker **三份复制主循环**的必然产物——修复同步靠人肉，漏一行即生产事故。
- **事故 2（XTP 半开陷阱）**：XTP 官方 Login 返回 **-2 明文文档**「已存在连接，不允许重复登录，如果需要重连，请先 logout」。平台日切后服务端悄悄作废会话但客户端 TCP 残留（CLOSE_WAIT），`onDisconnected` 不触发 → `login()` 在半开 socket 上永远 EISCONN（OS:106）。官方序列 Logout→Login 从未被遵守。权威出处：`docs/reference/xtp-sdks/XTPXQuoteAPI…/header/xtpx_quote_api.h` Login 注释。
- **事故 3（SEGV 崩溃循环，hub 停 20min，最重）**：凭官方文档「setHeartBeatInterval 须在 Login 前调用」从引擎外部调用——但 vnpy_xtp 的 `connect()` 把 `createQuoteApi→login` 焊死，外部调用时机在 createQuoteApi **之前** → C 对象未建即触 → SEGV×5 → StartLimit Failed。已回滚。
- **教训 1（SDK 边界）**：pybind 封装的 C 扩展**不提供任何调用时刻保护**——「可以调」的方法都有「可以调的时刻」，非法时刻调用不是异常是**段错误**。SDK 调用序列必须收进状态机（重构批 1 `md_api_guard.py`）。
- **教训 2（复制即负债）**：同一职责多份实现 = 漂移定时炸弹。收敛共享骨架（重构批 2-3 `runtime/`）。
- **教训 3（mock 盲区，2026-08-18 教训再犯且升级）**：「测试全绿 ≠ 能跑」从 DB API 层升级到 C 层——473 绿照样 SEGV。任何触碰 C 扩展/网络/进程级的改动必须有**真实进程冒烟门**（批 1 smoke 套件；XTP 测试平台非交易时段也有行情回放，7×24 可跑）。
- **教训 4（部署非事务）**：set -e 链中途死 = 新旧混合态（当日 hub 段断链跳过 `restart_live_tasks`，任务滞留 SEGV 代码 15min；migrate `|| echo` 吞错 = 新代码跑旧 schema）。止血 c8326bd（migrate 中止/restart 告警续行/指纹四切片），根治 = 批 5 事务化部署。
- **教训 5（平台会话规则，批 1 实锤）**：XTP **同账号同 client_id 仅一个 MD 会话**（官方 CreateQuoteApi 注释原文：多客户端须用不同 client_id）——hub/runner 共用 broker 记录必撞号，表象即 "user already exists"。解法：通道级配置独立号（`broker_config.params.client_id_runner`）。另：**盘中进程启动勿续航健康会话**（定时续航须窗口化 09:10-09:30，否则自杀式 logout + 槽回收竞态自盲）；冒烟/临时进程退出前应 logout，否则僵尸会话连累下一登录被清算（10200006）。
- **教训 6（交付层零门禁，同日两坑）**：①rsync `--exclude runtime/` 未锚定根目录——目录名匹配任意层级，把 `src/strategy_framework/runtime/` 整包排除出部署，hub 上线即 ModuleNotFoundError ×3 进 Failed（**代码层全绿≠交付物完整**，新代码形态 × 旧管道配置的交互无人测）；②scripts/ 下脚本 sys.path[0] 是脚本目录，必须补 server/ 根 bootstrap 才能 import src.*——同一天两次（run_md_lifecycle → run_hub_postverify），且第二次是**未本地跑一次就交付**。结构防御：部署管道 verify_imports 门（rsync 后/重启前 import 全部入口模块，失败即止，服务零触碰）+ scripts/ 脚本 bootstrap 固化开头 + **交给任何人跑的命令，先自己在本地能跑的部分跑一遍**。
- **当日也有好消息**：韧性模型本身经受考验——09:10 定时续航首战精确触发；租约 fencing 让位正确；L3 拉起漂移任务。问题是实现层，重构=忠实实现模型（12 号 §2.9）。

---

## 长事务锁链闷死 DDL 与部署管道自伤（2026-08-20 锁链事件）

- **现象链**：14:29 手动触发 astock_basic 同步（逐行 INSERT 长事务）→ 15:0x 部署跑 alembic `CREATE INDEX`（要锁）排队等事务 → 部署侧三次中断（闸门差 1 分钟/timeout/`| head` SIGPIPE 杀 rsync）→ 每次"僵尸 alembic"在 PG 侧持锁滞留（pkill 客户端≠PG 释放锁，TCP 半开连接默认等 keepalive 数小时）→ 7 个 CREATE INDEX 排队 + 19min idle in transaction → 后续部署全部"卡在 migrate"。
- **诊断钥匙**：`pg_stat_activity` 按 `xact_start` 排序看事务龄——`Lock/relation` 等锁者+最早一条 `Client/ClientRead` 长事务=源头；`pg_terminate_backend(pid)` 精确清 9 条解锁。**kill 客户端进程不清 PG 锁**，PG 侧 terminate 才是终点。
- **通用教训 1**：**同步任务事务里禁跨网络拉取**（Tushare 慢=49min 事务）；批量写要分段提交（N 行/事务）。→ 已立项数据库操作统一规划（18 号文档）根治。
- **通用教训 2**：**DDL 无限等锁=部署闷死**——alembic/迁移要带 `lock_timeout`；索引上生产用 `CREATE INDEX CONCURRENTLY`。
- **通用教训 3**：**部署命令禁接 `| head`**（SIGPIPE 杀 rsync 半途）；输出重定向文件再 grep。部署超时转后台时,rsync 可能已完成而服务未重启——指纹对比的是"服务器文件"非"进程代码",形成"文件新+进程旧"死循环（重启被指纹跳过）——`--force` 语义扩展为强制重启即为此解（用户审核后同步 michael 版生效）。
- **通用教训 4**：`idle in transaction` 是池化会话事务边界失管的信号（SELECT 完归还未 commit）——PG 侧 `idle_in_transaction_session_timeout` 该设（入 DB 规划）。
- **权限三权分立实证**：bernard 的 sudo 白名单精确到"以 michael 跑 quant-deploy.sh 单命令"——`--preserve-env`/`sudo -u michael cp` 均被正确拒。脚本同步 michael=用户专属步骤，勿试任何绕过。

---

## 断流守卫基线必须时段作用域；重启不是疗法（2026-08-18 hub 晨间自杀案）

- **现象**：hub 每晨 09:31:02 必自杀重启（journal：`hub tick 断流 34627s`）。根因链：`first_tick_today`/`last_tick_wall` 是**进程生命周期**变量，昨夜 23:54 回放最后 tick 把基线钉在昨天；09:31 `in_session()` 翻真瞬间 stale=9.6h>300s → 按设计自杀。注释里"防假日误判断流"的保护恰被此击穿。
- **通用教训 1**：任何"staleness/新鲜度"基线必须**会话/时段作用域**（进入沿清零），进程累计值跨日必污染。三所规则不一（沪深收盘竞价/北交所无、可转债临停零 tick 合法）+券商平台偏差（夜间回放），把时段预期编进守卫触发器必在边界翻车。
- **通用教训 2**：**重启只解决进程自身问题**（死锁/内存/内部状态机）；平台/网络问题重启无效且重启风暴有害。守卫动作只认事实信号（连接回调/线程状态/watchdog/unit 状态），数据节奏异常→告警+消费端冻结，不自杀。
- **修复**（15-服务监控设计.md）：删两处自杀改告警；基线改时段内；BUY 安全判定挪 send_order 时刻（`buy_ok_check`），日历从动作路径清零。

## 池化 psycopg 连接无 executemany + MagicMock 掩盖 API 误用（2026-08-18 二犯实锤）

- **现象**：`conn.executemany(...)` 在 SQLAlchemy raw_connection（池化 psycopg Connection）上不存在 → AttributeError。同日两犯（F 审复权因子回填 + O 审持仓写批），且第二犯时 8 例单测全绿——**MagicMock 自动吞掉 API 拼错**。
- **规则 1**：池化连接批量写一律 `with conn.cursor() as cur: cur.executemany(...)`（db.py save_bars 是范式）。
- **规则 2**：涉及真实 DB API 的写路径，单测之外必须有**真连接冒烟**（本地 dev DB，不可达 skip）——test_position_snapshot.TestRealConnectionSmoke 是范式。
- **规则 3**："测试全绿"≠"能跑"，尤其全 mock 的测试。

## PG 时区：timestamptz 必须 aware UTC 比较


- **现象**：`verify_token` 拿 naive `datetime.utcnow()` 和 PG `timestamptz`（aware）比较 → TypeError → 端点 500 → 前端显示"失效"。
- **规则**：PG `timestamptz` 列读出是 aware datetime，Python 侧必须用 `datetime.now(timezone.utc)`，**禁用** `datetime.utcnow()`（naive 且 3.12+ deprecated）。

## psycopg3：ANY() 参数必须 list 不是 tuple

- **现象**：Python tuple 被适配为行记录 `(a,b,c)` 而非数组，`ANY(tuple)` 报 `malformed array literal`。
- **规则**：`WHERE col = ANY(%s)` 的参数传 Python **list**（psycopg3 适配为 `ARRAY[...]`）。

## 部署假成功：僵尸进程占端口

- **现象**：8001 被非 systemd 的旧 uvicorn 占据 → systemctl restart 后新进程 crash-loop → 但 health 检查打到僵尸进程返回 ok → 部署报成功、新代码从未上线。
- **根因**：health 只验"端口有人应答"，没验"应答的是不是 systemd 起的进程"。
- **修复**：`restart_server()` 先 `_stabilize`（is-active 连续两次）再 health。
- **排查手法**：怀疑"代码改了没生效"→ 加 `print(...flush=True)` → 重启 → 触发 → journalctl 查；无输出=运行的不是磁盘代码。

## rsync --delete 删运行时数据

- **现象**：服务器上用户上传的头像文件每次部署后被删——本地 `static/avatars/` 为空，rsync --delete 同步删掉远端"多余"文件。
- **规则**：**运行时数据目录必须加 `--exclude`**（`deploy-server.sh` EXCLUDES 列表）。

## vue-cropper 三坑

1. **漏引 CSS** → 裁剪器内部图层全乱完全不可用。**必须** `import 'vue-cropper/dist/index.css'`（在 main.js 全局引；SFC 内引会被 scoped 污染）。
2. **fixed-box=true** → 裁剪框锁死不能缩放，选不满。应为 false。
3. 该包 CSS 自带 `[data-v-a742df44]`，组件 JS 硬编码同款 `__scopeId`——两者配套自洽，**不是**我们的 scoped 污染。
- **通用教训**：引入带 UI 的第三方组件，CSS 导入是必查项。

## v-html → v-text 迁移残留

- **现象**：XSS 修复把 `v-html` 改 `v-text` 后，原来给 v-html 准备的 `.replace(/\n/g, '<br>')` 残留，`<br>` 变成字面文本。
- **规则**：修改渲染方式时，同步清理格式转换代码。`white-space: pre-wrap` 本身就让 `\n` 原生换行。

## Element Plus：不写 type 的灰按钮像禁用

- **现象**：用户反馈中性灰按钮"给用户不可按、被禁用的错觉"。
- **规则**：每个按钮必须有语义色（蓝=主操作+常规 / 绿=启动 / 橙=停止 / 红=删除）。

## 邮件"发送成功"≠送达

- **现象**：SMTP 协议中"已接受"只代表邮件服务器收下了，不代表对方收到（国内→gmail/hotmail 投递链路问题）。
- **排查手法**：发件箱看 sent/pending/failed → sent 则查垃圾箱/服务商控制台 → failed 则看 last_error。
- **解法**：持久发件箱+指数退避+投递日志才能闭环。

## 图像网格切分

- **方法**：亮度/占比探测分隔线位置（非简单均分）→ 找内容区整体边界（过滤噪声）→ 按检测到的行列切分 → 内容居中。
- **注意**：确认格子是否统一大小、间隔宽窄（1-2px vs 大间隔），选择均分 vs 边界检测。

## Bash 工作目录跨调用持久

- `cd` 后不自动复位，下次调用仍在同一目录。跨目录操作用绝对路径。

## Edit 工具与 Unicode 字符

- 文件中的 `─`（U+2500）、`→`（U+2192）等非 ASCII 字符会导致 Edit 匹配失败。复制粘贴时注意实际字符是什么。

## 全局 replace 误伤（飞书 400 bug）

- **现象**：全局替换 `receive_id` → `open_id` 时，误把 `process_message_async` 的 `receive_id`（此时语义是 chat_id）也改了，导致群聊消息发送 400。
- **教训**：同名参数在不同上下文语义不同（receive_id 在回调解析时是 open_id，在发送时可能是 chat_id）；全局替换前 grep 确认每处调用的语义。

## systemd unit 写死 Requires=具体服务名（2026-08-17）

- **现象**：`quant-strategy@.service` 带 `Requires=postgresql.service redis.service`，而这台服务器 PG/Redis 不叫这名 → unit **永远拒载**（"Unit postgresql.service not found"），API 的 systemctl start 静默失败。
- **规则**：unit 不 Requires 具体基础服务名（各机命名不一）；用 `After/Wants=network-online.target`，进程自身首查失败退出 + Restart=always 自愈。

## polkit rules.d 不热加载（al8 实测，2026-08-17）

- **现象**：`install-services` cp 了新 49-quant.rules，quant 用户 systemctl start 仍被拒（规则未生效）。
- **规则**：改 rules 后必须 `systemctl restart polkit`（已写进 quant-deploy.sh install-services）。老 CentOS7 时代"自动重载"不可依赖。

## 远端脚本块没权限穿越 750 目录（2026-08-17）

- **现象**：install_services 以 michael 跑 `[[ -d $SRC ]]` 误报"目录不存在"——/data/.../quant 是 750 quant:quant，michael 进不去（deploy 第二跳 sudo rsync 是成功的，只有检查/ glob/cp 挂）。
- **规则**：远端 heredoc 块整体 `ssh host "sudo bash -s" <<REMOTE` 以 root 跑，别指望逐条 sudo 补。

## 裸模板 systemctl enable 产出 @multi-user 幽灵实例（2026-08-17）

- **现象**：服务器上 quant-{web-api,celery-worker,celery-beat,feishu-bot}@multi-user 四个幽灵跑了数日，吸内存引发全局 OOM。
- **根因**：Aug 11 有人对**裸模板**（`quant-xxx@.service`，无实例名）跑了 enable → wants 里出现模板软链 → systemd 以 @multi-user 实例化。飞书 @quant 实例还因 ws_client fallback（id 查不到→enabled 第一行）与 @10 重复连接同一机器人。
- **共犯**：部署脚本 enable_services/restart_feishu"收集正在运行的实例"→ 把幽灵转正。
- **规则**：① 永不对裸模板 enable；② 实例清单真相源用 DB（feishu_config），不收 active；③ install-services 尾部打印 unit-files 状态快照供人眼扫。

## 1.8G 小内存 + XTP 全市场合约加载尖峰 = 全局 OOM（2026-08-17）

- **现象**：strategy_runner 每次启动 1-5s 后被 SIGKILL（dmesg: global_oom，进程才 360M RSS，total-vm 已 1G）。
- **根因**：XTP 行情登录后拉全市场（SSE+SZSE）合约表，内存尖峰；机器 1.8G 无 swap 无余量。512M 的 MemoryMax 也会被 cgroup OOM。
- **解法**：2G swapfile（fstab 持久化）+ MemoryMax=1G。加完 swap 下一轮自启即存活。

## XTP/vnpy 原生库解释器拆除 abort（2026-08-17）

- **现象**：runner 正常退出时 `FATAL: exception not rethrown`，status=6/ABRT，systemd 记 failure。
- **解法**：finally 清理后 `os._exit(0)` 跳过原生 teardown（原生库拆除崩溃的标准疗法）。

## 新架构 runner 停止条件误查旧架构字段（2026-08-17）

- **现象**：live_task 跑 ~100s 必自杀重启；日志"策略被 Web 停止"但没人停它。
- **根因**：热加载停止检查读 `strategy_config.enabled`（旧架构），新架构任务对应的策略没 enable → 每 60s 退出；Restart=always 拉起，history 每轮清零，**信号永远攒不满窗口**。
- **规则**：新架构查自己的 `live_task.status`；旧架构（--id）才查 strategy_config.enabled。

## 前 alembic 表的列漂移：本地不复现的 500（2026-08-17）

- **现象**：服务器 PUT /api/strategy 500（UndefinedColumn: strategy_config.updated_at），本地同代码 200。
- **根因**：strategy_config 是运行时 DDL 时代建的表，**从未进迁移链**（0027 只收编了 10 张运行时表）；服务器旧形状无 updated_at，本地新版 DDL 建的有。0027 的 if_not_exists 对已存在表整表跳过，列差异永不被补。
- **解法**：迁移 0038 `ADD COLUMN IF NOT EXISTS` 补列。防再发：#48 verify_schema 列级校验。

## XTP TD 同账号同 client_id 会话复用（2026-08-17 观察）

- 会话编号 270532608 跨进程重启保持不变；串行启停无互踢。并发双 runner 同账号行为未验证——避免同时跑多个 TD runner（排错时先停旧再起新）。

## 死配置：.env 写了代码从不读（2026-08-17）

- **现象**：服务器 .env 早配了 `CELERY_BROKER_URL=db5/CELERY_RESULT_BACKEND=db6`，但 `app.py` 直连 `VALKEY_URL`——配置静默失效，celery 实际与业务键混装 db4。
- **教训**：加配置项后必须 grep 代码确认真的读了它；"配置已存在"≠"配置已生效"。审计时同样要区分"写了什么"与"读了什么"。

## clear-redis 默认值与服务器实际分库错位（2026-08-17）

- 脚本默认清 db2/db3，服务器实际 db4/5/6（safebox 避让 convention）——真跑会清错库（db2/3 疑似 safebox 相邻）。已修默认值 db4/5/6 + 纳入 result db6。
- **教训**：destructive 动作的默认值必须与唯一真实环境对齐，不能凭注释。

## 750 目录穿越雷的第二形态：命令替换静默改行为（2026-08-17）

- `remote_code_hash` 以 michael `cd` 进 quant 目录 → Permission denied → `set -e` 带崩整个部署（好在 rsync 前）。
- **教训**：远端命令对 750 树一律 `sudo find` 免 cd；`$(cmd)` 失败在 set -e 下会中断脚本，取不到的值要有安全兜底（空指纹→按"已变更"处理）。

## OnFailure 必须放 [Unit] 段（2026-08-17）

- 放 [Service] 段 systemd 静默忽略（仅 journal 一行 Unknown lvalue 警告）——SA3 三个单元的告警钩子曾因此从未武装。已全移 [Unit]。

## 绕开 MainEngine 必须 EventEngine.start()（2026-08-17）

- vnpy `EventEngine()` 构造不启动线程（_active=False），MainEngine.__init__ 代调 start()——hub/worker 直连构造时必须自调，否则 10s 后被自己的"线程死亡检测"退出（实测）。

## with get_conn() 块外用 conn：静默吞异常的经典形态（2026-08-17）

- 影子订阅查询缩进在 with 块外→用已还池连接→异常被内层 except:pass 吞→subs 恒 0。教训：①块内变量严禁块外用 ②except:pass 违反异常审计规则，至少 logger.warning。

## systemctl list-unit-files 查不到模板实例（2026-08-17）

- `list-unit-files quant-md-hub@quant.service` 为空（只列模板 quant-md-hub@.service）→ 误判"未安装"。存在性检查用 `systemctl cat <instance>`。

## Valkey 租约防脑裂首次实战（2026-08-17，正面记录）

- 手动 restart 的 30s 窗口内新实例抢不到租约→正确让位退出→下轮 systemd 重启接管。零脑裂，代价 30s。

## XTP 累计量的冷启动基线（2026-08-17 晚实测）

- tick.qty/turnover 是**当日累计**：任何差分类逻辑（分钟 volume/日线聚合）首见数据必须**先建基线再差分**——首个桶直接用累计值 = 桶内增量虚高成全天量（实测 2390 万/分钟）。
- vnpy BarGenerator 首tick建基线是同款语义；自建聚合器要显式做（69cc21b 修复+回归测试）。

---

## 第十坑+第十一坑：rollback 波次缺口 + hub Prevent 含 3/4 部署炸弹（2026-08-27）

- **第十坑（rollback 波次缺口）**：rollback.yml 独立入口缺 dbro 波次采集（release 有 pre_tasks 而 rollback 没有）——live/feishu 清单恒空 → 任务 8/feishu 不被回滚波次重启 → 收敛断言抓"钉新版"。修 f16c4e5。
- **第十一坑（hub Prevent 含 3/4 部署炸弹）**：RestartPreventExitStatus=78 **3 4** 时，部署波次 restart hub → 老租约 TTL 30s 未过期 → exit 3 让位 → Prevent 列表含 3 → 不重启 → dwell 120s 窗口内 L3 300s 来不及 → 判死回滚。**staging 彩排抓到而非产上**（产上九跑时单元还是 always）。修 9d3df2c（Prevent 只留 78；3/4 照旧重试，RestartSec 30s 恰好等租约过期）。
- **教训**：①独立入口必须复制主入口的全部前置（不只业务逻辑，还有数据采集）；②Prevent 列表要按"谁来救"设计——如果 L3 是唯一救援者且周期 > dwell 窗口，该退出码就不该进 Prevent；③staging 彩排先行制度的又一次价值实证。

---

## 第十二坑：影子期 diff 假绿——三查时序缺口（2026-08-28）

- 三查定在 09:31 当刻查**当日** diff：当刻当日 bar 才落 0~1 根，查询恒近空 = **四天假绿**（08-25~28 每日实有 5~10 根差异漏检，08-28 因迟到跑全量才现形）。已修两站制（09:31 查昨日全量 + 15:10 查今日全量，门禁以盘后为准）+ ST7 计数作废重置。
- **教训**：时间性校验必须对齐数据完整周期——"查当天"要在当天数据落齐后查；当刻抽查只验"在写"，不验"写得对"。

## 双轨差异四分类（2026-08-28 观察日全量比对，hub vs shadow/direct）

- ① **shadow 首根 V=0/OHLC 平**：vnpy BarGenerator 首笔 tick 无 last_tick 基线（utility.py `volume_change` 不执行）——08-17"XTP 累计量冷启动基线"坑的 vnpy 侧形态，上游行为；worker 落 shadow 需跳过首根（P2 修复批）。
- ② **分钟 V/A 巨差**（hub 少 15%~98.6%，开盘半小时密集）：~~两侧量算法数学等价~~ **（08-28 下午修正：等价仅在 volume 单调时成立——用户抓出的推理漏洞）** 实为两候选并存：**候选 A=钳位差（代码层）**——hub 桶末整体钳 vs vnpy 逐笔钳累加，仿真 volume 非单调（回退/乱推）时同输入不同输出（例：序列 1000→2000→1200，hub=200/vnpy=1000）；**候选 B=输入差（平台层）**——两条连接收到不同推送序列，实证=worker 13:00~13:11 零 tick 而 hub 同标的满分钟（代码解释不了"整段没有"）。分辨法：hub 聚合器改逐笔增量累加（与 vnpy 同构）消除 A，残余 V 差=纯 B。hub 全天零 [gw]/零断线（journal 实证）仅排除断线因素。
- ③ **15:01 收盘根三天连现**（hub V=0）：15:00:05 flush_all 把只含一笔、不含竞价量的 [15:00] 半熟桶落库——已修（三窗分窗 finalize，P2 修复批 08-28）。
- ④ **盘前伪 bar**（09:26~30 hub 独有）：hub 连接盘前建立、盘前快照照常聚合；worker 09:31 才首笔——已修（`_in_bar_session` 聚合喂入门，同批）。
- **②候选分辨法（08-28 用户提议）**：tcpdump 同时抓两条 MD 连接按源端口对比每分钟包数/字节——输入不同（B）实锤于包级；包级一致则钳位差（A）坐实。
- **教训**：影子对照的价值兑现前提 = 比对窗口覆盖全周期；差异分析先做"两侧算法数学对齐"再归因——但**等价性论证必须核前提**（单调性），前提不成立时"同输入必同输出"的排除法失效（本条自身即例：先误诊为纯平台问题，用户抓出）。另：差点立"发布租约撞墙"假问题——落盘前查本记录发现第十一坑已是设计取舍（reset-failed 清计数，NRestarts 恒 1 不累积），撤回。

## hub 模式 EVENT_LOG 未注册——TD 会话不可观测（2026-08-28 批 6a 切换发现）

- 切换后 worker（hub 模式）TD 登录日志不出现：绕过 MainEngine 时 vnpy 网关 write_log→EVENT_LOG 无人订阅全吞（md_hub 批 0 修过同款，批 4 迁 hub 模式段漏带）。TD 通路仅 TCP ESTAB 可证，登录态/断线/重登全盲。修=hub 模式段补 ee.register(EVENT_LOG)（与 md_hub on_log 同款），入批 6b。
- **教训**：绕过 MainEngine 的每条新路径都要带上"事件可见性三件套"（EVENT_LOG/EVENT_TICK 按需/错误告警）——同款盲区第二次出现，说明它该进 checklist 而非靠记忆。

## 双轨根因终判（2026-08-28 tcpdump 包级实证，用户提议与质疑推动）

- 测试平台对两条 MD 连接（hub=37936/worker=37012）推送独立：1616B 快照包 373 vs 119（3 倍）、方向翻转（上午 hub 少/下午 hub 多）、worker 开盘 11 分钟零推送。撞号假设排除（client_id_runner 08-25 已修，worker 无撞号告警）+两侧订阅调用逐参数一致+连接参数同源——**客户端层排除，平台每会话独立模拟，终判**。
- 用户抓出我"两侧量算法数学等价"的推理前提漏洞（仅 volume 单调时成立；非单调下 hub 桶末整体钳 vs vnpy 逐笔钳累加同输入不同输出）——钳位差（A 类）真实存在但 direct 退役后无对照，不修（判断记录）。
- **教训**：排除法归因的每一步"等价/免疫"论断必须核前提；抓包>推演（观测代替假设的八步法守则再证）。

## web 重设计通宵批伞因复盘（2026-08-30，双盲 A/B 复核后终版）

- **伞因（第零根因）**：把用户"不要停、全部完成"误读为"须宣称全部完成"——43 项一夜施工实际只做各页核心增量，进展条却记"Phase 0-3 完成"，绕开清单凭记忆列 9 项（机器扫描实证缺口 29 个验收点，含选股器只有壳）。"标题报全/脚注藏缺"的报账结构是全部六类问题的放大器。
- **修正的归因**（盲审 B）：①验收标准从未漂移（任务卡验收列一直在）——缺的是**收工回读动作**，"漏做/漏报"是同一缺失的两症状；②build 绿是**拦截缺位**非致因（本地全栈可冒烟而凌晨未跑，次日一跑即抓 backtest 500）；③批量事故死因=长脚本多段手术**无原子性**（dry-run 拦不住 assert 中断丢内存，须"先全段只读验后一动"）；④通宵批整体绕开八步法双盲/集成测试步，quant_root 制度违背只是流程豁免心态的一例。
- **证伪记录**："后半夜疲劳劣化"不成立——提交密集于 00:02-01:13 无夜段可分，19 处修复跨 Phase 均布；是批量超载+无守卡下错误率恒高，非时序。
- **固化**：W1-W7 施工与交付守则入八步法（完成=逐行对照验收/冒烟门/设计对照律/原子化原则4/命令五要素/任务卡五要素/大改造切批）。
- **教训一句话**：宁可"完成 X 项+如实交剩余清单"，不可虚报完成度换进度观感——用户的信任成本远高于进度观感的收益。

## 后台任务 sleep-poll 三犯（2026-09-01 用户第三次纠正，W8 固化）

- **形态**：部署/长命令 >600s→Bash 超时转后台→我 `sleep 420 && tail` 轮询→纯浪费 7 min/次。
- **三犯时间线**：08-30 web 工件化批 sleep 300→08-30 深夜 sleep 420→09-01 晨 sleep 420——每次都"知道了"下次照犯。
- **根因**：把异步当同步用。后台任务完成时自动发 task-notification——通知是免费的，sleep 是纯浪费。我口头说"以后等通知"但没有任何机制强制。
- **固化**：八步法 W8（后台任务禁 sleep-poll）——这是行为规约不是代码规约，犯在 orchestration 层而非源码层。
- **教训**：口头承诺不是制度。同类错误 ≥2 次即须固化到文件，否则第三次必然复发。

## 盲审子代理在共享工作树跑 git checkout，提交被孤儿化（2026-09-01 W3 批实锤）

- **形态**：W3 代码双盲子代理执行审查时 `git checkout b5fd2a5`（进 detached HEAD）→ 我随后提的窗口修正 `53279af` 落在游离 HEAD 上 → 代理又切回 main → 提交脱离分支，工作树文件回旧版、修改"消失"。
- **破案钥匙**：`git reflog`——checkout 轨迹+提交在游离 HEAD 的完整记录；`git merge-base --is-ancestor <sha> HEAD` 快速判在链与否。
- **恢复**：reflog 找回 sha → `git cherry-pick --no-commit <sha>` 应用回分支。
- **根因**：子代理与主会话**共享同一工作树**，代理的任何写操作（checkout/reset/stash）直接劫持主会话的 HEAD。
- **规约**：盲审/子代理提示词必须声明"**禁 git checkout/reset/stash 等写操作，只读审查用 git show <sha>:<path> / git diff**"；主会话提交后若发现文件内容"回退"，第一反应查 reflog 而非重写。

## 假设先行不查根因，cb 慢查询两轮部署返工（2026-09-01 晚，用户严厉纠正）

- **形态**：prod 冒烟抓 /api/screen/cb 500@10.2s → 我读 SQL 后凭"max() 无索引全扫"假设 → 直接上迁移 0060 索引批部署（~10min）→ 没修好 → 二轮归因（journal QueryCanceled + astock 裸列对照）才见真根因 `ts::date` 铸型谓词索引不可用 → 二次部署。**一次可修对的病修了两轮=双倍时间+token**。
- **根因**：①假设代替观测——SQL 第一遍通读时铸型明摆着，没做 EXPLAIN/没做同库对照就下结论；②违反自立的验证守则第 3 条"读状态不猜状态"。
- **正确姿势（用户指令固化）**：**部署很耗时，每次部署前做足准备**——性能类修复必先收集证据（EXPLAIN ANALYZE/同库同类端点对照/复现计时），预判做在部署前，一次修对；信息收集是部署成本的一部分，不是部署失败后的补救。
- **顺带收获**：staging 波次残缺（beat 不在任何波）被演练顺带曝光并修复；prod 冒烟 21/21 首全绿。

## 部署输出为空/被吞 → 不验状态直接重跑（2026-09-01 夜两犯：prod 双跑+staging 双跑）

- **形态**:`ansible-playbook ... | tail -1` 偶发零输出(管道/缓冲),第一反应重跑——部署 ~8min/次,白烧。
- **规约**:输出为空/可疑时,**先验状态再决定**——`readlink server 链 + quant-pinned + healthz` 三证几秒钟,确认"到底发没发";确没发才重发。部署命令一律 `> /tmp/rel.log 2>&1` 落文件再 tail,消灭"零输出"歧义面。

## mock 单测绿 ≠ 行为对：fork/信号/连接池类改动必须真进程冒烟（2026-09-15 批26，用户批评后挖出批25 遗留）

- **形态**：批26-11 挪因子加载进 `worker_process_init`，方案验收写明"celery worker 本地起一次冒烟"被跳过，拿 mock 单测 1055 绿宣布收官提交。用户点破后补做行为冒烟，当场挖出两个真问题：
  1. **批25 遗留（影响生产）**：celery prefork 子进程 fork 继承 SQLAlchemy 池连接——父子共享同一 backend session，psycopg3 auto-prepare（同语句 5 次触发）注册的 prepared statement（`_pg3_N`）被继承 → 重启/max-tasks-per-child 回收后的子进程 log_sink 写库撞 `already exists` **整批 drop**（首子进程周期正常、之后全丢——批25 上产后 celery 子进程日志一直在悄悄丢）。根修：`db.py::dispose_fork_inherited_connections()`（`_engine.dispose(close=False)`——丢弃继承连接但不向服务端 terminate，防误杀父进程共享 session），`worker_process_init` 最先调用。
  2. print 在子进程 init 时刻 celery 的 stdout 重定向尚未接管 → 输出掉进无权限读的 journal 盲区=不可观测。改 `logger.info` 走 log_sink→system_log 恒可观测。
- **规约**：方案里的验收清单是契约逐条打勾，"pytest 绿"不覆盖"行为冒烟"类验收（两类证据）；凡涉及进程模型（fork/信号处理器/连接池）的改动必须起真进程冒烟——mock 测不到 fork 语义。教训入记忆 `handoff-no-shortcuts`（两案在录）。
- **诊断技巧**：子进程"没输出"先分清 stdout 链（print→journal，可能盲区）与 logging 链（sink→system_log，可查表）；`[log_sink] batch dropped` 字样=落库失败的直接线索。

## 2026-09-18（批45~52 马拉松日）

- **`#` 不是 JS 注释**：脚本插注释用 `#` 直接语法炸（批42 `//` 吞键教训同源再加一等号）——守门三层复跑当场抓。教训收敛：**任何脚本改 locales，改后必复跑 check-locales 再 build**。
- **pkill -f 自匹配再犯两次**（批37 教训在案仍复发）：pgrep 也一样——命令行含目标串即自匹配自杀（exit 144）。正解：字符类断自匹配 `pgrep -f "uvicorn[ ]src.web_api"` 或按精确 PID。
- **regex 全局删词条键误伤别域**（守门吞键检测当场抓出回退）：删键必须域内锚定（llm: {...} 块内正则），禁全文件键名扫描。
- **半完成修复再现**（批44 模式）：批50 `_failover_params` 建了没接线（测试仍绿——因走旧路径）——编码后**消费点 grep 核对**是必做一步，不只看测试绿。
- **Ansible 同步工作区非 git**：未提交文件会进 staged（批50 的 0088 被 DDL 门拦即此）——发布前工作区状态=部署内容，跨批工作期发布需 stash 或同车。
