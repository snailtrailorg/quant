"""引擎运行时骨架（批 2，重构计划 12 号 §2.9）。

单点化三引擎复制主循环的公共职责：到期驱动钩子 / 看门狗 / 事件线程存活检查 /
时段沿与会话计数 / L2 监督 / 告警策略 / 订阅管理。hub 首迁（批 2），
runner/worker 随后（批 3）。设计原则：到期驱动废 counter%N 相位耦合
（hub 10s/5s flush 窗口历史坑的结构性根治）。
"""
