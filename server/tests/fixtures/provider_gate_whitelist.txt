# provider 分支豁免白名单（CI 断言一——29 号 §三立法）
# 格式：一行一个模块 dotted path（相对 src/，# 注释行）
# 现状基线（2026-09-20 首轮全量扫描）：零违规——多源路由走 _ADAPTERS 注册表查表，
# 全仓无 if provider== 形态硬编码。本文件保留空基线：未来新增违规须走此文件显式豁免
# （adapter 方言处理类），M3 起目标是保持为零。
